---
phase: 06-frontend-cleanup-final-ci-gates
plan: 05
type: execute
wave: 5
depends_on:
  - 04
files_modified:
  - backend/requirements.txt
  - backend/tests/test_pyjwt_migration.py
  - backend/tests/dependencies/test_auth_utils.py
autonomous: true
requirements:
  - QUAL-05
requirements_addressed:
  - QUAL-05
must_haves:
  truths:
    - "backend/requirements.txt pins sqlalchemy==2.0.49, alembic==1.18.4, uvicorn==0.45.0"
    - "backend/requirements.txt no longer contains `python-jose` (line removed entirely per D-14)"
    - "backend/tests/test_pyjwt_migration.py is deleted from the working tree (D-14)"
    - "backend/tests/dependencies/test_auth_utils.py imports PyJWT (`import jwt`) not python-jose (D-23)"
    - "cd backend && pytest -n auto exits 0 under the upgraded stack"
    - "cd backend && bash scripts/test_migration_round_trip.sh exits 0 (Phase 4 plan 04-06 Alembic 1.18 canary)"
    - "No file under backend/ imports python-jose (grep 'from jose\\|import jose' returns zero hits)"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "pinned sqlalchemy/alembic/uvicorn bumps + python-jose removal (PR-B bump)"
      contains: "sqlalchemy==2.0.49"
    - path: "backend/tests/dependencies/test_auth_utils.py"
      provides: "auth utility test suite migrated to PyJWT (D-23)"
      contains: "import jwt"
  key_links:
    - from: "backend/requirements.txt"
      to: "backend/tests/test_migration_round_trip.sh (via scripts/)"
      via: "Alembic 1.18 round-trip canary from Phase 4 plan 04-06"
      pattern: "alembic upgrade head"
    - from: "backend/tests/dependencies/test_auth_utils.py"
      to: "backend/app/api/dependencies/auth.py"
      via: "Shared PyJWT import pattern (`import jwt`)"
      pattern: "^import jwt$"
---

<objective>
Wave 5 ships PR-B of the two-PR upgrade train (D-11): SQLAlchemy 2.0.41 -> 2.0.49 + Alembic 1.16 -> 1.18.4 + Uvicorn 0.34 -> 0.45.0 + python-jose removal (D-14). This is the lower-risk patch PR per D-11 ordering; it lands after Plan 06-04 (PR-A) merges so that if any regression bisects to this phase it is clearly isolated to this patch set, not conflated with the FastAPI + Pydantic upgrade.

Purpose: D-14 explicitly closes the Phase 5 deferred follow-up — python-jose was kept through Phase 5 only to power `test_pyjwt_migration.py` byte-identity parity. That test is no longer load-bearing (PyJWT is already the production JWT library; no pre-migration tokens exist in flight). Deleting jose AND migrating the last remaining jose consumer (`backend/tests/dependencies/test_auth_utils.py` per D-23) removes the last jose dependency.

Output: requirements.txt bumped for sqlalchemy/alembic/uvicorn + python-jose line removed; test_pyjwt_migration.py deleted; test_auth_utils.py migrated to PyJWT; full backend test suite green; Alembic round-trip script green; zero `from jose` / `import jose` remains.
</objective>

<execution_context>
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/workflows/execute-plan.md
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-CONTEXT.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-RESEARCH.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-PATTERNS.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-VALIDATION.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-04-PLAN.md
@backend/requirements.txt
@backend/tests/dependencies/test_auth_utils.py
@backend/app/api/dependencies/auth.py
@backend/app/api/endpoints/auth/core.py

<interfaces>
Target pins (RESEARCH.md Standard Stack, verified 2026-04-23):
- `sqlalchemy==2.0.49` (was 2.0.41)
- `alembic==1.18.4` (was 1.16.x — RESEARCH.md notes 1.18.4 is latest patch of the 1.18 minor line CONTEXT.md D-11 targets)
- `uvicorn==0.45.0` (was 0.34.x — CONTEXT.md D-11 target is 0.45; 0.46 exists but 0.45 hits the QUAL-05 requirement exactly)

Lines to DELETE from requirements.txt (D-14 per PATTERNS.md Wave 5):
```
# python-jose — KEPT through Phase 5 only for test_pyjwt_migration.py parity assertion.
# Scheduled for removal in Phase 6 dependency cleanup.
# Note: python-jose depends on ecdsa (CVE-2024-23342), not exploitable in HS256-only usage.
python-jose[cryptography]==3.5.0
```

Exact location: `backend/requirements.txt` lines 30-33 per PATTERNS.md §Wave 5 requirements.txt section.

Files affected by python-jose removal:
1. `backend/tests/test_pyjwt_migration.py` — DELETE entirely. This file existed solely to prove byte-identity parity between jose-signed and PyJWT-signed tokens during the Phase 5 AUTH-04 migration. With PyJWT in prod and no pre-migration tokens in flight, the test has no guarded invariant.
2. `backend/tests/dependencies/test_auth_utils.py` line 3 — MIGRATE from `from jose import jwt` to `import jwt`. Per PATTERNS.md §Wave 5 test_auth_utils.py section, the `jwt.encode` / `jwt.decode` API is signature-identical between jose and PyJWT for HS256; the ONLY change is the import statement. Verify by reading the file's current jwt calls and confirming each fits PyJWT's signature.

Production canonical PyJWT import (PATTERNS.md cites this as the canonical pattern):
```python
# backend/app/api/dependencies/auth.py line 7
import jwt
```
Also present at:
- `backend/app/api/endpoints/auth/core.py:12`
- `backend/app/api/endpoints/auth/webauthn.py:14`
- `backend/app/api/endpoints/auth/oauth.py:13`

PyJWT API notes (PATTERNS.md + RESEARCH.md cross-reference):
- `jose.jwt.encode(claims, key, algorithm='HS256')` -> `jwt.encode(claims, key, algorithm='HS256')` — identical signature.
- `jose.jwt.decode(token, key, algorithms=['HS256'])` -> `jwt.decode(token, key, algorithms=['HS256'])` — identical signature.
- `jose.JWTError` -> `jwt.InvalidTokenError` — migrate any exception handlers. Per PATTERNS.md, a scan of test_auth_utils.py's current 72 lines found NO exception handlers, but confirm during edit.

Post-removal sanity checks:
- `grep -rn "from jose\|import jose" backend/` MUST return zero hits.
- `pip show python-jose` after reinstall MUST fail (package uninstalled).

Alembic 1.18 canary (D-13):
- `backend/scripts/test_migration_round_trip.sh` (from Phase 4 plan 04-06). Runs `alembic upgrade head` + `alembic downgrade base` + `alembic upgrade head` against a Postgres Docker instance, asserting the round-trip is clean. This is THE Alembic 1.18 canary per D-13.

Uvicorn 0.45 notes:
- Minor version bump from 0.34 -> 0.45 crosses many minors, but Uvicorn has a strong backward-compat track record for 0.x releases.
- Main surface change to watch: `--reload` dir watcher, `--workers` handling, and ASGI spec-revision defaults. The `lifespan` hook (used in backend/app/main.py) is stable across this range.
- No code changes expected; this is requirements.txt only for Uvicorn.

SQLAlchemy 2.0.41 -> 2.0.49 notes:
- Patch-range bump within 2.0.x; changelog shows only bug fixes and minor dialect improvements.
- No code changes expected. Phase 4 plan 04-04 already migrated every `session.query()` call to `select()` + `session.scalars()`, so the critical 2.0-era API surface is already modernized.

diagnostic escape hatch: if xdist misleads, `pytest -p no:xdist -x` to serialize.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate test_auth_utils.py from python-jose to PyJWT (D-23)</name>
  <read_first>
    - backend/tests/dependencies/test_auth_utils.py (full file — 72 lines per PATTERNS.md)
    - backend/app/api/dependencies/auth.py line 7 (canonical PyJWT import)
    - backend/app/api/endpoints/auth/core.py (secondary PyJWT import reference)
    - .planning/phases/06-frontend-cleanup-final-ci-gates/06-PATTERNS.md Wave 5 test_auth_utils.py section
  </read_first>
  <behavior>
    - backend/tests/dependencies/test_auth_utils.py line 3 imports `jwt` from PyJWT instead of jose
    - All `jwt.encode(...)` / `jwt.decode(...)` call sites still work (signature-identical between jose and PyJWT)
    - Any `jose.JWTError` catches (if present) are migrated to `jwt.InvalidTokenError`
    - cd backend && pytest -n auto -x tests/dependencies/test_auth_utils.py exits 0
    - grep -c "from jose\|import jose" backend/tests/dependencies/test_auth_utils.py returns 0
  </behavior>
  <action>
STEP 1 — Read `backend/tests/dependencies/test_auth_utils.py` in full. Identify:
- The jose import line (expected line 3: `from jose import jwt`)
- Every call site of `jwt.encode(...)`, `jwt.decode(...)`, or any other jose symbol
- Any exception handler catching `jose.JWTError` or similar jose-specific exceptions

STEP 2 — Replace line 3:
```python
# BEFORE
from jose import jwt
# AFTER
import jwt
```

STEP 3 — Verify each call site. The PyJWT API for HS256 is signature-identical to jose for `encode` / `decode`, so most call sites should require zero additional changes:
```python
# Unchanged — works identically in PyJWT:
jwt.encode(claims, settings.SECRET_KEY, algorithm="HS256")
jwt.decode(token, settings.SECRET_KEY, algorithms=["HS256"])
```

If any call uses a jose-specific keyword argument (e.g., `jose.jwt.encode`'s `headers=` argument is supported in PyJWT with identical semantics), verify by cross-referencing PyJWT docs (context7 if available: `library: PyJWT`, query: `jwt.encode jwt.decode HS256 algorithm`).

STEP 4 — Migrate exception handlers (if any):
```python
# BEFORE (if present)
try:
    jwt.decode(token, key, algorithms=['HS256'])
except jose.JWTError:
    ...
# AFTER
try:
    jwt.decode(token, key, algorithms=['HS256'])
except jwt.InvalidTokenError:
    ...
```
Per PATTERNS.md scan, test_auth_utils.py currently has no exception handlers — verify during edit.

STEP 5 — Run the migrated test file in isolation:
```bash
cd backend
pytest -n auto -x tests/dependencies/test_auth_utils.py -v
```

Must exit 0 with every test passing. If a test fails due to a jose-vs-PyJWT semantic difference (rare for HS256), document and fix the specific differing API call.

STEP 6 — Confirm no jose references remain in THIS file:
```bash
grep -n "jose" backend/tests/dependencies/test_auth_utils.py   # must return zero hits
```

Do NOT delete test_pyjwt_migration.py in this task — Task 2 handles it alongside requirements.txt and broader jose removal.
  </action>
  <verify>
    <automated>cd backend && ! grep -n "from jose\|import jose" tests/dependencies/test_auth_utils.py && grep -q "^import jwt$" tests/dependencies/test_auth_utils.py && pytest -n auto -x tests/dependencies/test_auth_utils.py</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "from jose\|import jose" backend/tests/dependencies/test_auth_utils.py returns 0
    - grep -q "^import jwt$" backend/tests/dependencies/test_auth_utils.py exits 0
    - grep -q "jose" backend/tests/dependencies/test_auth_utils.py returns 0 (no comment or reference to jose remains)
    - cd backend && pytest -n auto -x tests/dependencies/test_auth_utils.py exits 0
  </acceptance_criteria>
  <done>test_auth_utils.py migrated to PyJWT; tests green; zero jose references remain in this file.</done>
</task>

<task type="auto">
  <name>Task 2: Bump sqlalchemy/alembic/uvicorn; delete python-jose + test_pyjwt_migration.py; full gauntlet</name>
  <read_first>
    - backend/requirements.txt (full file — identify current sqlalchemy, alembic, uvicorn, python-jose lines)
    - .planning/phases/06-frontend-cleanup-final-ci-gates/06-RESEARCH.md Standard Stack (verified version pins)
    - .planning/phases/06-frontend-cleanup-final-ci-gates/06-PATTERNS.md Wave 5 requirements.txt + test_pyjwt_migration.py sections
    - backend/scripts/test_migration_round_trip.sh (Phase 4 plan 04-06 canary)
    - backend/tests/test_pyjwt_migration.py (file being deleted — read to confirm it references jose and therefore breaks after jose removal)
  </read_first>
  <behavior>
    - backend/requirements.txt has `sqlalchemy==2.0.49`
    - backend/requirements.txt has `alembic==1.18.4`
    - backend/requirements.txt has `uvicorn==0.45.0`
    - backend/requirements.txt does NOT contain `python-jose` on any line (including any comment-only reference — the 3-line comment block above the pin is also removed for cleanliness)
    - backend/tests/test_pyjwt_migration.py is DELETED from the working tree (`git rm` or equivalent)
    - fastapi==0.136.x and pydantic==2.13.x (from Plan 06-04) remain UNCHANGED
    - cd backend && pip install -r requirements.txt completes without resolution errors
    - cd backend && pip show python-jose fails (package uninstalled)
    - grep -rn "from jose\|import jose" backend/ returns zero hits
    - cd backend && pytest -n auto exits 0 under the upgraded stack
    - cd backend && bash scripts/test_migration_round_trip.sh exits 0 (Alembic 1.18 canary)
  </behavior>
  <action>
STEP 1 — Edit `backend/requirements.txt`:

Apply these version bumps (keep `==` pin syntax; do NOT use `~=` or `>=`):
```
sqlalchemy==2.0.41 -> sqlalchemy==2.0.49
alembic==1.16.x -> alembic==1.18.4
uvicorn==0.34.x -> uvicorn==0.45.0
```

DELETE these lines from requirements.txt (the 3-line comment block + the pin itself per PATTERNS.md §Wave 5):
```
# python-jose — KEPT through Phase 5 only for test_pyjwt_migration.py parity assertion.
# Scheduled for removal in Phase 6 dependency cleanup.
# Note: python-jose depends on ecdsa (CVE-2024-23342), not exploitable in HS256-only usage.
python-jose[cryptography]==3.5.0
```

Note: PATTERNS.md §Wave 5 cites exact lines 30-33 for the jose block. If the lines drift, grep `python-jose\|# python-jose\|ecdsa` to locate the block at edit time.

DO NOT touch:
- `fastapi==0.136.1` (Plan 06-04 owns)
- `pydantic==2.13.3` (Plan 06-04 owns)
- Any other line

STEP 2 — Reinstall and verify:
```bash
cd backend
pip install -r requirements.txt
pip show sqlalchemy | grep Version   # confirms 2.0.49
pip show alembic | grep Version      # confirms 1.18.4
pip show uvicorn | grep Version      # confirms 0.45.0
pip show python-jose 2>&1 | grep -q "Package(s) not found" && echo "jose uninstalled" || echo "FAIL: jose still installed"
```

If `pip show python-jose` does NOT fail (meaning a transitive dep still requires it), check `pip show python-jose` output for "Required-by:" — identify the transitive consumer and either upgrade that consumer or add an explicit `--no-deps` workaround. In practice no current requirement pulls jose transitively (PyJWT is the only JWT dep now), so this should succeed on first install.

STEP 3 — Delete `backend/tests/test_pyjwt_migration.py`:
```bash
cd backend
git rm tests/test_pyjwt_migration.py
# verify:
test ! -f tests/test_pyjwt_migration.py && echo "deleted OK"
```

STEP 4 — Global jose grep (D-14 static check):
```bash
grep -rn "from jose\|import jose" backend/
# must return zero hits
# NOTE: a literal "jose" substring anywhere in comments/docstrings is acceptable;
# only real import statements are forbidden.
```

If any hit remains, locate the file and migrate it to PyJWT (mirror Task 1's migration pattern). None is expected — Task 1 handled the last consumer.

STEP 5 — Run the test gauntlet in diagnostic-funnel order:
```bash
cd backend

# 5a: test_auth_utils.py (from Task 1 — confirm PyJWT migration still green under new stack)
pytest -n auto -x tests/dependencies/test_auth_utils.py

# 5b: Alembic 1.18 round-trip canary (D-13, Phase 4 plan 04-06)
bash scripts/test_migration_round_trip.sh

# 5c: Full suite under upgraded stack
pytest -n auto --cov=app --cov-report=term-missing
```

If 5b fails: Alembic 1.18 has a real schema or operation regression. Read the failure, identify which migration revision fails on upgrade or downgrade. Alembic 1.18 release notes identify the likely area. Fix or revert the bump if the fix is out of scope for this plan.

If 5c fails on anything not caught by 5a/5b: fix the underlying issue. Most likely surface: SQLAlchemy 2.0.49 minor bug fixes that tighten edge cases already exercised by the test suite.

STEP 6 — Confirm nothing from Plan 06-04 was accidentally reverted:
```bash
grep -q '^fastapi==0.136' backend/requirements.txt && echo "fastapi preserved"
grep -q '^pydantic==2.13' backend/requirements.txt && echo "pydantic preserved"
```

STEP 7 — Commit the bumps + deletion. Single commit is OK since the changes are tightly coupled (jose removal is conceptually one change across requirements.txt + the deleted test).
  </action>
  <verify>
    <automated>grep -q "^sqlalchemy==2.0.49" backend/requirements.txt && grep -q "^alembic==1.18" backend/requirements.txt && grep -q "^uvicorn==0.45" backend/requirements.txt && ! grep -q "python-jose" backend/requirements.txt && test ! -f backend/tests/test_pyjwt_migration.py && ! grep -rn "from jose\|import jose" backend/ && cd backend && pytest -n auto -x tests/dependencies/test_auth_utils.py && bash scripts/test_migration_round_trip.sh</automated>
  </verify>
  <acceptance_criteria>
    - grep -q '^sqlalchemy==2.0.49' backend/requirements.txt exits 0
    - grep -q '^alembic==1.18' backend/requirements.txt exits 0
    - grep -q '^uvicorn==0.45' backend/requirements.txt exits 0
    - grep -q 'python-jose' backend/requirements.txt returns nothing (pin + comment block both removed)
    - grep -q '^fastapi==0.136' backend/requirements.txt still exits 0 (Plan 06-04 preserved)
    - grep -q '^pydantic==2.13' backend/requirements.txt still exits 0 (Plan 06-04 preserved)
    - test ! -f backend/tests/test_pyjwt_migration.py exits 0 (file deleted)
    - grep -rn 'from jose\|import jose' backend/ returns zero hits
    - cd backend && pip show python-jose fails (package uninstalled)
    - cd backend && python -c "import sqlalchemy; print(sqlalchemy.__version__)" prints 2.0.49
    - cd backend && python -c "import alembic; print(alembic.__version__)" prints a version starting with 1.18
    - cd backend && pytest -n auto --cov=app --cov-report=term-missing exits 0
    - cd backend && bash scripts/test_migration_round_trip.sh exits 0
  </acceptance_criteria>
  <done>sqlalchemy/alembic/uvicorn bumped; python-jose removed from requirements.txt AND uninstalled; test_pyjwt_migration.py deleted; zero jose imports in backend/; full test suite + Alembic round-trip canary green; Plan 06-04 pins preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip index -> backend runtime | SQLAlchemy / Alembic / Uvicorn patch bumps are freshly installed; their advisories + release notes are the threat surface. |
| JWT library surface | Removing python-jose eliminates the historic CVE-2024-23342 (ecdsa transitive) supply-chain surface for the project. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-21 | Tampering | SQLAlchemy 2.0.41 -> 2.0.49 minor bug-fix creep | mitigate | Phase 4 plan 04-04 already migrated session.query -> select; the 2.0.x surface is clean. Full pytest suite gates merge. |
| T-06-22 | Tampering | Alembic 1.16 -> 1.18 migration operator surface | mitigate | Phase 4 plan 04-06 `test_migration_round_trip.sh` canary exercises upgrade+downgrade+upgrade under Alembic 1.18 against Postgres Docker. |
| T-06-23 | Information Disclosure | python-jose ecdsa transitive CVE-2024-23342 | mitigate | Removing python-jose entirely eliminates the vulnerable transitive dependency (ecdsa). HS256-only usage was already not exploitable, but the removal closes the attack surface permanently. |
| T-06-24 | Denial of Service | Uvicorn 0.34 -> 0.45 ASGI spec revision | accept | Uvicorn 0.x has a strong backward-compat track record; lifespan hook + ASGI 3.0 semantics stable across the range. Full pytest suite catches any regression. |
| T-06-25 | Elevation of Privilege | JWT-library migration (test_auth_utils.py) | mitigate | PyJWT is already production canonical (backend/app/api/dependencies/auth.py:7). This task just aligns the test utility with the production library. PyJWT HS256 API is signature-identical to jose for encode/decode. |
</threat_model>

<verification>
Before merging Plan 06-05:
1. cd backend && pytest -n auto --cov=app --cov-report=term-missing — exit 0
2. cd backend && pytest -n auto -x tests/dependencies/test_auth_utils.py — exit 0
3. cd backend && bash scripts/test_migration_round_trip.sh — exit 0
4. grep -q '^sqlalchemy==2.0.49' backend/requirements.txt — exit 0
5. grep -q '^alembic==1.18' backend/requirements.txt — exit 0
6. grep -q '^uvicorn==0.45' backend/requirements.txt — exit 0
7. ! grep -q 'python-jose' backend/requirements.txt — exit 0
8. test ! -f backend/tests/test_pyjwt_migration.py — exit 0
9. ! grep -rn 'from jose\|import jose' backend/ — exit 0
10. grep -q '^fastapi==0.136' backend/requirements.txt still exits 0 (Plan 06-04 preserved)
11. grep -q '^pydantic==2.13' backend/requirements.txt still exits 0 (Plan 06-04 preserved)
</verification>

<success_criteria>
- QUAL-05 PR-B complete: SQLAlchemy 2.0.49 + Alembic 1.18.4 + Uvicorn 0.45.0 pinned; full test suite green
- D-14 closes: python-jose removed from requirements.txt; test_pyjwt_migration.py deleted; zero jose imports remain
- D-23 closes: test_auth_utils.py migrated to PyJWT; tests green
- Alembic 1.18 round-trip canary green under the upgraded stack
- Plan 06-04 pins (FastAPI 0.136.1 + Pydantic 2.13.3) preserved
- All acceptance_criteria on both tasks pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-cleanup-final-ci-gates/06-05-SUMMARY.md` per template. SUMMARY must record: (a) final pinned versions for sqlalchemy/alembic/uvicorn, (b) confirmation that python-jose is uninstalled from the local environment, (c) whether the Alembic round-trip script passed without any inline migration fixes, (d) any jose consumers found besides test_auth_utils.py + test_pyjwt_migration.py (expected: none).
</output>
