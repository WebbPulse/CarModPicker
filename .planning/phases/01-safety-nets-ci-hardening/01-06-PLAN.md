---
phase: 01-safety-nets-ci-hardening
plan: 06
type: execute
wave: 5
depends_on: [04]
files_modified:
  - backend/requirements.txt
  - backend/tests/conftest.py
  - backend/tests/auth/__init__.py
  - backend/tests/auth/test_characterization_signup_verify.py
  - backend/tests/auth/test_characterization_login.py
  - backend/tests/auth/test_characterization_2fa_totp.py
  - backend/tests/auth/test_characterization_webauthn.py
  - backend/tests/auth/test_characterization_oauth_signin.py
  - backend/tests/auth/test_characterization_oauth_link.py
  - backend/tests/auth/test_characterization_password_reset.py
  - backend/tests/cassettes/auth/.gitkeep
  - backend/tests/test_cassette_secret_audit.py
autonomous: true
requirements:
  - SAFE-06
tags:
  - auth
  - characterization
  - vcr
  - pytest-recording
  - security

must_haves:
  truths:
    - "All 7 SAFE-06 happy-path auth flows have a dedicated `backend/tests/auth/test_characterization_*.py` file: (1) signup→verify-email, (2) login (email/password), (3) 2FA-TOTP enrollment+challenge, (4) WebAuthn passkey registration+assertion, (5) Google OAuth sign-in, (6) Google OAuth account link, (7) password-reset request→reset"
    - "Each characterization test asserts HTTP status + response-JSON key presence + DB state change (D-19 assertion depth) — NOT exhaustive per-field equality"
    - "`pytest-recording==0.13.4` is added to `backend/requirements.txt` in the existing `# Testing dependencies` block"
    - "A `vcr_config` fixture is present in `backend/tests/conftest.py` (appended near end) with `filter_headers=['authorization','cookie','set-cookie','x-goog-api-key']`, `filter_post_data_parameters=['client_secret','code','refresh_token']`, `filter_query_parameters=['api_key','access_token']`, `record_mode='none'`"
    - "WebAuthn test stubs the 4 cryptographic boundary functions from `webauthn.*` via `unittest.mock.patch` — NOT over HTTP (D-18)"
    - "A secret-audit test (`backend/tests/test_cassette_secret_audit.py`) scans every committed `backend/tests/cassettes/**/*.yaml` file for banned tokens (`authorization:`, `set-cookie:`, raw OAuth access tokens) and fails if any appear un-scrubbed — defense against T-06-01"
    - "Phase 1 Success Criterion #4 (`Auth characterization tests cover all 7+ happy-path flows and are CI-green`) is satisfied by this plan"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "Adds pytest-recording==0.13.4 to the Testing dependencies block"
      contains: "pytest-recording==0.13.4"
    - path: "backend/tests/conftest.py"
      provides: "vcr_config fixture with secret scrubbing"
      contains: "vcr_config"
    - path: "backend/tests/auth/__init__.py"
      provides: "Package marker for the new auth/ subdirectory"
    - path: "backend/tests/auth/test_characterization_signup_verify.py"
      provides: "Characterization for flow 1 (signup → verify-email)"
      min_lines: 40
    - path: "backend/tests/auth/test_characterization_login.py"
      provides: "Characterization for flow 2 (email/password login)"
      min_lines: 40
    - path: "backend/tests/auth/test_characterization_2fa_totp.py"
      provides: "Characterization for flow 3 (2FA-TOTP enrollment + challenge)"
      min_lines: 50
    - path: "backend/tests/auth/test_characterization_webauthn.py"
      provides: "Characterization for flow 4 (WebAuthn registration + assertion)"
      min_lines: 60
    - path: "backend/tests/auth/test_characterization_oauth_signin.py"
      provides: "Characterization for flow 5 (Google OAuth sign-in, VCR-replayed)"
      min_lines: 40
    - path: "backend/tests/auth/test_characterization_oauth_link.py"
      provides: "Characterization for flow 6 (Google OAuth account link, VCR-replayed)"
      min_lines: 40
    - path: "backend/tests/auth/test_characterization_password_reset.py"
      provides: "Characterization for flow 7 (password-reset request → reset)"
      min_lines: 50
    - path: "backend/tests/cassettes/auth/.gitkeep"
      provides: "Cassette directory marker (cassettes themselves generated by the user running `pytest --record-mode=once` locally — see output section)"
    - path: "backend/tests/test_cassette_secret_audit.py"
      provides: "Secret-audit test that greps committed cassettes for banned tokens"
      min_lines: 30
  key_links:
    - from: "backend/tests/auth/test_characterization_oauth_signin.py"
      to: "backend/tests/cassettes/auth/test_characterization_oauth_signin/test_google_oauth_signin.yaml (generated locally)"
      via: "@pytest.mark.vcr decorator; pytest-recording auto-resolves cassette path from test module + function name"
      pattern: "@pytest.mark.vcr"
    - from: "backend/tests/conftest.py vcr_config fixture"
      to: "All `@pytest.mark.vcr` tests"
      via: "pytest-recording fixture injection"
      pattern: "vcr_config"
    - from: "backend/tests/test_cassette_secret_audit.py"
      to: "backend/tests/cassettes/**/*.yaml"
      via: "glob scan + substring check for banned tokens"
      pattern: "cassettes/**/*.yaml"
---

<objective>
Implement SAFE-06: pin current auth behavior across 7 happy-path flows via characterization tests. The 2 Google-OAuth flows use `pytest-recording` VCR cassettes (filter-scrubbed); WebAuthn uses `unittest.mock.patch` at the library boundary (D-18); signup/login/TOTP/password-reset need neither (pure TestClient + DB assertions). Add a secret-audit test that fails CI if any committed cassette leaks an un-scrubbed token.

Purpose: Phase 1 Success Criterion #4 and the guardrail that Phase 5's auth refactor (AUTH-01…AUTH-06) depends on. Without these tests, decomposing `auth.py` (1,195 lines) into `auth/` package risks silent behavioral regression in a Chrome-extension critical path.

Output:
- 7 test files in a new `backend/tests/auth/` package directory.
- 1 `vcr_config` fixture appended to conftest.py.
- 1 line added to requirements.txt.
- Cassette directory placeholder.
- 1 secret-audit test.
</objective>

<execution_context>
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/workflows/execute-plan.md
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/REQUIREMENTS.md
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/phases/01-safety-nets-ci-hardening/01-CONTEXT.md
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/phases/01-safety-nets-ci-hardening/01-RESEARCH.md
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/phases/01-safety-nets-ci-hardening/01-PATTERNS.md
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/phases/01-safety-nets-ci-hardening/01-VALIDATION.md
@/home/tyler-webb/Documents/Github/CarModPicker/CLAUDE.md
@/home/tyler-webb/Documents/Github/CarModPicker/backend/tests/conftest.py
@/home/tyler-webb/Documents/Github/CarModPicker/backend/tests/api/endpoints/test_auth.py
@/home/tyler-webb/Documents/Github/CarModPicker/backend/tests/api/endpoints/test_webauthn.py
@/home/tyler-webb/Documents/Github/CarModPicker/backend/tests/api/endpoints/test_google_oauth.py
@/home/tyler-webb/Documents/Github/CarModPicker/backend/app/api/endpoints/auth.py
@/home/tyler-webb/Documents/Github/CarModPicker/backend/app/api/utils/google_oauth.py
@/home/tyler-webb/Documents/Github/CarModPicker/backend/requirements.txt

<interfaces>
pytest-recording public surface (what we install + use):

    import pytest
    @pytest.mark.vcr                 # records/replays HTTP calls made inside the test
    def test_foo(): ...

    @pytest.fixture
    def vcr_config() -> dict:        # per-test/module scoped fixture read by pytest-recording
        return {
            "filter_headers": [...],
            "filter_post_data_parameters": [...],
            "filter_query_parameters": [...],
            "record_mode": "none",   # default on CI; local dev uses --record-mode=once
            "match_on": (...),
        }

FastAPI app endpoints under test (verified from backend/app/api/endpoints/auth.py + utils/google_oauth.py):

    POST /api/users/                      # signup
    POST /api/auth/verify-email           # email verification
    POST /api/auth/token                  # login (form-encoded)
    POST /api/auth/2fa/totp/enroll        # TOTP enrollment
    POST /api/auth/2fa/totp/verify        # TOTP verify
    POST /api/auth/webauthn/register/begin
    POST /api/auth/webauthn/register/complete
    POST /api/auth/google                 # Google OAuth sign-in / signup
    POST /api/auth/google/link            # Link Google account to existing user
    POST /api/auth/forgot-password        # password reset request
    POST /api/auth/reset-password         # password reset completion

(Exact endpoint paths must be re-verified by reading auth.py at implementation time — the endpoint module is the source of truth.)

WebAuthn library boundary (4 functions to patch):

    webauthn.generate_registration_options
    webauthn.verify_registration_response
    webauthn.generate_authentication_options
    webauthn.verify_authentication_response

Patch location: the auth endpoint module imports these as module-local names, so patch `app.api.endpoints.auth.<func>` not `webauthn.<func>`.

Google OAuth boundary (VCR-replayable):

    google.oauth2.id_token.verify_oauth2_token
    google.auth.transport.requests.Request  # internal HTTPS call to JWKS endpoint

vcrpy intercepts both via urllib3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pytest-recording + add vcr_config fixture + package marker + cassette dir</name>
  <files>
    backend/requirements.txt,
    backend/tests/conftest.py,
    backend/tests/auth/__init__.py,
    backend/tests/cassettes/auth/.gitkeep
  </files>
  <read_first>
    - backend/requirements.txt (full file — identify the `# Testing dependencies` block at lines 54-62)
    - backend/tests/conftest.py (full file — understand fixture conventions + env-var setup; the new fixture goes at the END of the file)
    - backend/tests/crawlers/__init__.py (confirm convention for a per-package __init__.py — it is likely empty)
    - .planning/phases/01-safety-nets-ci-hardening/01-PATTERNS.md §7 (requirements.txt insertion point) and §13 (cassette storage)
    - .planning/phases/01-safety-nets-ci-hardening/01-CONTEXT.md D-15, D-17, D-18 (locked tool + storage + mocking strategy)
    - .planning/phases/01-safety-nets-ci-hardening/01-RESEARCH.md lines 821-856 (Code Example 6 — literal vcr_config body)
  </read_first>
  <action>
**Step A — add pytest-recording to requirements.txt:**

Insert the line `pytest-recording==0.13.4` between the existing `pytest-cov==6.2.1` line and `httpx==0.28.1` line (inside the `# Testing dependencies` block).

Before:

    pytest==9.0.3
    pytest-asyncio==1.3.0
    pytest-xdist==3.8.0
    pytest-cov==6.2.1
    httpx==0.28.1

After:

    pytest==9.0.3
    pytest-asyncio==1.3.0
    pytest-xdist==3.8.0
    pytest-cov==6.2.1
    pytest-recording==0.13.4
    httpx==0.28.1

Then install locally:

    cd /home/tyler-webb/Documents/Github/CarModPicker/backend && pip install -r requirements.txt

**Step B — append vcr_config fixture to conftest.py:**

Append the following block to the END of `backend/tests/conftest.py` (do NOT modify any existing fixtures):

    # -----------------------------------------------------------------------
    # SAFE-06: pytest-recording (vcrpy) configuration for auth characterization
    # tests. Scrubs headers, post-body, and query-params that might carry
    # production secrets. record_mode="none" means CI replays only — to record
    # a new cassette locally, run pytest with `--record-mode=once`.
    # -----------------------------------------------------------------------
    import pytest  # noqa: E402  (conftest imports at top are already satisfied)


    @pytest.fixture(scope="module")
    def vcr_config() -> dict:
        """VCR configuration consumed by pytest-recording's @pytest.mark.vcr."""
        return {
            "filter_headers": [
                ("authorization", "REDACTED"),
                ("cookie", "REDACTED"),
                ("set-cookie", "REDACTED"),
                ("x-goog-api-key", "REDACTED"),
            ],
            "filter_post_data_parameters": [
                ("client_secret", "REDACTED"),
                ("code", "REDACTED"),
                ("refresh_token", "REDACTED"),
            ],
            "filter_query_parameters": [
                ("api_key", "REDACTED"),
                ("access_token", "REDACTED"),
            ],
            "record_mode": "none",
            "match_on": ("method", "scheme", "host", "port", "path", "query"),
        }

Check whether `import pytest` is already at the top of conftest.py (it should be — it is conventional). If yes, the local `import pytest` in the block above is redundant but harmless; `black` may collapse it. Verify the top-of-file imports after editing.

**Step C — create package marker + cassette directory:**

    mkdir -p /home/tyler-webb/Documents/Github/CarModPicker/backend/tests/auth
    touch /home/tyler-webb/Documents/Github/CarModPicker/backend/tests/auth/__init__.py
    mkdir -p /home/tyler-webb/Documents/Github/CarModPicker/backend/tests/cassettes/auth
    touch /home/tyler-webb/Documents/Github/CarModPicker/backend/tests/cassettes/auth/.gitkeep

The `__init__.py` is empty (matches `backend/tests/crawlers/__init__.py` convention).

Format: `black --config pyproject.toml --line-length 120 backend/tests/conftest.py` + `isort backend/tests/conftest.py`.

Sanity:

    cd /home/tyler-webb/Documents/Github/CarModPicker/backend && pytest --collect-only 2>&1 | tail -5
    # Expect normal collection; no errors about vcr_config or pytest-recording.
  </action>
  <verify>
    <automated>cd /home/tyler-webb/Documents/Github/CarModPicker/backend && grep -c 'pytest-recording==0.13.4' requirements.txt && grep -c 'def vcr_config' tests/conftest.py && test -f tests/auth/__init__.py && test -f tests/cassettes/auth/.gitkeep && python -c "import pytest_recording; print('vcr installed')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/requirements.txt` contains the exact line `pytest-recording==0.13.4` (command: `grep -c '^pytest-recording==0.13.4$' backend/requirements.txt` returns 1)
    - The new line appears between `pytest-cov==6.2.1` and `httpx==0.28.1` (verified by `grep -n 'pytest-recording\|pytest-cov\|httpx==' backend/requirements.txt` showing correct ordering)
    - `backend/tests/conftest.py` contains the literal string `def vcr_config` (command: `grep -c 'def vcr_config' backend/tests/conftest.py` returns 1)
    - conftest.py's new block contains all four scrub configurations: `filter_headers`, `filter_post_data_parameters`, `filter_query_parameters`, `record_mode` (each grep returns 1)
    - conftest.py contains the 4 locked header filters: `("authorization"`, `("cookie"`, `("set-cookie"`, `("x-goog-api-key"`
    - conftest.py contains `"record_mode": "none"`
    - `backend/tests/auth/__init__.py` exists and is 0 bytes (`test -f backend/tests/auth/__init__.py && test ! -s backend/tests/auth/__init__.py`)
    - `backend/tests/cassettes/auth/.gitkeep` exists
    - `python -c "import pytest_recording"` exits 0 (package installed)
    - `cd backend && pytest --collect-only 2>&1 | head -3` shows no errors
    - `cd backend && pyright tests/conftest.py` reports 0 errors
    - `cd backend && black --config pyproject.toml --line-length 120 --check tests/conftest.py` exits 0
  </acceptance_criteria>
  <done>pytest-recording installed + in requirements.txt; vcr_config fixture committed with all 4 locked scrub lists + record_mode=none; auth/ package directory and cassettes/auth/ directory exist.</done>
</task>

<task type="auto">
  <name>Task 2: Write 4 non-VCR characterization tests (signup+verify, login, TOTP, password-reset)</name>
  <files>
    backend/tests/auth/test_characterization_signup_verify.py,
    backend/tests/auth/test_characterization_login.py,
    backend/tests/auth/test_characterization_2fa_totp.py,
    backend/tests/auth/test_characterization_password_reset.py
  </files>
  <read_first>
    - backend/app/api/endpoints/auth.py (the endpoint module being characterized — executor MUST read the actual endpoint implementations for each of: signup, verify-email, token, 2FA TOTP enroll/verify, forgot-password, reset-password. The exact URL paths, request body shapes, and response JSON keys come from this file.)
    - backend/tests/api/endpoints/test_auth.py (existing unit tests — copy the `get_unique_username` helper and the user-creation/login flow skeleton; lines 1-71 are the exact shape)
    - backend/tests/conftest.py (fixture topology: `client`, `db_session`, `test_user`, `mock_s3`)
    - .planning/phases/01-safety-nets-ci-hardening/01-CONTEXT.md D-16, D-19 (7 flows list + assertion depth)
    - .planning/phases/01-safety-nets-ci-hardening/01-PATTERNS.md §12 Analog A (literal imports + helpers)
    - backend/app/api/models/user.py (confirm the DBUser field names: `email_verified`, `disabled`, etc. — tests assert on these)
  </read_first>
  <action>
Create 4 files. Each test MUST:
- Use the `client: TestClient` and `db_session: Session` fixtures from conftest.
- Use the `get_unique_username` pattern from test_auth.py (PYTEST_XDIST_WORKER + os.getpid) so parallel runs don't collide.
- Assert per D-19: HTTP status code + expected response-JSON key presence + DB state change where relevant. DO NOT assert every field value.
- Format: `black --config pyproject.toml --line-length 120` + `isort` + pyright strict.
- DO NOT import `app.main` at module scope (C-05).

**File 1: `backend/tests/auth/test_characterization_signup_verify.py`** — Flow 1: signup → verify-email.

Read auth.py to find the signup endpoint (likely `POST /api/users/` — cross-reference with `backend/tests/api/endpoints/test_auth.py` which already exercises signup). Find the verify-email endpoint (likely `POST /api/auth/verify-email` with a `token` field). The existing test_auth.py file has the signup+verify pattern already — read it and adapt to the characterization shape.

Test body outline:

    """SAFE-06 flow 1: signup → email verification.

    Asserts the 2-step flow completes end-to-end with the committed DB effects.
    Per D-19 we pin HTTP status, response-key presence, and DB state change
    (user created + email_verified flipped to True).
    """
    import os

    from fastapi.testclient import TestClient
    from sqlalchemy.orm import Session

    from app.api.models.user import User as DBUser
    from app.core.config import settings


    def _uniq(base: str) -> str:
        worker = os.environ.get("PYTEST_XDIST_WORKER", "main")
        return f"{base}_{worker}_{os.getpid()}"


    def test_signup_and_verify_email(client: TestClient, db_session: Session) -> None:
        username = _uniq("sig_verify")
        email = f"{username}@example.com"
        password = "test_password_123!"

        # Signup
        signup = client.post(
            f"{settings.API_STR}/users/",
            json={"username": username, "email": email, "password": password},
        )
        assert signup.status_code == 200, signup.text
        body = signup.json()
        assert "id" in body
        assert body["username"] == username
        assert body["email"] == email

        # DB: user exists, email_verified is False
        db_user = db_session.query(DBUser).filter(DBUser.username == username).first()
        assert db_user is not None
        assert db_user.email_verified is False

        # Fetch the verification token. Source of truth is auth.py — in many
        # implementations verification tokens are emailed; the backend's
        # TESTING mode likely exposes them via the moto SES mock OR stores
        # them in the DB. Executor: read auth.py + user model to determine
        # the correct retrieval path, then populate `token` below.
        # If the project stores the token in DBUser.verification_token:
        #   token = db_user.verification_token
        # If the token is emitted via an SES send that moto captured:
        #   token = _extract_token_from_moto_mail_fixture(...)
        #
        # Use whichever mechanism existing test_auth.py uses for verify-email.
        # DO NOT hit a real SES endpoint.

        token = _get_verification_token(db_session, db_user)

        verify = client.post(
            f"{settings.API_STR}/auth/verify-email",
            json={"token": token},
        )
        assert verify.status_code in (200, 204), verify.text

        # DB: email_verified flipped True
        db_session.refresh(db_user)
        assert db_user.email_verified is True


    def _get_verification_token(db_session: Session, user: "DBUser") -> str:
        """Executor: implement per auth.py's token-storage mechanism."""
        # Placeholder — replace with the correct retrieval from DBUser or moto SES
        # capture. See backend/tests/api/endpoints/test_auth.py for the established
        # pattern in this repo.
        raise NotImplementedError

Executor MUST complete `_get_verification_token` per the mechanism actually used in auth.py — DO NOT commit the placeholder.

**File 2: `backend/tests/auth/test_characterization_login.py`** — Flow 2: email/password login.

Lift the `test_login_for_access_token_success` skeleton verbatim from `backend/tests/api/endpoints/test_auth.py` lines 34-71 (already cited in PATTERNS.md §12). Rename to `test_login_happy_path` and adjust the file-level docstring to say "SAFE-06 flow 2".

Assertions (D-19): status 200, `"access_token"` in response, `"token_type" == "bearer"`. DB state change: none (login is read-only) — omit that assertion.

**File 3: `backend/tests/auth/test_characterization_2fa_totp.py`** — Flow 3: TOTP enrollment + challenge.

Read auth.py to find the TOTP endpoints (likely `POST /api/auth/2fa/totp/enroll` + `POST /api/auth/2fa/totp/verify`). Existing test_auth.py or a sibling test may exercise TOTP already — crib shape from there.

Flow:
1. Login user (use fixture or create+login).
2. POST /2fa/totp/enroll → response contains `secret` (base32 TOTP secret) and `otpauth_url`.
3. Generate current TOTP code via `pyotp.TOTP(secret).now()`.
4. POST /2fa/totp/verify with `{code: <generated>}` → 200.
5. DB assert: user's `totp_enabled` (or equivalent) flag is True.

Imports include `import pyotp` (already a dep — verify in requirements.txt).

**File 4: `backend/tests/auth/test_characterization_password_reset.py`** — Flow 7: password reset request → reset.

Flow:
1. Create a verified user directly in DB (helper pattern from test_auth.py lines 19-31).
2. POST /api/auth/forgot-password with email → 200 or 204.
3. Retrieve the reset token (same mechanism as verification token — read auth.py).
4. POST /api/auth/reset-password with `{token, new_password}` → 200.
5. Attempt login with new password → 200 + access_token.
6. Attempt login with OLD password → 401.

Assertions (D-19): status + key presence + DB state (the user's `hashed_password` changed — read via `db_session.refresh(user)` then check `user.hashed_password != original_hash`).

**All 4 files** MUST pass the following acceptance bar:
- Each file contains exactly one `def test_` function per flow (1 test for flows 1/2/7, 1 test for flow 3 covering both enroll and verify).
- No `@pytest.mark.vcr` decorator on any of these four (they do not touch external HTTP).
- No network calls: the mock_s3 fixture + moto handles SES; direct DB manipulation handles verification/reset tokens.
- `cd backend && pytest -n auto tests/auth/ -x --no-cov` exits 0.
  </action>
  <verify>
    <automated>cd /home/tyler-webb/Documents/Github/CarModPicker/backend && pytest -n auto tests/auth/test_characterization_signup_verify.py tests/auth/test_characterization_login.py tests/auth/test_characterization_2fa_totp.py tests/auth/test_characterization_password_reset.py --no-cov -x -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All 4 test files exist in `backend/tests/auth/`
    - Each contains at least one `def test_` function and at least one `assert response.status_code` line
    - None of the 4 files contains `@pytest.mark.vcr` (these flows don't hit external HTTP) — command: `grep -c '@pytest.mark.vcr' backend/tests/auth/test_characterization_{signup_verify,login,2fa_totp,password_reset}.py` returns 0 for each
    - None contains `from app.main import app` at module scope — command: `grep -cE '^from app\.main' backend/tests/auth/*.py` returns 0 for each
    - The signup_verify and password_reset files exercise the complete 2-step flow (grep each for both the signup/request endpoint AND the verify/reset endpoint)
    - TOTP file imports `pyotp` and calls `pyotp.TOTP(...).now()`
    - The signup_verify file does NOT commit the `_get_verification_token` NotImplementedError stub — the placeholder MUST be replaced with a real token-retrieval implementation before commit. Command: `grep -cE 'raise\s+NotImplementedError' backend/tests/auth/test_characterization_signup_verify.py` returns 0.
    - The signup_verify file DOES assert that email_verified flipped to True after the verify-email call — command: `grep -E 'db_user.email_verified is True' backend/tests/auth/test_characterization_signup_verify.py` returns at least one match (proves the 2-step flow reaches the DB-state assertion, which means `_get_verification_token` returned a real token rather than raising).
    - `cd backend && pytest -n auto tests/auth/test_characterization_signup_verify.py tests/auth/test_characterization_login.py tests/auth/test_characterization_2fa_totp.py tests/auth/test_characterization_password_reset.py --no-cov` exits 0 (all 4 pass)
    - `cd backend && pyright tests/auth/test_characterization_signup_verify.py tests/auth/test_characterization_login.py tests/auth/test_characterization_2fa_totp.py tests/auth/test_characterization_password_reset.py` reports 0 errors
    - `cd backend && black --config pyproject.toml --line-length 120 --check tests/auth/` exits 0
    - Full backend suite still green: `cd backend && pytest -n auto -x` exits 0 (coverage floor from Plan 04 still met; 4 new tests ADD to coverage)
  </acceptance_criteria>
  <done>4 non-VCR test files cover flows 1, 2, 3, 7; each asserts status + keys + DB state; all pass under -n auto; no module-scope app import; pyright/black/isort clean.</done>
</task>

<task type="auto">
  <name>Task 3: Write WebAuthn characterization test (flow 4) with library-boundary stubs</name>
  <files>backend/tests/auth/test_characterization_webauthn.py</files>
  <read_first>
    - backend/app/api/endpoints/auth.py (find WebAuthn routes: register/begin, register/complete, authenticate/begin, authenticate/complete — these are the 4+ endpoints the flow exercises. Also identify the 4 `webauthn.*` library functions imported: `generate_registration_options`, `verify_registration_response`, `generate_authentication_options`, `verify_authentication_response`)
    - backend/tests/api/endpoints/test_webauthn.py (existing WebAuthn test with the mock.patch pattern — lines 1-46 provide the stub-at-boundary shape; executor lifts this verbatim for the characterization file)
    - backend/app/api/models/webauthn_credential.py (model used by DB state assertion)
    - .planning/phases/01-safety-nets-ci-hardening/01-CONTEXT.md D-18 (WebAuthn stubbed at library boundary, NOT over HTTP — this is LOCKED, do not swap to VCR)
    - .planning/phases/01-safety-nets-ci-hardening/01-RESEARCH.md lines 906-960 (Code Example 7 — WebAuthn stub shape)
    - .planning/phases/01-safety-nets-ci-hardening/01-PATTERNS.md §12 Analog B (stub-at-library-boundary pattern source)
  </read_first>
  <action>
Create `backend/tests/auth/test_characterization_webauthn.py`.

Read the 4 webauthn library call sites in `backend/app/api/endpoints/auth.py`. The module likely does:

    from webauthn import (
        generate_registration_options,
        verify_registration_response,
        generate_authentication_options,
        verify_authentication_response,
    )

If so, the patch target is `app.api.endpoints.auth.<function>` (because Python resolves the imported names into the importing module's namespace, and `unittest.mock.patch` must target the name where it is LOOKED UP, not where it is DEFINED).

Flow outline (one test function, single test for the complete round-trip registration + authentication assertion):

    """SAFE-06 flow 4: WebAuthn passkey registration + authentication.

    Per D-18 the cryptographic boundary is stubbed at the `webauthn` library
    boundary — NOT over HTTP. Real attestation signing requires a hardware
    authenticator we cannot cheaply reproduce in CI.
    """
    import os
    from types import SimpleNamespace
    from typing import Any
    from unittest.mock import patch

    from fastapi.testclient import TestClient
    from sqlalchemy.orm import Session

    from app.api.dependencies.auth import get_password_hash
    from app.api.models.user import User as DBUser
    from app.api.models.webauthn_credential import WebAuthnCredential
    from app.core.config import settings


    def _uniq(base: str) -> str:
        return f"{base}_{os.environ.get('PYTEST_XDIST_WORKER', 'main')}_{os.getpid()}"


    def _create_verified_user(db: Session) -> DBUser:
        username = _uniq("wa_char")
        user = DBUser(
            username=username,
            email=f"{username}@example.com",
            hashed_password=get_password_hash("testpass123!"),
            email_verified=True,
            disabled=False,
        )
        db.add(user)
        db.commit()
        db.refresh(user)
        return user


    def _login(client: TestClient, user: DBUser) -> str:
        r = client.post(
            f"{settings.API_STR}/auth/token",
            data={"username": user.username, "password": "testpass123!"},
        )
        assert r.status_code == 200, r.text
        return r.json()["access_token"]


    @patch("app.api.endpoints.auth.verify_authentication_response")
    @patch("app.api.endpoints.auth.generate_authentication_options")
    @patch("app.api.endpoints.auth.verify_registration_response")
    @patch("app.api.endpoints.auth.generate_registration_options")
    def test_webauthn_register_and_authenticate(
        mock_gen_reg: Any,
        mock_ver_reg: Any,
        mock_gen_auth: Any,
        mock_ver_auth: Any,
        client: TestClient,
        db_session: Session,
    ) -> None:
        user = _create_verified_user(db_session)
        token = _login(client, user)
        headers = {"Authorization": f"Bearer {token}"}

        # Registration: step 1 — request options
        mock_gen_reg.return_value = SimpleNamespace(
            # Executor: populate with the minimal fields auth.py reads off the
            # return value. Read the endpoint to identify which attributes are
            # accessed; typical set includes `challenge`, `rp`, `user`, `pubKeyCredParams`.
            challenge=b"fake_challenge_bytes",
        )
        r1 = client.post(
            f"{settings.API_STR}/auth/webauthn/register/begin",
            headers=headers,
        )
        assert r1.status_code == 200
        assert "challenge" in r1.json() or "publicKey" in r1.json()

        # Registration: step 2 — submit credential
        mock_ver_reg.return_value = SimpleNamespace(
            # Executor: populate with fields auth.py reads to persist the credential.
            # Typical: credential_id, credential_public_key, sign_count.
            credential_id=b"\x01\x02\x03",
            credential_public_key=b"\x04\x05\x06",
            sign_count=0,
        )
        r2 = client.post(
            f"{settings.API_STR}/auth/webauthn/register/complete",
            headers=headers,
            json={"credential": {"id": "fake", "rawId": "fake", "response": {}, "type": "public-key"}},
        )
        assert r2.status_code == 200
        creds = db_session.query(WebAuthnCredential).filter_by(user_id=user.id).all()
        assert len(creds) == 1

        # Authentication: step 1 — request challenge
        mock_gen_auth.return_value = SimpleNamespace(challenge=b"another_challenge")
        r3 = client.post(
            f"{settings.API_STR}/auth/webauthn/authenticate/begin",
            json={"username": user.username},
        )
        assert r3.status_code == 200

        # Authentication: step 2 — submit assertion
        mock_ver_auth.return_value = SimpleNamespace(new_sign_count=1)
        r4 = client.post(
            f"{settings.API_STR}/auth/webauthn/authenticate/complete",
            json={"credential": {"id": "fake", "rawId": "fake", "response": {}, "type": "public-key"}},
        )
        assert r4.status_code in (200, 204)
        assert "access_token" in r4.json() or r4.status_code == 204

**Executor implementation notes:**
- The exact endpoint paths (`/webauthn/register/begin`, etc.) and the exact field names read off the `verify_registration_response` return value MUST be looked up in `backend/app/api/endpoints/auth.py`. The skeleton above is illustrative — adjust to the actual surface.
- The `unittest.mock.patch` decorators apply in REVERSE order (innermost-first in function arg order). The test function above takes mocks in the order `mock_gen_reg, mock_ver_reg, mock_gen_auth, mock_ver_auth` corresponding to the decorator stack bottom-to-top.
- DO NOT hit any external HTTPS endpoint; all webauthn library calls are stubbed.

Format: `black --config pyproject.toml --line-length 120` + `isort` + pyright strict.
  </action>
  <verify>
    <automated>cd /home/tyler-webb/Documents/Github/CarModPicker/backend && pytest -n auto tests/auth/test_characterization_webauthn.py --no-cov -x -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/auth/test_characterization_webauthn.py` exists
    - File contains the literal string `@patch("app.api.endpoints.auth.verify_registration_response")` (patch at the import-target boundary, not at `webauthn.*`)
    - File contains the literal string `@patch("app.api.endpoints.auth.generate_registration_options")`
    - File contains the literal string `@patch("app.api.endpoints.auth.verify_authentication_response")`
    - File contains the literal string `@patch("app.api.endpoints.auth.generate_authentication_options")`
    - File does NOT contain `@pytest.mark.vcr` (WebAuthn per D-18 uses mocks, not VCR)
    - File does NOT make any `requests.*` or `httpx.*` or `urllib.*` calls
    - DB state assertion present: query WebAuthnCredential by user_id after registration, assert `len(creds) == 1`
    - `cd backend && pytest -n auto tests/auth/test_characterization_webauthn.py --no-cov` exits 0
    - `cd backend && pyright tests/auth/test_characterization_webauthn.py` reports 0 errors
    - `cd backend && black --config pyproject.toml --line-length 120 --check tests/auth/test_characterization_webauthn.py` exits 0
    - Full backend suite still green: `cd backend && pytest -n auto -x` exits 0
  </acceptance_criteria>
  <done>WebAuthn test stubs all 4 library-boundary functions via unittest.mock.patch targeting `app.api.endpoints.auth.*`; exercises registration round-trip AND authentication round-trip in one test; passes under -n auto.</done>
</task>

<task type="auto">
  <name>Task 4: Write 2 Google OAuth characterization tests (flows 5 + 6) with VCR cassettes</name>
  <files>
    backend/tests/auth/test_characterization_oauth_signin.py,
    backend/tests/auth/test_characterization_oauth_link.py
  </files>
  <read_first>
    - backend/app/api/endpoints/auth.py (find Google OAuth endpoints: likely `POST /api/auth/google` for signin and `POST /api/auth/google/link` for linking. Confirm request body shape — typically `{id_token, nonce}` or `{code}`.)
    - backend/app/api/utils/google_oauth.py (`verify_oauth2_token` wrapper — this is where `google.oauth2.id_token.verify_oauth2_token` is called; pytest-recording intercepts the underlying urllib3 call to Google's JWKS endpoint)
    - backend/tests/api/endpoints/test_google_oauth.py (existing test with `@patch("app.api.utils.google_oauth.verify_oauth2_token")` pattern. Characterization tests REPLACE this patch with @pytest.mark.vcr per D-18.)
    - backend/tests/conftest.py (the vcr_config fixture added in Task 1 — executor must understand it is scope=module)
    - .planning/phases/01-safety-nets-ci-hardening/01-CONTEXT.md D-16 (flows 5 + 6), D-17 (cassette location), D-18 (VCR vs mock split)
    - .planning/phases/01-safety-nets-ci-hardening/01-RESEARCH.md lines 859-904 (Code Example 6 — VCR usage)
    - .planning/phases/01-safety-nets-ci-hardening/01-PATTERNS.md §12 Analog C (VCR pattern for Google OAuth)
  </read_first>
  <action>
**Key choice at author time:** VCR records HTTP traffic. The `verify_oauth2_token` call from the `google.auth.transport.requests` client does hit Google's JWKS endpoint over urllib3, which vcrpy can intercept (A3 in RESEARCH Assumptions Log). The two tests exercise the auth endpoint; the VCR cassette captures the outbound JWKS traffic.

Create the two files with the structure below. Executor fills in the exact request/response shape after reading `backend/app/api/endpoints/auth.py`.

**File 1: `backend/tests/auth/test_characterization_oauth_signin.py`** — Flow 5: first-time Google sign-in.

    """SAFE-06 flow 5: Google OAuth first-time sign-in.

    Uses pytest-recording: the cassette at backend/tests/cassettes/auth/
    test_characterization_oauth_signin/test_google_oauth_signin.yaml records
    the JWKS HTTPS roundtrip. Cassette regeneration (when Google rotates
    keys or the endpoint contract changes):

        cd backend
        rm tests/cassettes/auth/test_characterization_oauth_signin/test_google_oauth_signin.yaml
        pytest -n 0 --record-mode=once tests/auth/test_characterization_oauth_signin.py::test_google_oauth_signin

    MUST use -n 0 to avoid pytest-xdist write races (Pitfall 3). Record against
    a sandbox Google account, never a personal/admin account.
    """
    import os

    import pytest
    from fastapi.testclient import TestClient
    from sqlalchemy.orm import Session

    from app.api.models.user import User as DBUser
    from app.core.config import settings


    def _uniq(base: str) -> str:
        return f"{base}_{os.environ.get('PYTEST_XDIST_WORKER', 'main')}_{os.getpid()}"


    @pytest.mark.vcr
    def test_google_oauth_signin(client: TestClient, db_session: Session) -> None:
        # Executor: populate id_token from the cassette's recorded request body.
        # A fresh recording against a Google sandbox account will contain a
        # real short-lived token; after cassette is committed the value is
        # replayed regardless of expiry.
        response = client.post(
            f"{settings.API_STR}/auth/google",
            json={
                "id_token": "<ID_TOKEN_FROM_CASSETTE>",
                "nonce": "test_nonce_" + _uniq("oauth_signin"),
            },
        )
        # D-19: status + key presence + DB state change
        assert response.status_code == 200, response.text
        body = response.json()
        assert "access_token" in body
        assert "user" in body or "user_id" in body

        # DB state: user now exists and email_verified=True (OAuth email is trusted)
        # Executor: replace the email with the one used in the cassette.
        test_email = "<EMAIL_FROM_CASSETTE>"
        user = db_session.query(DBUser).filter(DBUser.email == test_email).first()
        assert user is not None
        assert user.email_verified is True

**File 2: `backend/tests/auth/test_characterization_oauth_link.py`** — Flow 6: link Google to existing user.

    """SAFE-06 flow 6: link Google OAuth account to an EXISTING email/password user.

    Same VCR mechanism as flow 5. Cassette at
    backend/tests/cassettes/auth/test_characterization_oauth_link/test_google_oauth_link.yaml.
    """
    import os

    import pytest
    from fastapi.testclient import TestClient
    from sqlalchemy.orm import Session

    from app.api.dependencies.auth import get_password_hash
    from app.api.models.oauth_account import OAuthAccount
    from app.api.models.user import User as DBUser
    from app.core.config import settings


    def _uniq(base: str) -> str:
        return f"{base}_{os.environ.get('PYTEST_XDIST_WORKER', 'main')}_{os.getpid()}"


    @pytest.mark.vcr
    def test_google_oauth_link_existing_user(client: TestClient, db_session: Session) -> None:
        # Step 1 — create + login an email/password user (whose email matches the
        # Google account used when recording the cassette).
        email_from_cassette = "<EMAIL_FROM_CASSETTE>"
        username = _uniq("oauth_link")
        user = DBUser(
            username=username,
            email=email_from_cassette,
            hashed_password=get_password_hash("testpass123!"),
            email_verified=True,
            disabled=False,
        )
        db_session.add(user)
        db_session.commit()
        db_session.refresh(user)

        login = client.post(
            f"{settings.API_STR}/auth/token",
            data={"username": username, "password": "testpass123!"},
        )
        assert login.status_code == 200
        token = login.json()["access_token"]

        # Step 2 — link Google account
        link = client.post(
            f"{settings.API_STR}/auth/google/link",
            json={"id_token": "<ID_TOKEN_FROM_CASSETTE>"},
            headers={"Authorization": f"Bearer {token}"},
        )
        assert link.status_code in (200, 201), link.text

        # DB state: OAuthAccount row now exists for this user
        oauth = db_session.query(OAuthAccount).filter_by(user_id=user.id).first()
        assert oauth is not None

**Important notes for the executor:**

1. **Cassette recording is a local, manual step.** The CI pipeline only REPLAYS cassettes. The executor agent MUST NOT attempt to run `--record-mode=once` in its own sandbox — that requires a real Google sandbox account. Instead:
   - Commit the test files + `.gitkeep` marker.
   - Document in the PR description that the user must run:

         cd backend
         rm -rf tests/cassettes/auth/test_characterization_oauth_signin tests/cassettes/auth/test_characterization_oauth_link
         pytest -n 0 --record-mode=once tests/auth/test_characterization_oauth_signin.py
         pytest -n 0 --record-mode=once tests/auth/test_characterization_oauth_link.py

   - After recording, the user commits the cassette YAML files. The secret-audit test in Task 5 validates scrubbing on the committed cassettes.

2. **Tests are SKIPPED (not failing) when cassettes are missing.** Add a module-level guard at the top of each file:

        import pathlib
        import pytest
        _CASSETTE = pathlib.Path(__file__).parent.parent / "cassettes" / "auth" / pathlib.Path(__file__).stem / (pathlib.Path(__file__).stem.replace("test_characterization_", "test_") + ".yaml")
        pytestmark = pytest.mark.skipif(
            not _CASSETTE.exists(),
            reason="Cassette missing — run `pytest -n 0 --record-mode=once` locally to generate."
        )

   Adjust the cassette path formula to match pytest-recording's actual layout; pytest-recording defaults to `cassettes/<test_module>/<test_function>.yaml` relative to the test file. Verify the exact path with a dry-run at implementation time.

3. **Do NOT commit placeholder cassettes.** Committing a fake YAML that pytest-recording tries to replay will cause CI failures. Until the user records real cassettes, the skipif gate keeps CI green.

4. Format: `black --config pyproject.toml --line-length 120` + `isort` + pyright strict.
  </action>
  <verify>
    <automated>cd /home/tyler-webb/Documents/Github/CarModPicker/backend && pytest -n auto tests/auth/test_characterization_oauth_signin.py tests/auth/test_characterization_oauth_link.py --no-cov -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist at the specified paths
    - Each file contains exactly one `@pytest.mark.vcr` decorator (command: `grep -c '@pytest.mark.vcr' backend/tests/auth/test_characterization_oauth_signin.py` returns 1; same for link file)
    - Each file contains a `pytest.mark.skipif(not _CASSETTE.exists(), reason=...)` guard so tests are SKIPPED (not FAILED) when cassettes are absent
    - Neither file uses `@patch("app.api.utils.google_oauth.verify_oauth2_token")` or similar — per D-18 these flows use VCR, not mocks
    - Each file includes a DB state assertion (user/oauth row created) in addition to HTTP status + response-key presence
    - `cd backend && pytest -n auto tests/auth/test_characterization_oauth_signin.py tests/auth/test_characterization_oauth_link.py --no-cov` exits 0 — either all tests PASS (cassettes present + replay succeeds) OR all tests are SKIPPED (cassettes absent); zero failures
    - `cd backend && pyright tests/auth/test_characterization_oauth_signin.py tests/auth/test_characterization_oauth_link.py` reports 0 errors
    - `cd backend && black --config pyproject.toml --line-length 120 --check tests/auth/test_characterization_oauth_signin.py tests/auth/test_characterization_oauth_link.py` exits 0
    - No cassette YAML files are committed as part of this plan (empty `cassettes/auth/` beyond the `.gitkeep` marker). Command: `find backend/tests/cassettes -name '*.yaml' | wc -l` returns 0 — the user records cassettes post-merge per PR-description instructions
    - Full backend suite still green (skipped tests don't break the coverage gate): `cd backend && pytest -n auto -x` exits 0
  </acceptance_criteria>
  <post_merge_validation>
    **After the user records cassettes post-merge**, run the OAuth characterization tests and confirm they report PASSED (not SKIPPED):

        cd backend && pytest -n auto tests/auth/test_characterization_oauth_signin.py tests/auth/test_characterization_oauth_link.py -v

    A SKIPPED result after cassettes are committed would indicate the `skipif` cassette-path formula does NOT match pytest-recording's actual default layout (`cassettes/<test_module>/<test_function>.yaml` relative to the test file). In that case, fix the `_CASSETTE` path computation at the top of each test file so `_CASSETTE.exists()` returns True when a real cassette YAML is present, then re-run. This post-merge verification is REQUIRED — without it, a wrong path formula silently skips the tests forever and the guardrail provides no real signal.
  </post_merge_validation>
  <done>2 OAuth test files exist with @pytest.mark.vcr + skipif-missing-cassette guards; no mocks on the oauth boundary; pyright/black clean; PR description captures the cassette regeneration command.</done>
</task>

<task type="auto">
  <name>Task 5: Secret-audit test that grep-scans committed cassettes for un-scrubbed tokens</name>
  <files>backend/tests/test_cassette_secret_audit.py</files>
  <read_first>
    - backend/tests/conftest.py (the vcr_config fixture, to confirm which filters are applied — audit asserts their RESULT is present in committed files, i.e. `REDACTED` appears in place of banned tokens)
    - .planning/phases/01-safety-nets-ci-hardening/01-RESEARCH.md lines 1252-1258 (Security Domain, Known Threat Patterns — T-06-01 style secret leakage)
    - .planning/phases/01-safety-nets-ci-hardening/01-CONTEXT.md D-17 (cassettes committed to git — audit is how we catch leaked secrets post-commit)
  </read_first>
  <action>
Create `backend/tests/test_cassette_secret_audit.py` with the exact contents below. This test is the guardrail for T-06-01 (cassette leakage of OAuth tokens/session cookies/PII).

    """SAFE-06 guardrail: audit committed VCR cassettes for un-scrubbed secrets.

    Reads every backend/tests/cassettes/**/*.yaml file and fails if any of the
    following BANNED patterns appear in the file body:

      - `authorization: Bearer `  (case-insensitive) — real Bearer token leaked
      - `set-cookie:` with content longer than 8 chars (real session cookie leaked)
      - `client_secret` with a value that is not `REDACTED`
      - `refresh_token` with a value that is not `REDACTED`
      - OAuth access-token-like strings: `ya29.` (Google access-token prefix),
        `ghp_`/`gho_` (GitHub), `sk-` (OpenAI) — catch-all for common providers

    If cassettes are absent (none committed yet), the test PASSES trivially —
    presence of the audit enforces scrubbing AS SOON AS any cassette lands.

    Executor: treat this test as part of SAFE-06. If a legitimate cassette
    regeneration trips this audit, the regeneration is incorrect — extend the
    filter list in conftest.py's vcr_config fixture and re-record.
    """

    from __future__ import annotations

    import re
    from pathlib import Path
    from typing import Iterator

    import pytest

    CASSETTE_ROOT = Path(__file__).parent / "cassettes"

    # Regexes for banned content. Each regex should catch ONLY un-scrubbed
    # content — a cassette that replaced the value with `REDACTED` must not
    # match. Bounded quantifiers only (ReDoS-safe).
    BANNED_PATTERNS: dict[str, re.Pattern[str]] = {
        "authorization_bearer": re.compile(r"(?i)authorization:\s*Bearer\s+[A-Za-z0-9._\-]{16,}"),
        "set_cookie_real": re.compile(r"(?i)set-cookie:\s*\S{16,}"),
        "client_secret_real": re.compile(r"client_secret[\"'=:\s]+(?!REDACTED)[A-Za-z0-9._\-]{8,}"),
        "refresh_token_real": re.compile(r"refresh_token[\"'=:\s]+(?!REDACTED)[A-Za-z0-9._\-]{8,}"),
        "google_access_token": re.compile(r"\bya29\.[A-Za-z0-9._\-]{20,}"),
        "github_token": re.compile(r"\bgh[pousr]_[A-Za-z0-9]{20,}"),
        "openai_key": re.compile(r"\bsk-[A-Za-z0-9]{32,}"),
    }


    def _all_cassettes() -> Iterator[Path]:
        if not CASSETTE_ROOT.is_dir():
            return iter([])
        return CASSETTE_ROOT.rglob("*.yaml")


    @pytest.mark.parametrize("cassette_path", list(_all_cassettes()), ids=lambda p: str(p.name))
    def test_cassette_contains_no_unscrubbed_secrets(cassette_path: Path) -> None:
        """Each committed cassette must NOT match any banned pattern."""
        content = cassette_path.read_text(encoding="utf-8", errors="replace")
        hits: list[str] = []
        for name, pattern in BANNED_PATTERNS.items():
            match = pattern.search(content)
            if match:
                # Truncate match text so an accidental secret doesn't get echoed
                # to CI logs in full.
                snippet = match.group(0)[:40] + "..."
                hits.append(f"{name}: {snippet}")
        assert not hits, (
            f"Cassette {cassette_path.relative_to(CASSETTE_ROOT)} contains un-scrubbed "
            f"secrets: {hits}. Extend vcr_config filter in conftest.py, delete cassette, "
            f"and re-record with `pytest -n 0 --record-mode=once`."
        )


    def test_cassette_audit_enforces_presence_of_redacted_markers() -> None:
        """Meta-guard: if any cassettes exist, at least one `REDACTED` must
        appear across the committed cassette tree (proving filter_headers etc.
        are actually running). Passes trivially when no cassettes committed yet."""
        cassettes = list(_all_cassettes())
        if not cassettes:
            pytest.skip("No cassettes committed yet — audit-meta guard trivially OK")

        combined = "\n".join(p.read_text(encoding="utf-8", errors="replace") for p in cassettes)
        assert "REDACTED" in combined, (
            "No `REDACTED` marker found across committed cassettes — vcr_config "
            "filter_headers / filter_post_data_parameters may not be wired up. "
            "Check conftest.py vcr_config fixture."
        )

Format: `black --config pyproject.toml --line-length 120` + `isort` + pyright strict.

Worker safety: all tests read committed files only; no state. Safe under `-n auto --dist=loadfile`.

Expected behavior at PR time:
- With 0 cassettes committed: the parametrized test has 0 cases; the meta-guard test skips. Exit 0.
- With cassettes committed and properly scrubbed (REDACTED markers present, no banned tokens): all parametrized cases pass; meta-guard passes. Exit 0.
- With a cassette that leaked a real token: the parametrized case for THAT cassette fails, blocking the PR. Failure message points to the exact filter to extend.
  </action>
  <verify>
    <automated>cd /home/tyler-webb/Documents/Github/CarModPicker/backend && pytest -n auto tests/test_cassette_secret_audit.py --no-cov -v 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_cassette_secret_audit.py` exists
    - File contains the literal strings `BANNED_PATTERNS` (case-sensitive) and `REDACTED`
    - File contains at least 6 keys in the `BANNED_PATTERNS` dict (command: `grep -cE '^\s+"[a-z_]+":\s+re\.compile' backend/tests/test_cassette_secret_audit.py` returns at least 6)
    - `cd backend && pytest -n auto tests/test_cassette_secret_audit.py --no-cov` exits 0 (either all parametrized cases pass, or zero cases and the meta-guard skips)
    - `cd backend && pyright tests/test_cassette_secret_audit.py` reports 0 errors
    - `cd backend && black --config pyproject.toml --line-length 120 --check tests/test_cassette_secret_audit.py` exits 0
    - Full backend suite still green: `cd backend && pytest -n auto -x` exits 0
    - The test is parametrized over cassettes (command: `grep -c '@pytest.mark.parametrize' backend/tests/test_cassette_secret_audit.py` returns 1)
  </acceptance_criteria>
  <done>Cassette secret-audit test exists; passes trivially when no cassettes committed; parametrizes over `**/*.yaml` when cassettes are present; catches 7+ banned token patterns; meta-guard asserts REDACTED markers actually appear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev recording → committed cassette YAML | Cassettes are committed to git; developer machine talks to real Google OAuth / SES / JWKS endpoints during recording |
| Cassette file → replay in CI | CI replays cassette bytes; any secret that leaked past vcr_config filters is permanently in git history |
| Auth endpoint ↔ webauthn library | WebAuthn library calls are stubbed at the import boundary in tests; production path unaffected |
| Test code ↔ DB | Tests use SQLite in-memory via conftest; parallel workers isolated via `os.getpid()` unique naming |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Committed VCR cassettes leaking OAuth bearers, session cookies, client_secret, refresh_token, or user PII | mitigate | (a) Task 1 conftest.py vcr_config has `filter_headers=['authorization','cookie','set-cookie','x-goog-api-key']`, `filter_post_data_parameters=['client_secret','code','refresh_token']`, `filter_query_parameters=['api_key','access_token']`. (b) Task 5 audit test greps every committed cassette for 7+ banned patterns AND asserts `REDACTED` markers are present. Severity HIGH if missed; mitigations reduce residual to LOW. |
| T-06-02 | Tampering | Malicious PR that deletes vcr_config filters and re-records cassettes | mitigate | Task 5 audit runs on EVERY PR. Even if filters are removed, the audit catches the leaked tokens before merge. Acceptance criterion on Task 5 asserts `REDACTED` appears across cassettes if any exist — removing filters would produce cassettes with no `REDACTED` markers, triggering the meta-guard. |
| T-06-03 | Spoofing | Real production credentials used during recording (instead of sandbox) | accept | Documented in test file docstring: "Record against a dedicated test Google account, never personal/admin." No technical enforcement — human procedure. Residual risk: developer-discipline. |
| T-06-04 | Denial of Service | pytest-xdist write race on cassette files during recording | mitigate | Cassette recording procedure in test docstrings mandates `-n 0` (serial). RESEARCH.md Pitfall 3 documents this; each test docstring repeats the `-n 0` instruction so developers recording locally don't miss it. |
| T-06-05 | Information Disclosure | Secret-audit test echoing a real leaked token to CI logs | mitigate | Task 5 truncates any regex match to 40 characters before failure message. Reviewer sees `authorization_bearer: authorization: Bearer abc...` and can investigate without the full token landing in CI-log searchable text. |
| T-06-06 | Tampering | WebAuthn crypto boundary patched at wrong target (e.g. `webauthn.verify_...` instead of `app.api.endpoints.auth.verify_...`) — patches do not apply, real crypto runs | mitigate | Task 3 acceptance criteria pin the patch target as `"app.api.endpoints.auth.<func>"`. If the auth module switches to `from app.api.utils.webauthn import *` in Phase 5, these tests will fail (patches miss) — this is GOOD: the failure alerts us the patch target needs updating alongside the refactor. |
| T-06-07 | Elevation of Privilege | Characterization test creates an admin/superuser user instead of a regular user | accept | All helper functions in these tests create regular users (disabled=False, no `is_admin=True`). If a future edit adds admin flags, lint/review catches it. |

Severity summary: T-06-01 is HIGH (secret leakage risk) with multiple mitigations (vcr_config filters + audit test + documented developer procedure). T-06-06 is MEDIUM (maintenance surface for Phase 5 refactor) with a natural fail-loud mitigation. All other threats LOW after mitigations.
</threat_model>

<verification>
- `grep -c 'pytest-recording==0.13.4' backend/requirements.txt` returns 1
- `grep -c 'def vcr_config' backend/tests/conftest.py` returns 1
- `grep -c 'filter_headers' backend/tests/conftest.py` returns 1
- `ls backend/tests/auth/test_characterization_*.py | wc -l` returns 7
- `grep -c '@pytest.mark.vcr' backend/tests/auth/test_characterization_oauth_signin.py` returns 1
- `grep -c '@pytest.mark.vcr' backend/tests/auth/test_characterization_oauth_link.py` returns 1
- `grep -c '@pytest.mark.vcr' backend/tests/auth/test_characterization_webauthn.py` returns 0 (per D-18)
- `grep -c '@patch("app.api.endpoints.auth.' backend/tests/auth/test_characterization_webauthn.py` returns at least 4
- `grep -cE 'raise\s+NotImplementedError' backend/tests/auth/test_characterization_signup_verify.py` returns 0 (the stub placeholder must be replaced before commit)
- `cd backend && pytest -n auto tests/auth/ tests/test_cassette_secret_audit.py --no-cov` exits 0
- `cd backend && pytest -n auto -x` exits 0 (full suite meets coverage floor from Plan 04)
- `cd backend && pyright tests/auth/ tests/test_cassette_secret_audit.py tests/conftest.py` reports 0 errors
- `cd backend && black --config pyproject.toml --line-length 120 --check tests/auth/ tests/test_cassette_secret_audit.py tests/conftest.py` exits 0
</verification>

<success_criteria>
1. 7 characterization tests exist, one per flow, under `backend/tests/auth/`
2. WebAuthn test uses library-boundary `@patch` (per D-18), NOT VCR
3. Google OAuth tests use `@pytest.mark.vcr` + skipif-missing-cassette guards (per D-18)
4. vcr_config fixture present in conftest.py with all 4 scrub categories (headers, post data, query params, record_mode)
5. pytest-recording installed and pinned in requirements.txt
6. Secret-audit test exists and parametrizes over committed cassettes (fails PR if un-scrubbed tokens present)
7. Full backend suite still green under Plan 04's `--cov-fail-under` floor
8. PR description documents the cassette-recording procedure for the user
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-nets-ci-hardening/01-06-SUMMARY.md` with:
- 7 test file names (list absolute paths)
- Confirmation that WebAuthn uses `@patch` at `app.api.endpoints.auth.*` (citing the 4 patch targets)
- Confirmation that OAuth tests are GATED by skipif-missing-cassette (citing the guard snippet)
- The full cassette-regeneration command block, ready to paste into the PR description:

      cd backend
      rm -rf tests/cassettes/auth/test_characterization_oauth_signin tests/cassettes/auth/test_characterization_oauth_link
      pytest -n 0 --record-mode=once tests/auth/test_characterization_oauth_signin.py
      pytest -n 0 --record-mode=once tests/auth/test_characterization_oauth_link.py

- **Post-merge OAuth cassette validation (REQUIRED):** After the user records the cassettes (command block above), they MUST run:

      cd backend && pytest -n auto tests/auth/test_characterization_oauth_signin.py tests/auth/test_characterization_oauth_link.py -v

  and confirm the two OAuth tests report PASSED, not SKIPPED. A SKIPPED result after the cassette YAMLs are present in `backend/tests/cassettes/auth/` indicates the `skipif` cassette-path formula in each test file does not match pytest-recording's default layout. In that case, fix the `_CASSETTE = pathlib.Path(__file__).parent.parent / "cassettes" / ...` computation so `_CASSETTE.exists()` returns True, then re-run. This validation is the only way to catch a silently-skipped guardrail.

- Handoff note for Phase 5 (auth refactor): these tests are the guardrail; Phase 5's AUTH-01 PR must keep all 7 green + keep cassette scrubbing green.
</output>
