---
phase: 08-frontend-coverage-expansion
plan: 05
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - frontend/src/api/votes.test.ts
  - frontend/src/api/reports.test.ts
  - frontend/src/api/bug_reports.test.ts
autonomous: true
requirements: [SAFE-03]
requirements_addressed: [SAFE-03]
tags: [frontend, api-tests, votes-reports, wave-1]

must_haves:
  truths:
    - "Every votesApi method has URL + method + body assertions"
    - "Every reportsApi and bugReportsApi method is covered"
    - "Polymorphic entity_type parameters are explicitly tested (votes + reports are polymorphic per backend admin.py)"
  artifacts:
    - path: "frontend/src/api/votes.test.ts"
      provides: "Full test coverage for votesApi (~8 methods)"
      min_lines: 80
    - path: "frontend/src/api/reports.test.ts"
      provides: "Full test coverage for reportsApi (~7 methods)"
      min_lines: 70
    - path: "frontend/src/api/bug_reports.test.ts"
      provides: "Full test coverage for bugReportsApi (~5 methods)"
      min_lines: 50
  key_links:
    - from: "frontend/src/api/votes.test.ts"
      to: "frontend/src/api/client.ts"
      via: "apiClient mocked by setup.ts D-18"
      pattern: "vi\\.mocked\\(apiClient\\."
---

<objective>
Test the Votes + Reports cluster of API modules: `votes.ts` (72 lines, ~8 methods), `reports.ts` (79 lines, ~7 methods), `bug_reports.ts` (43 lines, ~5 methods). 194 source lines total per RESEARCH.md §2.

Purpose: Votes and reports are polymorphic (entity_type can be part, build_list, comment, etc.) — tests must verify the correct entity_type string is forwarded to the URL or body. Bug reports use FormData in the page layer but the API client itself may be JSON-only; verify by reading the source.

Output: 3 new test files covering ~20 API methods total. Polymorphic entity_type parameters get explicit multi-case assertions (at least 2 entity types tested per vote/report method).
</objective>

<execution_context>
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/workflows/execute-plan.md
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-frontend-coverage-expansion/08-CONTEXT.md
@.planning/phases/08-frontend-coverage-expansion/08-PATTERNS.md

<interfaces>
Existing mocks in `frontend/src/test/mocks/api.ts`:
- `mockVoteSummary` (VoteSummary) — `entity_id: mockPart.id`, `entity_type: 'part'`, `upvotes: 5`, `downvotes: 1`, `vote_score: 4`.
- `mockPart` (PartRead) — for entity_id assertions.
- `mockBuildList` — for a second entity_type to test polymorphism.

Per-module method count estimates (verify by reading source):
- `votes.ts` (~8): likely `castVote`, `removeVote`, `getVoteSummary`, `listMyVotes`, possibly per-entity-type helpers.
- `reports.ts` (~7): likely `createReport`, `listReports`, `getReport`, `updateReportStatus`, plus user-facing helpers.
- `bug_reports.ts` (~5): likely `createBugReport`, `listBugReports`, `getBugReport`, `updateBugReport`, `closeBugReport`.

Canonical skeleton: PATTERNS.md §7. Follows the same pattern as plans 08-02 through 08-04.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write votes.test.ts covering polymorphic entity types</name>
  <read_first>
    - frontend/src/api/votes.ts
    - frontend/src/test/mocks/api.ts (mockVoteSummary, mockPart, mockBuildList)
    - .planning/phases/08-frontend-coverage-expansion/08-PATTERNS.md §7
  </read_first>
  <behavior>
    - At least 8 it-blocks covering every votesApi method.
    - At least 16 expect() calls.
    - At least 2 tests exercise DIFFERENT entity_type values (e.g., 'part' vs 'build_list') on the same method to verify polymorphism.
  </behavior>
  <action>
    Open `frontend/src/api/votes.ts`, enumerate every exported method. Write one test per method. For methods that accept entity_type + entity_id (e.g., `castVote(entity_type, entity_id, vote_type)`), write two tests: one with `entity_type: 'part'` and one with `entity_type: 'build_list'`, each asserting the correct URL path.

    Example:
    ```ts
    it('castVote forwards entity_type=part in URL', async () => {
      vi.mocked(apiClient.post).mockResolvedValueOnce({ data: mockVoteSummary });
      await votesApi.castVote('part', mockPart.id, 'upvote');
      expect(apiClient.post).toHaveBeenCalledWith(
        `/votes/part/${mockPart.id}`,
        { vote_type: 'upvote' }
      );
    });

    it('castVote forwards entity_type=build_list in URL', async () => {
      vi.mocked(apiClient.post).mockResolvedValueOnce({ data: { ...mockVoteSummary, entity_type: 'build_list' } });
      await votesApi.castVote('build_list', mockBuildList.id, 'downvote');
      expect(apiClient.post).toHaveBeenCalledWith(
        `/votes/build_list/${mockBuildList.id}`,
        { vote_type: 'downvote' }
      );
    });
    ```

    Verify the exact URL shape by reading `frontend/src/api/votes.ts` — do NOT guess at URL templates.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/api/votes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run src/api/votes.test.ts` exits 0
    - `grep -c "it(\|test(" frontend/src/api/votes.test.ts` returns at least 8
    - `grep -c "expect(" frontend/src/api/votes.test.ts` returns at least 16
    - `grep -c "'part'" frontend/src/api/votes.test.ts` returns at least 1 (polymorphic test exists)
    - `grep -c "'build_list'" frontend/src/api/votes.test.ts` returns at least 1 (second entity_type covered)
    - `grep -c "\.skip(" frontend/src/api/votes.test.ts` returns 0
  </acceptance_criteria>
  <done>All votesApi methods tested; polymorphic entity_type dispatch verified via at least 2 entity types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write reports.test.ts and bug_reports.test.ts</name>
  <read_first>
    - frontend/src/api/reports.ts
    - frontend/src/api/bug_reports.ts
    - frontend/src/test/mocks/api.ts
  </read_first>
  <behavior>
    reports.test.ts:
    - At least 7 it-blocks.
    - At least 14 expect() calls.
    - For polymorphic reports (entity_type field), at least 2 tests cover different entity types.

    bug_reports.test.ts:
    - At least 5 it-blocks.
    - At least 10 expect() calls.
    - If bugReportsApi accepts file attachments via FormData, assert with `expect.any(FormData)`. If JSON-only, assert body shape directly.
  </behavior>
  <action>
    Same pattern as Task 1. For reports, enumerate every exported method and cover polymorphic entity_type dispatch in at least 2 tests.

    For bug_reports, verify whether the API module wraps file uploads. Read `frontend/src/api/bug_reports.ts` first — if it uses `FormData`, test accordingly. If not (pure JSON), the FormData path is in the page component, not the API module.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/api/reports.test.ts src/api/bug_reports.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run src/api/reports.test.ts` exits 0
    - `cd frontend && npm test -- --run src/api/bug_reports.test.ts` exits 0
    - `grep -c "it(\|test(" frontend/src/api/reports.test.ts` returns at least 7
    - `grep -c "expect(" frontend/src/api/reports.test.ts` returns at least 14
    - `grep -c "it(\|test(" frontend/src/api/bug_reports.test.ts` returns at least 5
    - `grep -c "expect(" frontend/src/api/bug_reports.test.ts` returns at least 10
    - `grep -c "\.skip(" frontend/src/api/reports.test.ts frontend/src/api/bug_reports.test.ts` returns 0
  </acceptance_criteria>
  <done>All reportsApi and bugReportsApi methods tested; polymorphic dispatch in reports verified.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm test -- --run src/api/votes.test.ts src/api/reports.test.ts src/api/bug_reports.test.ts` exits 0.
- Per-file: expect-count > it-count.
- Polymorphism covered in votes (and reports if applicable).
</verification>

<success_criteria>
- Every votesApi, reportsApi, bugReportsApi method has URL + method + body assertions.
- Polymorphic entity_type dispatch explicitly tested.
- Votes/reports cluster ready to feed Wave 3 page tests (vote widgets on ViewPart, build-list report flows).
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-coverage-expansion/08-05-SUMMARY.md` with:
- Test counts per file.
- Coverage delta vs baseline for the 3 votes/reports source files.
- List of polymorphic dispatch cases covered (which entity_types tested per method).
</output>
