---
phase: 04-db-parts-hardening
plan: 01
subsystem: database
tags: [database, indexes, alembic, sqlalchemy, connection-pool, postgres]

# Dependency graph
requires:
  - phase: 01-safety-nets-ci-hardening
    provides: "MetaData(naming_convention=...) on Base.metadata — autogenerated indexes inherit ix_<table>_<column> names (SAFE-09)"
  - phase: 01-safety-nets-ci-hardening
    provides: "Migration DROP-guard (backend/scripts/check_migrations.py) validates no destructive ops"
  - phase: 03-non-breaking-internal-improvements
    provides: "Crawler ThreadPoolExecutor sizing formula DB_POOL_SIZE + DB_MAX_OVERFLOW - API_CONNECTION_RESERVE = 80 (D-14) — this plan preserves those literals"
provides:
  - "13 FK indexes created (closes DATA-05 Performance Insights gap)"
  - "pool_recycle=1800 (tightens RDS staleness; DATA-07)"
  - "Regression tests guarding FK-index presence and pool config from future regression"
affects: [04-02-build-log-eager-create, 04-03-n-plus-one-fix, 04-04-session-query-sweep, 04-05-row-lock-concurrency, 04-06-conventions-lazy-raise, 05-admin-auth-splits]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Index-only Alembic migration: autogenerate scaffold, discard non-index ops (constraint renames / FK re-adds), keep only op.create_index/op.drop_index per D-13 + Pitfall 10"
    - "FK-index regression test shape: introspection-only (SQLAlchemy Table.c[col].index and Table.indexes leading-column scan); no DB required under pytest -n auto"
    - "Pool-config regression test shape: import engine from app.db.session, assert engine.pool._recycle and envelope constants"

key-files:
  created:
    - "backend/alembic/versions/55291406b6a4_add_missing_fk_indexes.py"
    - "backend/tests/test_fk_indexes.py"
    - "backend/tests/test_db_pool_config.py"
  modified:
    - "backend/app/api/models/build_list.py"
    - "backend/app/api/models/build_list_part.py"
    - "backend/app/api/models/build_list_phase.py"
    - "backend/app/api/models/build_log.py"
    - "backend/app/api/models/bug_report.py"
    - "backend/app/api/models/crawler_adapter_config.py"
    - "backend/app/api/models/part.py"
    - "backend/app/api/models/report.py"
    - "backend/app/db/session.py"

key-decisions:
  - "Migration scope expanded from plan's 9-target baseline to 13 FKs (added build_log_posts.user_id, bug_reports.assigned_to, build_list_phases.build_list_id, crawler_adapter_configs.default_category_id) after full FK sweep surfaced them"
  - "Discarded non-index autogenerate output (op.create_foreign_key / op.drop_constraint / op.create_unique_constraint renames) per D-13 + Pitfall 10 — those reflect Phase 1 naming_convention overlay and are forward-only deferrals, not real schema work"
  - "Kept BugReport.user_id explicit composite index (ix_bug_reports_user_id) rather than moving to column-level index=True — single-column and covered, no duplication needed"
  - "pool_recycle=1800 literal committed; pool_size=25 + max_overflow=75 + API_CONNECTION_RESERVE=20 preserved per Phase 3 D-14 coupling (25 + 75 - 20 = 80 crawler workers)"

patterns-established:
  - "Index-only Alembic migration shape: no destructive ops, symmetric op.create_index/op.drop_index pairs, naming convention auto-named via op.f()"
  - "Introspection-only FK-index regression test: column.index OR Index leading-column scan"
  - "Postgres-required local migration verification: docker compose up -d persistant_volume_db + alembic upgrade head/downgrade -1/upgrade head round-trip (CI-automation deferred to Phase 4+ per D-31)"

requirements-completed: [DATA-05, DATA-07]

# Metrics
duration: 15min
completed: 2026-04-23
---

# Phase 4 Plan 01: FK-Index Audit and Pool-Recycle Hardening Summary

**13 missing FK indexes created via index-only autogenerated Alembic migration, pool_recycle tightened from 3600s to 1800s, and two always-on introspection regression tests landed to prevent reintroduction.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-23T03:32:04Z
- **Completed:** 2026-04-23T03:39:04Z
- **Tasks:** 3
- **Files modified:** 11 (8 model files, 1 session.py, 1 migration, 2 new test modules)

## Accomplishments

- Systematic FK audit across all 22+ model files; 13 missing indexes identified and landed (4 more than plan's baseline-of-9 floor)
- Single autogenerated Alembic migration (revision `55291406b6a4`) with symmetric create/drop pairs — zero destructive ops, passes DROP-guard (`check_migrations.py`)
- Migration round-trip verified against Postgres 16: `upgrade head` → `downgrade -1` → `upgrade head` all succeed
- `pool_recycle=1800` committed; pool-size envelope (25 + 75 - 20 = 80 crawler workers) preserved per Phase 3 D-14 coupling
- Full backend suite stays green (2233 passed, 6 pre-existing skipped) at unchanged `--cov-fail-under=51`

## Task Commits

1. **Task 1: FK audit + index=True additions** — `a81a34e` (feat)
2. **Task 2: Autogenerate FK-index migration + pool_recycle tweak** — `6b7906b` (feat)
3. **Task 3: Regression tests for FK indexes and pool config** — `0429ffe` (test)

## FK Audit Results

### FKs that received `index=True` (13 total)

| Model | Column | File |
|-------|--------|------|
| BuildList | user_id | backend/app/api/models/build_list.py |
| BuildList | car_id | backend/app/api/models/build_list.py |
| BuildListPart | build_list_id | backend/app/api/models/build_list_part.py |
| BuildListPart | part_id | backend/app/api/models/build_list_part.py |
| BuildListPart | added_by | backend/app/api/models/build_list_part.py |
| BuildListPart | build_list_phase_id | backend/app/api/models/build_list_part.py |
| BuildListPhase | build_list_id | backend/app/api/models/build_list_phase.py |
| BuildLogPost | user_id | backend/app/api/models/build_log.py |
| BugReport | assigned_to | backend/app/api/models/bug_report.py |
| CrawlerAdapterConfig | default_category_id | backend/app/api/models/crawler_adapter_config.py |
| Part | user_id | backend/app/api/models/part.py |
| Part | category_id | backend/app/api/models/part.py |
| Report | reviewed_by | backend/app/api/models/report.py |

### FKs explicitly SKIPPED per D-14 left-prefix rule

| Model | Column | Covered by |
|-------|--------|-----------|
| Vote | user_id | Composite index `ix_votes_user_entity_type` (leading column) |
| Report | user_id | Composite index `ix_reports_user_entity_type` (leading column) |
| BugReport | user_id | Explicit single-column `Index("ix_bug_reports_user_id", "user_id")` in `__table_args__` (no column-level dup needed) |

### FKs already correctly indexed (no action required)

- `webauthn_credentials.user_id` (column-level index=True)
- `part_listings.part_id`, `part_listings.retailer_id` (column-level index=True)
- `oauth_accounts.user_id` (column-level index=True)
- `car_models.car_make_id` (column-level index=True)
- `car_generations.car_model_id` (column-level index=True)
- `part_price_history.part_listing_id` (column-level index=True)
- `build_logs.build_list_id` (column-level index=True, also UNIQUE)
- `build_log_posts.build_log_id` (column-level index=True)
- `background_jobs.created_by_user_id` (column-level index=True)
- `parts.part_manufacturer_id`, `parts.canonical_part_id` (column-level index=True)
- `crawled_pages.part_id` (column-level index=True)

### Association tables (PK composite covers left-prefix)

- `part_cars.part_id`, `part_cars.car_id` — both part of composite PK
- `crawler_schedule_adapters.schedule_id` — leading column of composite PK

## Migration Details

**File:** `backend/alembic/versions/55291406b6a4_add_missing_fk_indexes.py`
**Revision:** `55291406b6a4`
**Down-revision:** `aa583927d86a`
**Shape:** 13 × `op.create_index(op.f('ix_...'), ...)` in `upgrade()`, 13 × `op.drop_index(op.f('ix_...'), ...)` in `downgrade()`
**Destructive ops:** 0 (no drop_column / drop_table / drop_constraint / alter_column / rename / add_column / execute) — passes DROP-guard.
**Naming:** Auto-named via Phase 1 SAFE-09 `MetaData(naming_convention={"ix": "ix_%(column_0_label)s"})` — zero hand-naming.

### Autogenerate output discarded per D-13 / Pitfall 10

Alembic autogenerate emitted non-index ops reflecting Phase 1's `naming_convention` overlay on historic unnamed constraints:
- `op.drop_constraint('categories_name_key')` + `op.create_unique_constraint('uq_categories_name')` — historic UNIQUE rename
- `op.create_foreign_key('fk_build_list_parts_build_list_phase_id_build_list_phases', ...)` — FK re-add with convention-name
- `op.create_foreign_key('fk_parts_canonical_part_id_parts', ...)` — same
- `op.create_foreign_key('fk_parts_part_manufacturer_id_part_manufacturers', ...)` — same

All four were discarded. These are forward-only historic-name deferrals (Phase 1 D-12 decision) — the live constraints already exist under their old names and work correctly. Retroactive renames are out of scope for Phase 4.

## Pool Configuration

**Change:** `pool_recycle=3600` → `pool_recycle=1800` in `backend/app/db/session.py:34`.

**Preserved per Phase 3 D-14 / Phase 4 D-18 / D-22 coupling:**
- `DB_POOL_SIZE = 25`
- `DB_MAX_OVERFLOW = 75`
- `API_CONNECTION_RESERVE = 20`
- `pool_pre_ping=True`

**Crawler-worker formula stays:** `25 + 75 - 20 = 80` parallel adapters.

**Deviation from REQUIREMENTS.md DATA-07 literal acknowledged:** REQ-07 specifies `pool_size=50`; we keep `25 + 75 = 100` total capacity (exceeds 50) to avoid ripping Phase 3's crawler worker sizing. Documented per D-21.

## Files Created/Modified

**Created:**
- `backend/alembic/versions/55291406b6a4_add_missing_fk_indexes.py` — 13 FK indexes, symmetric up/down
- `backend/tests/test_fk_indexes.py` — 2 tests (FK-index presence, left-prefix skip honored)
- `backend/tests/test_db_pool_config.py` — 2 tests (pool_recycle=1800, envelope constants preserved)

**Modified:**
- `backend/app/api/models/build_list.py` — index=True on user_id, car_id
- `backend/app/api/models/build_list_part.py` — index=True on build_list_id, part_id, added_by, build_list_phase_id
- `backend/app/api/models/build_list_phase.py` — index=True on build_list_id
- `backend/app/api/models/build_log.py` — index=True on BuildLogPost.user_id
- `backend/app/api/models/bug_report.py` — index=True on assigned_to
- `backend/app/api/models/crawler_adapter_config.py` — index=True on default_category_id
- `backend/app/api/models/part.py` — index=True on user_id, category_id
- `backend/app/api/models/report.py` — index=True on reviewed_by
- `backend/app/db/session.py` — pool_recycle: 3600 → 1800 (single-literal change, inline comment updated)

## Decisions Made

- **Migration scope grew from 9 to 13 FKs during audit sweep.** Plan's D-15 listed 9 as the confirmed floor; the systematic audit added BuildLogPost.user_id, BuildListPhase.build_list_id, CrawlerAdapterConfig.default_category_id, BugReport.assigned_to — all FKs without column-level indexes and not left-prefix-covered.
- **Discarded 4 non-index autogenerate ops per D-13 / Pitfall 10** (categories UNIQUE rename, 3 FK re-adds). Forward-only deferrals reflecting Phase 1 naming_convention overlay.
- **BugReport.user_id kept as explicit `Index("ix_bug_reports_user_id", "user_id")`** in `__table_args__` rather than moving to column-level index=True. Single-column, already indexed, not worth a structural change.
- **pool_recycle literal changed without touching pool envelope** — Phase 3 D-14 coupling acknowledged explicitly.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Audited 4 additional FKs not in plan's baseline-of-9 list**
- **Found during:** Task 1 (systematic FK sweep across all 22+ models)
- **Issue:** Plan's `<action>` directed a full audit sweep and D-15 explicitly noted "Others surface during planner's systematic pass." Four FKs without indexes and not left-prefix-covered were found: BuildLogPost.user_id, BugReport.assigned_to, BuildListPhase.build_list_id, CrawlerAdapterConfig.default_category_id.
- **Fix:** Added `index=True` to all four mapped_column declarations and included them in the migration.
- **Files modified:** backend/app/api/models/build_log.py, bug_report.py, build_list_phase.py, crawler_adapter_config.py
- **Verification:** Python introspection block (plan's `<automated>` check) + `alembic upgrade head` / `downgrade -1` / `upgrade head` round-trip + full suite pytest -n auto.
- **Committed in:** a81a34e (Task 1), 6b7906b (Task 2)

**2. [Rule 3 - Blocking] Started Docker Desktop + Postgres container for autogenerate**
- **Found during:** Task 2 (alembic revision --autogenerate requires a live DB at HEAD to diff)
- **Issue:** Docker daemon was not running at agent start. Autogenerate needs a reachable Postgres to compare ORM metadata against the live schema.
- **Fix:** Started Docker Desktop via `/opt/docker-desktop/bin/docker-desktop` (background), then `docker compose up -d persistant_volume_db` from backend/.
- **Files modified:** none (pure infra)
- **Verification:** `alembic current` reports `aa583927d86a (head)`; autogenerate succeeded.
- **Committed in:** n/a (no file change)

---

**Total deviations:** 2 auto-fixed (1 missing-critical scope expansion, 1 blocking infra)
**Impact on plan:** All auto-fixes within scope of the plan's `<action>` ("systematically audit and apply"). No scope creep — the audit expansion was the plan's intent.

## Issues Encountered

None beyond the deviations above. Autogenerate emitted expected non-index output (Phase 1 naming_convention overlay on historic constraints); plan's D-13 / Pitfall 10 guidance handled it cleanly.

## User Setup Required

None — no external service configuration required.

## Verification

- `pytest -n auto backend/tests/test_fk_indexes.py backend/tests/test_db_pool_config.py -v` → 4 passed
- `pytest -n auto` (full backend suite) → 2233 passed, 6 pre-existing skipped; `--cov-fail-under=51` gate preserved
- `alembic upgrade head` → applied 55291406b6a4
- `alembic downgrade -1` → reverted to aa583927d86a
- `alembic upgrade head` → re-applied 55291406b6a4 (round-trip clean)
- `python scripts/check_migrations.py` → OK (35 files scanned)
- Python introspection block from plan's `<automated>`: migration shape + pool config OK (13 create_index / 13 drop_index)
- `pyright` on all 8 modified model files: 0 errors, 0 warnings

## Next Phase Readiness

- Plan 04-02 (build-log eager creation + backfill migration) unblocked — Phase 4 migration head is now `55291406b6a4` (downstream plans chain from it).
- Plan 04-03 (N+1 fix) unblocked — build_log_posts.user_id is now indexed, so author-batch IN queries hit the new ix_build_log_posts_user_id index.
- Plans 04-04 (session.query sweep), 04-05 (row-lock concurrency), and 04-06 (conventions) have no dependency on this plan's output and may start in parallel per D-42.

## Self-Check: PASSED

File existence:
- FOUND: backend/alembic/versions/55291406b6a4_add_missing_fk_indexes.py
- FOUND: backend/tests/test_fk_indexes.py
- FOUND: backend/tests/test_db_pool_config.py

Commit existence:
- FOUND: a81a34e (Task 1)
- FOUND: 6b7906b (Task 2)
- FOUND: 0429ffe (Task 3)

---
*Phase: 04-db-parts-hardening*
*Plan: 01*
*Completed: 2026-04-23*
