---
phase: 08-frontend-coverage-expansion
plan: 20
type: execute
wave: 5
depends_on: ["08-02", "08-03", "08-04", "08-05", "08-06", "08-07", "08-08", "08-09", "08-10", "08-11", "08-12", "08-13", "08-14", "08-15", "08-16", "08-17", "08-18", "08-19"]
files_modified:
  - frontend/vitest.config.ts
  - .planning/phases/08-frontend-coverage-expansion/08-COVERAGE-POST-WAVES.txt
  - .planning/phases/08-frontend-coverage-expansion/08-FAIL-FORCE-PROOF.txt
autonomous: false
requirements: [SAFE-03]
requirements_addressed: [SAFE-03]
tags: [frontend, coverage, threshold-enable, wave-5, safe-03, human-verify]

must_haves:
  truths:
    - "Coverage measured with all Wave 1-4 tests landed meets D-06 thresholds (lines >=60, functions >=50, branches >=50, statements >=60)"
    - "Any below-threshold file is either test-covered OR excluded via D-15 with rationale"
    - "coverage.thresholds block in vitest.config.ts is uncommented with exact D-06 values"
    - "Fail-force proof recorded: threshold block is proven enforcing (raise value above measured, confirm non-zero exit, restore)"
    - "frontend-ci.yml still runs npm test -- --run --coverage and is now gated"
  artifacts:
    - path: "frontend/vitest.config.ts"
      provides: "coverage.thresholds uncommented with D-06 values"
      contains: "lines: 60"
    - path: ".planning/phases/08-frontend-coverage-expansion/08-COVERAGE-POST-WAVES.txt"
      provides: "Full coverage report post-Waves-1-4 (before threshold enable)"
      min_lines: 30
    - path: ".planning/phases/08-frontend-coverage-expansion/08-FAIL-FORCE-PROOF.txt"
      provides: "Evidence of fail-force cycle per VALIDATION.md Manual-Only Verifications"
      min_lines: 10
  key_links:
    - from: "frontend/vitest.config.ts"
      to: "frontend-ci.yml"
      via: "vitest exits non-zero when thresholds not met; CI propagates to PR check"
      pattern: "thresholds"
---

<objective>
Close the phase: verify Waves 1-4 have lifted coverage to at least D-06 thresholds (lines 60, functions 50, branches 50, statements 60), write gap-fill component tests if any metric is below target, uncomment the `coverage.thresholds` block in `vitest.config.ts` per D-22, and capture fail-force proof per VALIDATION.md Manual-Only Verifications.

Purpose: This plan is the SAFE-03 enforcement point. Per D-22, threshold uncomment happens ONLY after coverage is verified at or above targets — the explicit "zero window of red CI" discipline from Phase 1 D-05. Per D-12, components are gap-filled HERE (not upfront), based on the actual coverage report.

This plan has `autonomous: false` because the fail-force proof + final UAT is a human-verify checkpoint per VALIDATION.md.

Output: Updated vitest.config.ts with uncommented thresholds; 2 new artifact files (post-waves coverage report + fail-force proof); potentially new component test files for coverage gaps; plan SUMMARY.md documenting all of the above.
</objective>

<execution_context>
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/workflows/execute-plan.md
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-frontend-coverage-expansion/08-CONTEXT.md
@.planning/phases/08-frontend-coverage-expansion/08-RESEARCH.md
@.planning/phases/08-frontend-coverage-expansion/08-PATTERNS.md
@.planning/phases/08-frontend-coverage-expansion/08-VALIDATION.md
@.planning/phases/08-frontend-coverage-expansion/08-COVERAGE-BASELINE.txt

<interfaces>
Current `frontend/vitest.config.ts` coverage block (post-Wave 0 with D-13 additions):
```ts
coverage: {
  provider: 'v8',
  reporter: ['text', 'json', 'html'],
  exclude: [
    'node_modules/',
    'src/test/',
    '**/*.d.ts',
    '**/*.config.*',
    '**/coverage/**',
    'dist/',
    'build/',
    // D-13 (Phase 8): app bootstrap; executes once on mount, not meaningfully testable as a unit.
    'src/main.tsx',
    // D-13 (Phase 8): pure TypeScript types; no executable runtime code.
    'src/types/Api.ts',
    // (possibly more D-15 per-file exclusions added during Waves 1-4 for retailers.ts / utility.ts)
  ],
  // Deferred to plan 01-09 (SAFE-03 threshold enforcement).
  // D-06 targets: lines: 60, functions: 50, branches: 50, statements: 60.
  // thresholds: {
  //   lines: 60,
  //   functions: 50,
  //   branches: 50,
  //   statements: 60,
  // },
},
```

Wave 5 must:
1. Remove the deferred-to-plan-01-09 comment.
2. Uncomment the thresholds block with exactly the D-06 values.
3. Keep the D-13 and D-15 exclusions.

Fail-force proof per VALIDATION.md Manual-Only Verifications:
1. Uncomment with values slightly ABOVE measured (e.g., `lines: 95`).
2. Run `npm run test:coverage` → expect non-zero exit + "does not meet global threshold" message.
3. Restore D-06 values (lines: 60, functions: 50, branches: 50, statements: 60).
4. Run `npm run test:coverage` → expect exit 0.
5. Paste both outputs into `08-FAIL-FORCE-PROOF.txt`.

Per RESEARCH.md §Pitfall 3: Vitest counts untested files in the global denominator. Gap-fill may require writing component tests OR adding per-file D-15 exclusions for truly-untestable files.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Measure coverage post-Waves-1-4 and identify gaps</name>
  <read_first>
    - .planning/phases/08-frontend-coverage-expansion/08-COVERAGE-BASELINE.txt (Wave 0 baseline for comparison)
    - frontend/vitest.config.ts (current state — D-13 + any D-15 exclusions from Waves 1-4)
    - .planning/phases/08-frontend-coverage-expansion/08-VALIDATION.md §Phase Gate
  </read_first>
  <action>
    Step 1: Run the full coverage report and capture it:
      `cd frontend && npm run test:coverage -- --reporter=text > ../.planning/phases/08-frontend-coverage-expansion/08-COVERAGE-POST-WAVES.txt 2>&1 || true`
    The `|| true` defense is because coverage hasn't yet had thresholds enabled; any non-zero exit is unexpected but shouldn't block the capture.

    Step 2: Read the resulting file. Look at:
    - Summary row: lines / functions / branches / statements percentages.
    - Per-file table: identify files at 0% coverage (the big denominator drag per Pitfall 3) that Waves 1-4 did not touch.

    Step 3: Produce a gap analysis. For each dimension:
    - If Lines >= 60, Functions >= 50, Branches >= 50, Statements >= 60: no gap-fill needed, skip to Task 2.
    - If any dimension is below target: identify the largest 0%-covered files (components primarily, per D-12) + decide gap-fill strategy.

    Options for each gap file:
    - **Option A: Write a minimal component test.** Follow PATTERNS.md §13 skeleton (based on RouteGroupBoundary.test.tsx). Target: render the component + assert at least one visible element.
    - **Option B: Add per-file D-15 exclusion.** Only for genuinely-untestable files (e.g., pure layout wrappers with no logic). Include inline rationale comment.

    Step 4: Write gap-fill tests or exclusions. Prioritize components in `src/components/**/*.tsx` that drag the denominator; targeting largest files first maximizes coverage lift per test.

    Step 5: Re-run `cd frontend && npm run test:coverage` and confirm every dimension now meets the D-06 target.

    Capture the final coverage run into `08-COVERAGE-POST-WAVES.txt` (overwrite the earlier capture so the file reflects the state AT THE POINT OF THRESHOLD ENABLE).
  </action>
  <verify>
    <automated>test -s .planning/phases/08-frontend-coverage-expansion/08-COVERAGE-POST-WAVES.txt &amp;&amp; cd frontend &amp;&amp; npm run test:coverage 2>&amp;1 | tee /tmp/cov.out &amp;&amp; grep -qE "Lines *: *(6[0-9]|[789][0-9]|100)" /tmp/cov.out &amp;&amp; grep -qE "Functions *: *(5[0-9]|[6-9][0-9]|100)" /tmp/cov.out &amp;&amp; grep -qE "Branches *: *(5[0-9]|[6-9][0-9]|100)" /tmp/cov.out &amp;&amp; grep -qE "Statements *: *(6[0-9]|[789][0-9]|100)" /tmp/cov.out</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/08-frontend-coverage-expansion/08-COVERAGE-POST-WAVES.txt` exists and is at least 30 lines
    - `cd frontend && npm run test:coverage` exits 0
    - Summary row shows Lines at or above 60 (check via `grep -E "Lines *: *" 08-COVERAGE-POST-WAVES.txt`)
    - Summary row shows Functions at or above 50
    - Summary row shows Branches at or above 50
    - Summary row shows Statements at or above 60
    - If any component test was added, `grep -c "\.skip(" $(find frontend/src/components -name '*.test.tsx')` returns 0
    - No production source code modified (only tests + vitest.config.ts if D-15 exclusions added)
  </acceptance_criteria>
  <done>All four D-06 dimensions verified at target; 08-COVERAGE-POST-WAVES.txt committed as evidence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Uncomment coverage.thresholds block in vitest.config.ts per D-22</name>
  <read_first>
    - frontend/vitest.config.ts (current state with commented thresholds block)
    - .planning/phases/08-frontend-coverage-expansion/08-CONTEXT.md §D-22 (threshold uncomment is Wave 5 only)
  </read_first>
  <action>
    Edit `frontend/vitest.config.ts`:

    Before:
    ```ts
    // Deferred to plan 01-09 (SAFE-03 threshold enforcement).
    // D-06 targets: lines: 60, functions: 50, branches: 50, statements: 60.
    // Frontend baseline as of 2026-04-22: lines 0.43% / functions 10.52% / branches 18.43% / statements 0.43%.
    // Un-comment these values only after plan 01-09 lifts coverage to >=60/50/50/60.
    // thresholds: {
    //   lines: 60,
    //   functions: 50,
    //   branches: 50,
    //   statements: 60,
    // },
    ```

    After:
    ```ts
    // SAFE-03 threshold enforcement enabled by Phase 8 plan 08-20.
    // Phase 8 lifted frontend coverage from the 2026-04-22 baseline (lines 0.43%)
    // to meet D-06 targets. See .planning/phases/08-frontend-coverage-expansion/
    // for per-plan coverage deltas and 08-FAIL-FORCE-PROOF.txt for enforcement evidence.
    thresholds: {
      lines: 60,
      functions: 50,
      branches: 50,
      statements: 60,
    },
    ```

    DO NOT modify the `exclude` block or any other part of vitest.config.ts.

    Confirm with:
      `cd frontend && npm run test:coverage`
    which should still exit 0 (since Task 1 verified coverage is at/above target).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -q "lines: 60" vitest.config.ts &amp;&amp; grep -q "functions: 50" vitest.config.ts &amp;&amp; grep -q "branches: 50" vitest.config.ts &amp;&amp; grep -q "statements: 60" vitest.config.ts &amp;&amp; npm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^ *lines: 60" frontend/vitest.config.ts` returns 1 (uncommented)
    - `grep -c "^ *functions: 50" frontend/vitest.config.ts` returns 1
    - `grep -c "^ *branches: 50" frontend/vitest.config.ts` returns 1
    - `grep -c "^ *statements: 60" frontend/vitest.config.ts` returns 1
    - `grep -vc "^ *//" frontend/vitest.config.ts | head -1` (non-comment lines) grep returns include `thresholds:`
    - `grep -c "// thresholds:" frontend/vitest.config.ts` returns 0 (no commented threshold block remaining)
    - `grep -c "Deferred to plan 01-09" frontend/vitest.config.ts` returns 0 (old deferred comment removed)
    - `cd frontend && npm run test:coverage` exits 0
  </acceptance_criteria>
  <done>coverage.thresholds block uncommented with exact D-06 values; `npm run test:coverage` still green; old deferred comment gone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Fail-force proof — prove the threshold is actually enforcing</name>
  <what-built>
    Task 2 uncommented the coverage.thresholds block in vitest.config.ts with D-06 values. This checkpoint proves the gate is gating (not silently accepting every value) by temporarily raising thresholds above measured, confirming vitest exits non-zero with the threshold-failure message, then restoring the D-06 values.

    The fail-force proof is per VALIDATION.md Manual-Only Verifications — a one-time CI proof that the enforcement mechanism works.
  </what-built>
  <how-to-verify>
    1. Open `frontend/vitest.config.ts`. Temporarily change `lines: 60` to `lines: 95` (or another value clearly above measured).
    2. Run: `cd frontend && npm run test:coverage 2>&1 | tee /tmp/fail-force-raised.out`
    3. **Expected:** Non-zero exit. Output contains a message like:
       `ERROR: Coverage for lines (XX.XX%) does not meet global threshold (95%)`
    4. Restore `lines: 60` (revert the temporary edit).
    5. Run: `cd frontend && npm run test:coverage 2>&1 | tee /tmp/fail-force-restored.out`
    6. **Expected:** Exit 0. Output shows thresholds met.
    7. Write both outputs (tagged "RAISED" and "RESTORED") into `.planning/phases/08-frontend-coverage-expansion/08-FAIL-FORCE-PROOF.txt`:
       ```
       === FAIL-FORCE PROOF — Wave 5 SAFE-03 enforcement evidence ===
       Date: YYYY-MM-DD

       --- RAISED: lines=95 ---
       (paste npm run test:coverage output here — should show non-zero exit + threshold error)

       --- RESTORED: lines=60 (D-06 value) ---
       (paste npm run test:coverage output here — should show exit 0)
       ```
    8. Confirm `frontend/vitest.config.ts` `lines` is back to `60`.
    9. Reply with "approved" after pasting both proofs into the file.
  </how-to-verify>
  <resume-signal>Type "approved" after fail-force proof is captured in 08-FAIL-FORCE-PROOF.txt, or describe issues encountered (e.g., coverage report format changed, threshold not actually enforcing, D-06 values regressed below target).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Final verification — full test suite + lint + type-check green</name>
  <read_first>
    - .planning/phases/08-frontend-coverage-expansion/08-VALIDATION.md §Phase Gate (6-item checklist before /gsd-verify-work)
  </read_first>
  <action>
    Run the VALIDATION.md Phase Gate checks in order:

    1. `cd frontend && npm run test:coverage` — exit 0 with Lines >= 60, Functions >= 50, Branches >= 50, Statements >= 60.
    2. `cd frontend && npm test -- --run` — exit 0, all tests passing.
    3. `cd frontend && npm run lint` — exit 0.
    4. `cd frontend && npm run type-check` — exit 0.
    5. Verify `vitest.config.ts` `thresholds:` is uncommented and matches D-06 (lines: 60, functions: 50, branches: 50, statements: 60).
    6. Confirm `.planning/phases/08-frontend-coverage-expansion/08-FAIL-FORCE-PROOF.txt` exists and is non-empty.

    Record outputs from each command (at minimum: exit code + summary row) in the plan's SUMMARY.md.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run test:coverage &amp;&amp; npm test -- --run &amp;&amp; npm run lint &amp;&amp; npm run type-check &amp;&amp; test -s ../.planning/phases/08-frontend-coverage-expansion/08-FAIL-FORCE-PROOF.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test:coverage` exits 0
    - `cd frontend && npm test -- --run` exits 0
    - `cd frontend && npm run lint` exits 0
    - `cd frontend && npm run type-check` exits 0
    - `frontend/vitest.config.ts` has uncommented `thresholds: { lines: 60, functions: 50, branches: 50, statements: 60 }` (all 4 values present, not commented)
    - `.planning/phases/08-frontend-coverage-expansion/08-FAIL-FORCE-PROOF.txt` file size > 0
    - `08-FAIL-FORCE-PROOF.txt` contains both "RAISED" and "RESTORED" sections
    - No `.skip(` committed anywhere in `frontend/src/**/*.test.*` (per VALIDATION.md §Meta-Checks)
  </acceptance_criteria>
  <done>All 6 VALIDATION.md Phase Gate items green; SAFE-03 fully satisfied; SUMMARY.md documents the closing evidence.</done>
</task>

</tasks>

<verification>
- All 4 tasks complete.
- `cd frontend && npm run test:coverage` exits 0 with D-06 thresholds enforcing.
- Fail-force proof recorded.
- Full test suite + lint + type-check green.
- SAFE-03 requirement satisfied — frontend coverage gated in CI.
</verification>

<success_criteria>
- Frontend coverage meets all 4 D-06 thresholds (lines 60, functions 50, branches 50, statements 60).
- `vitest.config.ts` coverage.thresholds block is uncommented and enforcing (D-22).
- `frontend-ci.yml` (no edit needed — already runs `--coverage` per SAFE-02 from Phase 1) now fails PRs that drop coverage.
- Fail-force proof recorded as evidence that the gate actually gates.
- REQUIREMENTS.md SAFE-03 can flip from Pending → Satisfied after `/gsd-verify-work`.
- Phase 8 is complete; milestone v1.0 audit closes with SAFE-03 resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-coverage-expansion/08-20-SUMMARY.md` with:
- Final coverage summary (all 4 dimensions, with ≥ target confirmation).
- Coverage delta vs `08-COVERAGE-BASELINE.txt` (from 0.43% → >= 60% on lines).
- Fail-force evidence summary (pointer to 08-FAIL-FORCE-PROOF.txt).
- List of component tests added in Task 1 (if any).
- List of D-15 per-file exclusions added during Wave 5 (if any) with rationale.
- Phase Gate checklist (all 6 items green).
- Recommendation: Phase 8 ready for /gsd-verify-work → milestone v1.0 close.
</output>
