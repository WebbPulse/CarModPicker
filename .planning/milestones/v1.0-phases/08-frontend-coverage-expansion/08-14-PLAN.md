---
phase: 08-frontend-coverage-expansion
plan: 14
type: execute
wave: 3
depends_on: ["08-01"]
files_modified:
  - frontend/src/pages/Home.test.tsx
  - frontend/src/pages/Profile.test.tsx
  - frontend/src/pages/ViewUser.test.tsx
  - frontend/src/pages/About.test.tsx
  - frontend/src/pages/ContactUs.test.tsx
  - frontend/src/pages/PrivacyPolicy.test.tsx
  - frontend/src/pages/TermsOfService.test.tsx
  - frontend/src/pages/Support.test.tsx
  - frontend/src/pages/Pricing.test.tsx
  - frontend/src/pages/Checkout.test.tsx
  - frontend/src/pages/Search.test.tsx
  - frontend/src/pages/BugReport.test.tsx
  - frontend/src/pages/NotFound.test.tsx
autonomous: true
requirements: [SAFE-03]
requirements_addressed: [SAFE-03]
tags: [frontend, page-tests, public, wave-3]

must_haves:
  truths:
    - "Every public top-level page has at least one render test"
    - "Static policy pages (PrivacyPolicy, TermsOfService, About) have a heading-visible assertion"
    - "Interactive pages (Home, Profile, Search, BugReport, Checkout, ViewUser) have happy-path + at least one error/empty"
    - "Profile covers image upload stub at apiClient.post layer"
  artifacts:
    - path: "frontend/src/pages/Home.test.tsx"
      provides: "Home hero + CTA + nav link rendering"
      min_lines: 40
    - path: "frontend/src/pages/Profile.test.tsx"
      provides: "Authenticated profile render + image upload"
      min_lines: 60
    - path: "frontend/src/pages/ViewUser.test.tsx"
      provides: "Public user profile render + empty state"
      min_lines: 40
    - path: "frontend/src/pages/About.test.tsx"
      provides: "Static page render + heading assertion"
      min_lines: 25
    - path: "frontend/src/pages/ContactUs.test.tsx"
      provides: "Contact form render + submit"
      min_lines: 35
    - path: "frontend/src/pages/PrivacyPolicy.test.tsx"
      provides: "Static page render + heading"
      min_lines: 20
    - path: "frontend/src/pages/TermsOfService.test.tsx"
      provides: "Static page render + heading"
      min_lines: 20
    - path: "frontend/src/pages/Support.test.tsx"
      provides: "Support page render + FAQ or contact link"
      min_lines: 25
    - path: "frontend/src/pages/Pricing.test.tsx"
      provides: "Pricing tier cards render + CTA visibility"
      min_lines: 35
    - path: "frontend/src/pages/Checkout.test.tsx"
      provides: "Checkout page render + tier-upgrade CTA"
      min_lines: 35
    - path: "frontend/src/pages/Search.test.tsx"
      provides: "Search results render + one filter round-trip"
      min_lines: 50
    - path: "frontend/src/pages/BugReport.test.tsx"
      provides: "Bug report form render + submit"
      min_lines: 50
    - path: "frontend/src/pages/NotFound.test.tsx"
      provides: "404 page render"
      min_lines: 15
  key_links:
    - from: "frontend/src/pages/Profile.test.tsx"
      to: "frontend/src/api/images.ts uploadImage"
      via: "apiClient.post mocked for FormData upload"
      pattern: "expect\\.any\\(FormData\\)"
---

<objective>
Test all 13 public top-level customer pages per D-11 — the LARGEST Wave 3 group per RESEARCH.md §3 (14 files, 3,787 lines). Each page gets a happy-path render test + at least 1 error/empty-state test per D-11.

Purpose: This is the largest Wave 3 group by line count. Static policy pages (PrivacyPolicy, TermsOfService) get minimal heading-assertion tests; interactive pages (Home, Profile, Search, BugReport) get full happy-path tests. Coverage impact here is significant because these files collectively are 30%+ of the total customer page line count.

Output: 13 new page test files. Total estimated at least 30 it-blocks + at least 80 expects across the group.

Per RESEARCH.md §3 Open Question 3: if scoping surfaces >500 test-lines total (i.e., the single PR becomes unreviewable), executor surfaces a CHECKPOINT requesting a split into public-info (static) and public-interactive (Search, Home, Profile, BugReport, Checkout, ViewUser) sub-plans. Default: stay as one plan.
</objective>

<execution_context>
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/workflows/execute-plan.md
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-frontend-coverage-expansion/08-CONTEXT.md
@.planning/phases/08-frontend-coverage-expansion/08-PATTERNS.md

<interfaces>
Canonical skeleton: PATTERNS.md §11.

Per-page happy-path targets per RESEARCH.md §3:
- **Home.tsx (414):** hero CTA present + link target.
- **Profile.tsx (461):** authenticated render shows username + email; image upload works via mocked imagesApi.uploadImage.
- **ViewUser.tsx (149):** renders another user's profile when API returns mockUser.
- **About.tsx (244), PrivacyPolicy.tsx (414), TermsOfService.tsx (401), Support.tsx (237):** heading visible + scrollable content present.
- **ContactUs.tsx (114):** form + submit.
- **Pricing.tsx (275):** tier cards render + CTA visible.
- **Checkout.tsx (161):** authenticated render + tier-upgrade CTA.
- **Search.tsx (522):** initial render + one filter param round-trip. Mock `usePartsFilters` or pass initial search params via route.
- **BugReport.tsx (368):** form submission + one error state.
- **NotFound.tsx (27):** 404 message visible.

Auth scenarios per page:
- Public-no-auth: Home, About, ContactUs, PrivacyPolicy, TermsOfService, Support, Pricing, Search, NotFound — use `testScenarios.unauthenticated`.
- Authenticated: Profile, Checkout, ViewUser (if requires auth), BugReport (may be either) — use `testScenarios.authenticated`.
- Verify by reading source.

Existing mocks:
- `mockUser`, `mockPart`, `mockBuildList`, `mockCar`, `mockCategory`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write static-page tests (About, PrivacyPolicy, TermsOfService, Support, NotFound)</name>
  <read_first>
    - frontend/src/pages/About.tsx
    - frontend/src/pages/PrivacyPolicy.tsx
    - frontend/src/pages/TermsOfService.tsx
    - frontend/src/pages/Support.tsx
    - frontend/src/pages/NotFound.tsx
    - .planning/phases/08-frontend-coverage-expansion/08-PATTERNS.md §11
  </read_first>
  <behavior>
    Each static page gets a minimal test — at least 1 it-block + at least 2 expects. Shape:
    - Render the page under unauthenticated scenario.
    - Assert heading / key text is visible.

    NotFound additionally gets a second test for the "return home" link (if present).
  </behavior>
  <action>
    For each static page:
    ```tsx
    import { describe, it, expect } from 'vitest';
    import { render, screen, testScenarios } from '../test/utils/test-utils';
    import About from './About';

    describe('About page', () => {
      it('renders the About heading', () => {
        render(<About />, testScenarios.unauthenticated);
        expect(screen.getByRole('heading', { name: /about/i })).toBeInTheDocument();
      });

      it('renders key content sections', () => {
        render(<About />, testScenarios.unauthenticated);
        // Assert at least one paragraph or section visible — e.g.,
        // the "Our Mission" heading if present, or a specific piece of text.
        const headings = screen.getAllByRole('heading');
        expect(headings.length).toBeGreaterThan(0);
      });
    });
    ```

    Adjust heading text to match actual source. For PrivacyPolicy / TermsOfService, assert a distinctive phrase like "last updated" or a major section heading.

    For NotFound.test.tsx (smallest page, 27 lines):
    ```tsx
    describe('NotFound page', () => {
      it('renders the 404 message', () => {
        render(<NotFound />, testScenarios.unauthenticated);
        expect(screen.getByText(/404|not found/i)).toBeInTheDocument();
      });
    });
    ```
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/pages/About.test.tsx src/pages/PrivacyPolicy.test.tsx src/pages/TermsOfService.test.tsx src/pages/Support.test.tsx src/pages/NotFound.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 5 static-page test files exit 0
    - Each file: `grep -c "it(\|test(" ...` returns at least 1
    - Each file: `grep -c "expect(" ...` returns at least 2
    - Each file: `grep -c "render(" ...` returns at least 1
    - `grep -c "\.skip(" frontend/src/pages/About.test.tsx frontend/src/pages/PrivacyPolicy.test.tsx frontend/src/pages/TermsOfService.test.tsx frontend/src/pages/Support.test.tsx frontend/src/pages/NotFound.test.tsx` returns 0
  </acceptance_criteria>
  <done>5 static pages covered with minimal heading-visible + content-present tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write interactive public pages (Home, ContactUs, Pricing, Checkout, Search, BugReport)</name>
  <read_first>
    - frontend/src/pages/Home.tsx
    - frontend/src/pages/ContactUs.tsx
    - frontend/src/pages/Pricing.tsx
    - frontend/src/pages/Checkout.tsx
    - frontend/src/pages/Search.tsx
    - frontend/src/pages/BugReport.tsx
    - frontend/src/test/mocks/api.ts
    - .planning/phases/08-frontend-coverage-expansion/08-PATTERNS.md §11
  </read_first>
  <behavior>
    Each page gets at least 2 it-blocks (happy path + at least one error/empty/branch state). Total: at least 12 it-blocks across the 6 files, at least 25 expects.

    Page-specific:
    - Home: hero CTA + link renders + at least 1 secondary section.
    - ContactUs: form renders + submit hits apiClient.post.
    - Pricing: tier cards visible + upgrade CTA.
    - Checkout: authenticated render + tier-upgrade or purchase CTA.
    - Search: renders with initial query + re-renders when filter changes.
    - BugReport: form renders + submit calls apiClient.post (assert body shape).
  </behavior>
  <action>
    Apply PATTERNS.md §11 skeleton for each page. For Search, handle multiple API calls:
    ```tsx
    beforeEach(() => {
      vi.clearAllMocks();
      vi.mocked(apiClient.get).mockImplementation((url: string) => {
        if (url.includes('/search')) return Promise.resolve({ data: { results: [mockPart] } });
        return Promise.resolve({ data: [] });
      });
    });

    it('renders search results from the API', async () => {
      render(<Search />, {
        route: '/search?q=honda',
        ...testScenarios.unauthenticated,
      });
      await waitFor(() => expect(screen.getByText(mockPart.name)).toBeInTheDocument());
    });
    ```

    For BugReport (may need `testScenarios.authenticated`):
    ```tsx
    it('submits bug report form', async () => {
      vi.mocked(apiClient.post).mockResolvedValueOnce({ data: { id: 'bug-1' } });
      const user = userEvent.setup();
      render(<BugReport />, testScenarios.authenticated);
      await user.type(screen.getByLabelText(/title|subject/i), 'test');
      await user.type(screen.getByLabelText(/description|details/i), 'desc');
      await user.click(screen.getByRole('button', { name: /submit|send/i }));
      await waitFor(() => expect(apiClient.post).toHaveBeenCalled());
    });
    ```

    For ContactUs: simpler form; assert submit hits the contact endpoint.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/pages/Home.test.tsx src/pages/ContactUs.test.tsx src/pages/Pricing.test.tsx src/pages/Checkout.test.tsx src/pages/Search.test.tsx src/pages/BugReport.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 6 interactive-page test files exit 0
    - Each file: `grep -c "it(\|test(" ...` returns at least 2
    - Each file: `grep -c "expect(" ...` returns at least 4
    - Each file: `grep -c "render(" ...` returns at least 2
    - `grep -c "vi.mocked(apiClient" frontend/src/pages/ContactUs.test.tsx frontend/src/pages/Search.test.tsx frontend/src/pages/BugReport.test.tsx` returns at least 3
    - `grep -c "\.skip(" frontend/src/pages/Home.test.tsx frontend/src/pages/ContactUs.test.tsx frontend/src/pages/Pricing.test.tsx frontend/src/pages/Checkout.test.tsx frontend/src/pages/Search.test.tsx frontend/src/pages/BugReport.test.tsx` returns 0
  </acceptance_criteria>
  <done>6 interactive public pages covered with happy-path + at least one error/empty state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write Profile.test.tsx and ViewUser.test.tsx (user profile pages)</name>
  <read_first>
    - frontend/src/pages/Profile.tsx
    - frontend/src/pages/ViewUser.tsx
    - frontend/src/api/images.ts (uploadImage signature)
    - frontend/src/test/mocks/api.ts (mockUser)
    - .planning/phases/08-frontend-coverage-expansion/08-PATTERNS.md §11
  </read_first>
  <behavior>
    Profile.test.tsx (requires testScenarios.authenticated):
    - Test 1: renders authenticated user profile (username + email visible).
    - Test 2: image upload — user selects File; apiClient.post to /images/upload called with FormData.
    - Test 3: error state on upload failure.
    At least 2 it-blocks, at least 6 expects.

    ViewUser.test.tsx:
    - Test 1: renders another user's profile from route param.
    - Test 2: 404 / empty state when user not found.
    At least 2 it-blocks, at least 4 expects.
  </behavior>
  <action>
    Profile.test.tsx:
    ```tsx
    import { describe, it, expect, beforeEach, vi } from 'vitest';
    import userEvent from '@testing-library/user-event';
    import { render, screen, waitFor, testScenarios } from '../test/utils/test-utils';
    import { apiClient } from '../api/client';
    import { mockUser } from '../test/mocks/api';
    import Profile from './Profile';

    describe('Profile page', () => {
      beforeEach(() => {
        vi.clearAllMocks();
        vi.mocked(apiClient.get).mockResolvedValue({ data: mockUser });
      });

      it('renders authenticated user profile', async () => {
        render(<Profile />, testScenarios.authenticated);
        await waitFor(() => expect(screen.getByText(mockUser.username)).toBeInTheDocument());
        expect(screen.getByText(mockUser.email)).toBeInTheDocument();
      });

      it('uploads profile image via apiClient.post with FormData', async () => {
        vi.mocked(apiClient.post).mockResolvedValueOnce({
          data: { file_key: 'k', presigned_url: 'u', message: 'ok' },
        });
        const user = userEvent.setup();
        const file = new File(['x'], 'avatar.jpg', { type: 'image/jpeg' });
        render(<Profile />, testScenarios.authenticated);
        await waitFor(() => screen.getByText(mockUser.username));
        const fileInput = screen.getByLabelText(/upload|avatar|image/i);
        await user.upload(fileInput, file);
        await waitFor(() =>
          expect(apiClient.post).toHaveBeenCalledWith(
            expect.stringContaining('/images/upload'),
            expect.any(FormData),
            expect.anything()
          )
        );
      });
    });
    ```

    ViewUser.test.tsx:
    ```tsx
    describe('ViewUser page', () => {
      beforeEach(() => vi.clearAllMocks());

      it('renders public user profile', async () => {
        vi.mocked(apiClient.get).mockResolvedValue({ data: mockUser });
        render(<ViewUser />, {
          route: `/users/${mockUser.id}`,
          ...testScenarios.unauthenticated,
        });
        await waitFor(() => expect(screen.getByText(mockUser.username)).toBeInTheDocument());
      });

      it('shows error when user not found', async () => {
        vi.mocked(apiClient.get).mockRejectedValue({ response: { status: 404 } });
        render(<ViewUser />, {
          route: `/users/missing`,
          ...testScenarios.unauthenticated,
        });
        await waitFor(() => expect(screen.getByText(/not found|user does not exist/i)).toBeInTheDocument());
      });
    });
    ```

    Verify selectors against actual source.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/pages/Profile.test.tsx src/pages/ViewUser.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exit 0
    - `grep -c "it(\|test(" frontend/src/pages/Profile.test.tsx` returns at least 2
    - `grep -c "expect(" frontend/src/pages/Profile.test.tsx` returns at least 6
    - `grep -c "it(\|test(" frontend/src/pages/ViewUser.test.tsx` returns at least 2
    - `grep -c "expect(" frontend/src/pages/ViewUser.test.tsx` returns at least 4
    - `grep -c "FormData\|expect.any(FormData)" frontend/src/pages/Profile.test.tsx` returns at least 1 (image upload covered)
    - `grep -c "\.skip(" frontend/src/pages/Profile.test.tsx frontend/src/pages/ViewUser.test.tsx` returns 0
  </acceptance_criteria>
  <done>Profile covers authenticated render + image upload via FormData; ViewUser covers public-user render + 404 path.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm test -- --run src/pages/` (excluding admin and subdirectories — `src/pages/*.test.tsx`) exits 0.
- Per VALIDATION.md Wave 3: every page test has at least 1 render( and at least 3 assertions.
- No `.skip()`.
- Static pages have minimal coverage; interactive pages have full happy-path + error.
</verification>

<success_criteria>
- All 13 public top-level pages have at least one test file.
- Static policy pages: heading visible + content present.
- Interactive pages: happy path + at least one error/empty state.
- Profile image upload via FormData exercised.
- Wave 3 nears completion (this is the largest Wave 3 group).
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-coverage-expansion/08-14-SUMMARY.md` with:
- Test counts per file.
- Coverage delta vs baseline for `frontend/src/pages/*.tsx` (top-level).
- If the plan was split per the RESEARCH.md §3 Open Question 3 checkpoint, note the split rationale.
</output>
