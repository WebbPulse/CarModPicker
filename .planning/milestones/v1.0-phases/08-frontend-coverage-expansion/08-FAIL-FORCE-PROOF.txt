=== FAIL-FORCE PROOF — Wave 5 SAFE-03 enforcement evidence ===
Date: 2026-04-24
Plan: 08-20
Source config: frontend/vitest.config.ts
Purpose: Prove the SAFE-03 coverage gate in vitest.config.ts actually enforces by
         raising one threshold above measured, confirming non-zero exit + threshold
         error message, then restoring D-06 values and confirming exit 0.

Per VALIDATION.md Manual-Only Verifications (row 1 — "Fail-force proof:
threshold block actually enforces"):
  1. Uncomment thresholds with values ABOVE measured (e.g. lines: 95).
  2. Run `npm run test:coverage`; confirm non-zero exit + "does not meet global
     threshold" message.
  3. Restore D-06 values (lines: 60, functions: 50, branches: 50, statements: 60).
  4. Confirm exit 0.
  5. Paste both outputs into this file.

Command under test:
  cd frontend && npm run test:coverage -- --run
  (package.json `test:coverage` = `vitest --coverage --run`)

--------------------------------------------------------------------------------
--- RAISED: lines=95 (forced failure) ---
--------------------------------------------------------------------------------

Config under test (thresholds block only):
  thresholds: {
    lines: 95,           <-- RAISED above measured 64.13%
    functions: 50,
    branches: 50,
    statements: 60,
  },

Command:     npm run test:coverage -- --run
Exit code:   1   (non-zero -> CI would fail the PR check)

Test run summary:
   Test Files  78 passed (78)
        Tests  516 passed (516)

Coverage summary row:
  All files   |   64.13 |    70.25 |   53.72 |   64.13 |

Threshold violation message (written by vitest to the tail of stdout, final line):
  ERROR: Coverage for lines (64.13%) does not meet global threshold (95%)

Outcome:     PASS — vitest propagated the threshold miss as exit code 1 AND
             emitted a human-readable ERROR line naming the offending dimension
             (lines), the measured value (64.13%), and the configured global
             threshold (95%). Frontend CI would now red-flag a PR at this state.

Full output captured to: /tmp/fail-force-raised.out (1351 lines, not committed —
this file preserves the verdict-bearing lines above).

--------------------------------------------------------------------------------
--- RESTORED: lines=60 (D-06 value) ---
--------------------------------------------------------------------------------

Config under test (thresholds block only) — matches final vitest.config.ts:
  thresholds: {
    lines: 60,           <-- RESTORED to D-06 target
    functions: 50,
    branches: 50,
    statements: 60,
  },

Command:     npm run test:coverage -- --run
Exit code:   0   (clean; PR check would be green)

Test run summary:
   Test Files  78 passed (78)
        Tests  516 passed (516)

Coverage summary row:
  All files   |   64.07 |    69.72 |   53.72 |   64.07 |

Threshold violation message: (none — vitest emitted no ERROR: lines with
                              threshold values at D-06 target)

Outcome:     PASS — with D-06 values in place, vitest observes every dimension
             at or above target (lines 64.07 >= 60, functions 53.72 >= 50,
             branches 69.72 >= 50, statements 64.07 >= 60) and exits 0.

Full output captured to: /tmp/fail-force-restored.out (1349 lines, not
committed — this file preserves the verdict-bearing lines above).

--------------------------------------------------------------------------------
--- VERDICT ---
--------------------------------------------------------------------------------

The SAFE-03 coverage gate in frontend/vitest.config.ts is proven to enforce:

  * When measured coverage is below configured threshold, vitest exits non-zero
    and emits a clear ERROR: Coverage for X does not meet global threshold.
  * When measured coverage meets configured threshold, vitest exits zero.
  * The gate is wired end-to-end; frontend-ci.yml runs `npm test -- --run
    --coverage` per SAFE-02 (Phase 1), which invokes the same vitest binary
    that produced these outputs, so the CI workflow will propagate both exit
    states into PR check status.

Final state of vitest.config.ts after this proof:
  thresholds: { lines: 60, functions: 50, branches: 50, statements: 60 }
  (i.e. D-06 values — matches the state committed in Task 2: 2fee4db)

SAFE-03 is fully armed.
