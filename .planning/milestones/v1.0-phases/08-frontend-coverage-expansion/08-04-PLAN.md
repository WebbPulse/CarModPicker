---
phase: 08-frontend-coverage-expansion
plan: 04
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - frontend/src/api/build_lists.test.ts
  - frontend/src/api/build_list_parts.test.ts
  - frontend/src/api/build_list_phases.test.ts
  - frontend/src/api/build_logs.test.ts
autonomous: true
requirements: [SAFE-03]
requirements_addressed: [SAFE-03]
tags: [frontend, api-tests, build-lists, wave-1]

must_haves:
  truths:
    - "Every buildListsApi method has URL + method + body assertions"
    - "Every buildListPartsApi, buildListPhasesApi, buildLogsApi method is covered"
    - "Nested URL patterns (e.g., /build-lists/:id/parts/:part_id) are asserted literally, not templated"
  artifacts:
    - path: "frontend/src/api/build_lists.test.ts"
      provides: "Full test coverage for buildListsApi (~8 methods)"
      min_lines: 80
    - path: "frontend/src/api/build_list_parts.test.ts"
      provides: "Full test coverage for buildListPartsApi (~6 methods)"
      min_lines: 70
    - path: "frontend/src/api/build_list_phases.test.ts"
      provides: "Full test coverage for buildListPhasesApi (~2 methods)"
      min_lines: 25
    - path: "frontend/src/api/build_logs.test.ts"
      provides: "Full test coverage for buildLogsApi (~4 methods)"
      min_lines: 45
  key_links:
    - from: "frontend/src/api/build_lists.test.ts"
      to: "frontend/src/api/client.ts"
      via: "apiClient mocked by setup.ts D-18"
      pattern: "vi\\.mocked\\(apiClient\\."
---

<objective>
Test the Build-list cluster of API modules: `build_lists.ts` (80 lines, ~8 methods), `build_list_parts.ts` (96 lines, ~6 methods), `build_list_phases.ts` (15 lines, ~2 methods), `build_logs.ts` (36 lines, ~4 methods). 227 source lines total per RESEARCH.md §2.

Purpose: Build lists are the core "build planning" surface. Covering these API modules unlocks Wave 3 buildLists and builder page tests (plans 08-12 and 08-13). Nested URL patterns (e.g., `/build-lists/:id/parts/:part_id`) are common here — tests must assert the concrete string, not a template.

Output: 4 new test files covering ~20 API methods total. Each test asserts URL + method + body/params per PATTERNS.md §7 skeleton. Nested-URL methods get assertions with interpolated test IDs (using `mockBuildList.id`).
</objective>

<execution_context>
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/workflows/execute-plan.md
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-frontend-coverage-expansion/08-CONTEXT.md
@.planning/phases/08-frontend-coverage-expansion/08-PATTERNS.md

<interfaces>
Canonical skeleton: PATTERNS.md §7.

Existing mocks in `frontend/src/test/mocks/api.ts`:
- `mockBuildList` (BuildListRead) — `id: '33333333-3333-7333-8333-333333333333'`, `car_id: mockCar.id`, `user_id: mockUser.id`.
- `mockPart` (PartRead) — `id: '44444444-4444-7444-8444-444444444444'`.

For tests that interpolate IDs into URLs, use `mockBuildList.id` literally and assert the full interpolated string:
```ts
expect(apiClient.get).toHaveBeenCalledWith(
  `/build-lists/${mockBuildList.id}/parts`
);
```

Per-module method count estimates (verify exact names by reading each source file):
- `build_lists.ts` (~8 methods): likely `listBuildLists`, `getBuildList`, `createBuildList`, `updateBuildList`, `deleteBuildList`, `copyBuildList`, `getBuildListsByCar`, `searchBuildLists`.
- `build_list_parts.ts` (~6 methods): likely `getBuildListParts`, `linkPartToBuildList`, `updateBuildListPart`, `removePartFromBuildList`, plus reorder/move helpers.
- `build_list_phases.ts` (~2 methods): likely `listPhases` and/or `updatePhases` on a build list.
- `build_logs.ts` (~4 methods): likely `getBuildLog`, `listBuildLogPosts`, `createPost`, `updatePost`, `deletePost`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write build_lists.test.ts and build_list_parts.test.ts</name>
  <read_first>
    - frontend/src/api/build_lists.ts
    - frontend/src/api/build_list_parts.ts
    - frontend/src/test/mocks/api.ts
    - .planning/phases/08-frontend-coverage-expansion/08-PATTERNS.md §7
  </read_first>
  <behavior>
    build_lists.test.ts:
    - At least 8 it-blocks, one per buildListsApi method.
    - At least 16 expect() calls (1 URL + 1 data or body per test).
    - copyBuildList test (if the method exists per IN-02 from Phase 7) asserts the free-tier-cap branch is not exercised in this test — just happy path.

    build_list_parts.test.ts:
    - At least 6 it-blocks.
    - At least 12 expect() calls.
    - Nested-URL methods use `mockBuildList.id` + `mockPart.id` interpolation.
  </behavior>
  <action>
    Open each source file, enumerate every exported method, write one test per method following PATTERNS.md §7. For nested URLs (e.g., `linkPartToBuildList(build_list_id, part_id, body)`):
    ```ts
    it('linkPartToBuildList POSTs to nested URL', async () => {
      vi.mocked(apiClient.post).mockResolvedValueOnce({ data: {} });
      await buildListPartsApi.linkPartToBuildList(mockBuildList.id, mockPart.id, { note: 'x' });
      expect(apiClient.post).toHaveBeenCalledWith(
        `/build-lists/${mockBuildList.id}/parts/${mockPart.id}`,
        { note: 'x' }
      );
    });
    ```

    DO NOT re-mock `../api/client` — setup.ts D-18 handles it.

    For list/filter methods that accept optional query params, include one test with default params and one with a populated filter:
    ```ts
    it('listBuildLists with car_id param forwards via params option', async () => {
      vi.mocked(apiClient.get).mockResolvedValueOnce({ data: [] });
      await buildListsApi.listBuildLists({ car_id: mockCar.id });
      expect(apiClient.get).toHaveBeenCalledWith('/build-lists', {
        params: expect.objectContaining({ car_id: mockCar.id }),
      });
    });
    ```
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/api/build_lists.test.ts src/api/build_list_parts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run src/api/build_lists.test.ts` exits 0
    - `cd frontend && npm test -- --run src/api/build_list_parts.test.ts` exits 0
    - `grep -c "it(\|test(" frontend/src/api/build_lists.test.ts` returns at least 8
    - `grep -c "expect(" frontend/src/api/build_lists.test.ts` returns at least 16
    - `grep -c "it(\|test(" frontend/src/api/build_list_parts.test.ts` returns at least 6
    - `grep -c "expect(" frontend/src/api/build_list_parts.test.ts` returns at least 12
    - `grep -c "mockBuildList" frontend/src/api/build_lists.test.ts` returns at least 1
    - `grep -c "mockBuildList\|mockPart" frontend/src/api/build_list_parts.test.ts` returns at least 2 (both used in nested URLs)
    - `grep -c "\.skip(" frontend/src/api/build_lists.test.ts frontend/src/api/build_list_parts.test.ts` returns 0
  </acceptance_criteria>
  <done>All buildListsApi and buildListPartsApi methods tested; nested URLs verified with literal interpolated strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write build_list_phases.test.ts and build_logs.test.ts</name>
  <read_first>
    - frontend/src/api/build_list_phases.ts
    - frontend/src/api/build_logs.ts
    - frontend/src/test/mocks/api.ts
  </read_first>
  <behavior>
    build_list_phases.test.ts:
    - At least 2 it-blocks (one per buildListPhasesApi method).
    - At least 4 expect() calls.

    build_logs.test.ts:
    - At least 4 it-blocks (one per buildLogsApi method).
    - At least 8 expect() calls.
  </behavior>
  <action>
    Same pattern as Task 1. Enumerate every exported method by reading the source file; write one test per method with URL + method + body/params + return-value assertions.

    For build_logs.ts if it exposes post-creation + post-list methods, ensure both are covered. If there is an image-upload-related method that uses FormData, assert `expect.any(FormData)` like the PATTERNS.md §7 FormData variant.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/api/build_list_phases.test.ts src/api/build_logs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run src/api/build_list_phases.test.ts` exits 0
    - `cd frontend && npm test -- --run src/api/build_logs.test.ts` exits 0
    - `grep -c "it(\|test(" frontend/src/api/build_list_phases.test.ts` returns at least 2
    - `grep -c "expect(" frontend/src/api/build_list_phases.test.ts` returns at least 4
    - `grep -c "it(\|test(" frontend/src/api/build_logs.test.ts` returns at least 4
    - `grep -c "expect(" frontend/src/api/build_logs.test.ts` returns at least 8
    - `grep -c "\.skip(" frontend/src/api/build_list_phases.test.ts frontend/src/api/build_logs.test.ts` returns 0
  </acceptance_criteria>
  <done>All buildListPhasesApi and buildLogsApi methods tested.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm test -- --run src/api/build_lists.test.ts src/api/build_list_parts.test.ts src/api/build_list_phases.test.ts src/api/build_logs.test.ts` exits 0.
- Per-file: expect-count > it-count.
- Nested URLs use literal `mockBuildList.id` + `mockPart.id` interpolation.
</verification>

<success_criteria>
- Every build-list-cluster API method has URL + method + body assertions.
- Nested URL patterns are tested against concrete IDs from canonical fixtures.
- Build-list API surface fully ready to feed Wave 3 buildLists + builder page tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-coverage-expansion/08-04-SUMMARY.md` with:
- Test counts per file.
- Coverage delta vs baseline for the 4 build-list-cluster source files.
- Note any methods that required branch testing (e.g., copyBuildList free-tier cap).
</output>
