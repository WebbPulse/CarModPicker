---
phase: 08-frontend-coverage-expansion
plan: 06
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - frontend/src/api/search.test.ts
  - frontend/src/api/app_settings.test.ts
  - frontend/src/api/utility.test.ts
autonomous: true
requirements: [SAFE-03]
requirements_addressed: [SAFE-03]
tags: [frontend, api-tests, utility, wave-1]

must_haves:
  truths:
    - "searchApi covers global search + per-entity-type search if exposed"
    - "appSettingsApi covers read + update paths"
    - "utility.ts either has minimal tests OR is excluded with D-15 rationale"
  artifacts:
    - path: "frontend/src/api/search.test.ts"
      provides: "Full test coverage for searchApi (~1-3 methods)"
      min_lines: 25
    - path: "frontend/src/api/app_settings.test.ts"
      provides: "Full test coverage for appSettingsApi (~2 methods)"
      min_lines: 25
    - path: "frontend/src/api/utility.test.ts"
      provides: "Minimal smoke test for utilityApi (or D-15 exclude if test cost exceeds module value)"
      min_lines: 15
  key_links:
    - from: "frontend/src/api/search.test.ts"
      to: "frontend/src/api/client.ts"
      via: "apiClient mocked by setup.ts D-18"
      pattern: "vi\\.mocked\\(apiClient\\."
---

<objective>
Test the Utility cluster of API modules: `search.ts` (27 lines, ~1 method), `app_settings.ts` (25 lines, ~2 methods), `utility.ts` (9 lines, ~2 methods — D-15 exclusion candidate). 61 source lines total per RESEARCH.md §2 — the smallest Wave 1 cluster.

Purpose: These are small modules but collectively contribute several percentage points to line coverage. Also, `utility.ts` at 9 lines is the clearest D-15 exclusion candidate in the API layer — the planner delegates the final call to the executor per D-15.

Output: 3 new test files covering the search + settings + utility API modules. If utility.ts is a trivial re-export, it gets a D-15 exclude with rationale comment in vitest.config.ts.
</objective>

<execution_context>
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/workflows/execute-plan.md
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-frontend-coverage-expansion/08-CONTEXT.md
@.planning/phases/08-frontend-coverage-expansion/08-PATTERNS.md

<interfaces>
Canonical skeleton: PATTERNS.md §7. Same pattern as plans 08-02 through 08-05.

Per-module method count estimates:
- `search.ts` (~1 method): likely a single `search(query, filters)` method.
- `app_settings.ts` (~2 methods): likely `getAppSettings` + `updateAppSettings`.
- `utility.ts` (~2 methods): 9 lines — likely either a pair of trivial helpers or a thin re-export. The executor reads the source to decide test vs exclude.

D-15 precedent: plan 08-03 may have already added an exclusion for retailers.ts. If so, utility.ts exclusion (if chosen) follows the same comment-inline-rationale pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write search.test.ts and app_settings.test.ts</name>
  <read_first>
    - frontend/src/api/search.ts
    - frontend/src/api/app_settings.ts
    - frontend/src/test/mocks/api.ts
    - .planning/phases/08-frontend-coverage-expansion/08-PATTERNS.md §7
  </read_first>
  <behavior>
    search.test.ts:
    - At least 1 it-block per searchApi method (likely 1-3 total).
    - At least 2 expect() calls per test.
    - Cover both a happy query (with results) and an empty-results case.

    app_settings.test.ts:
    - At least 2 it-blocks (read + update).
    - At least 4 expect() calls.
  </behavior>
  <action>
    Open each source file, list exported methods, write one test per method. For search.ts:
    ```ts
    it('search forwards query string to /search', async () => {
      vi.mocked(apiClient.get).mockResolvedValueOnce({ data: { results: [] } });
      await searchApi.search('honda civic');
      expect(apiClient.get).toHaveBeenCalledWith('/search', {
        params: expect.objectContaining({ q: 'honda civic' }),
      });
    });
    ```
    (Adjust param name + URL to match actual source.)

    For app_settings.ts, cover the read path (GET /app-settings or similar) and the update path (PUT/PATCH /app-settings).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/api/search.test.ts src/api/app_settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run src/api/search.test.ts` exits 0
    - `cd frontend && npm test -- --run src/api/app_settings.test.ts` exits 0
    - `grep -c "it(\|test(" frontend/src/api/search.test.ts` returns at least 1
    - `grep -c "expect(" frontend/src/api/search.test.ts` returns at least 2
    - `grep -c "it(\|test(" frontend/src/api/app_settings.test.ts` returns at least 2
    - `grep -c "expect(" frontend/src/api/app_settings.test.ts` returns at least 4
    - `grep -c "\.skip(" frontend/src/api/search.test.ts frontend/src/api/app_settings.test.ts` returns 0
  </acceptance_criteria>
  <done>searchApi and appSettingsApi covered with URL + method + params/body assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decide utility.test.ts — test or D-15 exclude</name>
  <read_first>
    - frontend/src/api/utility.ts (read entire file — 9 lines, determine if triviality warrants D-15 exclude)
    - frontend/vitest.config.ts (check if plan 08-03 already landed a D-15 exclude for retailers.ts)
    - .planning/phases/08-frontend-coverage-expansion/08-CONTEXT.md §D-15
  </read_first>
  <behavior>
    utility.test.ts (if chosen):
    - At least 1 it-block per utilityApi method (~2 methods likely).
    - At least 2 expect() calls.

    OR if excluding:
    - `frontend/vitest.config.ts` gains `'src/api/utility.ts',` in the coverage.exclude list with a D-15 rationale comment on the preceding line.
  </behavior>
  <action>
    Read `frontend/src/api/utility.ts`. If every exported function is a trivial wrapper (e.g., `apiClient.get('/health')` or similar), choose Option B (exclude):
    ```ts
    // D-15: utility.ts exposes ~2 trivial health/ping wrappers, 9 lines; test-to-value ratio below threshold.
    'src/api/utility.ts',
    ```
    Add to `vitest.config.ts` coverage.exclude. Do NOT create utility.test.ts.

    If there is any branching logic or non-trivial computation, choose Option A (test). Write 1 test per exported method following PATTERNS.md §7 skeleton.

    Document the decision in the plan's SUMMARY.md with the source file's 9 lines pasted for evidence.
  </action>
  <verify>
    <automated>test -f frontend/src/api/utility.test.ts &amp;&amp; cd frontend &amp;&amp; npm test -- --run src/api/utility.test.ts || grep -q "src/api/utility.ts" frontend/vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - Either: `frontend/src/api/utility.test.ts` exists AND `cd frontend && npm test -- --run src/api/utility.test.ts` exits 0
    - OR: `grep -c "src/api/utility.ts" frontend/vitest.config.ts` returns at least 1 (inside coverage.exclude) AND an adjacent line contains `D-15`
    - If test file exists: `grep -c "\.skip(" frontend/src/api/utility.test.ts` returns 0
    - If test file exists: `grep -c "it(\|test(" frontend/src/api/utility.test.ts` returns at least 1
  </acceptance_criteria>
  <done>utility.ts either has tests covering its exported methods OR is excluded from coverage with a D-15 rationale comment; decision documented in SUMMARY.md.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm test -- --run src/api/search.test.ts src/api/app_settings.test.ts` exits 0.
- utility.ts covered OR excluded (one of the two paths).
- Per-file: expect-count > it-count for all test files created.
</verification>

<success_criteria>
- Every searchApi and appSettingsApi method tested.
- utility.ts decision (test vs exclude) recorded with rationale.
- Wave 1 API-module cluster complete — plans 08-02 through 08-06 combined cover all 20 domain modules + client.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-coverage-expansion/08-06-SUMMARY.md` with:
- Test counts per file.
- Coverage delta vs baseline for the 3 utility-cluster source files.
- utility.ts decision record (test vs exclude).
</output>
