---
phase: 02-observability
plan: 5
plan_number: 5
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - terraform/monitoring.tf
  - .planning/codebase/CONCERNS.md
  - .planning/phases/02-observability/02-HUMAN-UAT.md
autonomous: false
requirements: [OBS-03]
threat_refs: [T-02-METRIC-CARD, T-02-IAM-DRIFT]

must_haves:
  truths:
    - "Terraform contains exactly one new `aws_cloudwatch_metric_alarm.crawler_parse_failure_composite` resource"
    - "Alarm uses metric-math expression with m1=Ingested, m2=ParseFailures, both filtered `RunType=live`, `Environment=var.app_environment`"
    - "Metric-math expression literal is `IF((m1+m2) < 10, 0, m2/(m1+m2))` (suppresses below 10-sample noise, D-23)"
    - "Threshold=0.5, comparison_operator=GreaterThanThreshold (strict > per Landmine 10), evaluation_periods=1, datapoints_to_alarm=1, treat_missing_data=notBreaching, period=3600 (1 hour)"
    - "`period` appears ONLY inside each `metric_query.metric {}` block — NEVER as a top-level attribute of aws_cloudwatch_metric_alarm (terraform-provider-aws#29398 / Landmine 7)"
    - "Both `alarm_actions` and `ok_actions` reference `aws_sns_topic.alarms.arn` (recovery emails per D-26)"
    - "Alarm description includes runbook anchor `Runbook: .planning/codebase/CONCERNS.md#crawler-drift-runbook` (D-27 — surfaced in SNS email)"
    - "`# TODO(phase-3): convert composite alarm to per-adapter via for_each = toset(file(...))` marker present in monitoring.tf beside the new alarm (D-30)"
    - ".planning/codebase/CONCERNS.md has a new `## Crawler Drift Runbook` section with anchor `#crawler-drift-runbook` (D-28)"
    - "02-HUMAN-UAT.md enumerates 7 D-62 checklist items across all 5 OBS requirements + staging→prod gate per D-58"
    - "`terraform validate` and `terraform fmt -check` both exit 0"
    - "`terraform plan` for staging successfully renders the new alarm resource (autonomous step); `terraform apply` to prod is flagged `autonomous: false` pending HUMAN-UAT sign-off per D-58"
  artifacts:
    - path: terraform/monitoring.tf
      provides: "aws_cloudwatch_metric_alarm.crawler_parse_failure_composite resource with metric-math + NaN-via-0 suppression + Phase 3 for_each TODO marker"
      contains: "crawler_parse_failure_composite"
    - path: .planning/codebase/CONCERNS.md
      provides: "Crawler Drift Runbook section (anchor #crawler-drift-runbook) — step-by-step response to parse-failure alarm"
      contains: "crawler-drift-runbook"
    - path: .planning/phases/02-observability/02-HUMAN-UAT.md
      provides: "Phase 2 success-verification checklist (7 items per D-62) + staging→prod bake gate per D-58"
      min_lines: 80
      contains: "HUMAN-UAT"
  key_links:
    - from: "terraform/monitoring.tf crawler_parse_failure_composite.alarm_actions"
      to: "terraform/monitoring.tf aws_sns_topic.alarms.arn"
      via: "ARN reference (pre-existing SNS topic per D-24)"
      pattern: "aws_sns_topic.alarms.arn"
    - from: "alarm metric_query[ingested/failures]"
      to: "CarModPicker/Crawlers namespace (populated by plan 02-03 EMF emission)"
      via: "namespace + metric_name + dimensions { Environment, RunType=live }"
      pattern: "CarModPicker/Crawlers"
    - from: "alarm description"
      to: ".planning/codebase/CONCERNS.md#crawler-drift-runbook"
      via: "string description surfaced in SNS email"
      pattern: "crawler-drift-runbook"
    - from: "02-HUMAN-UAT.md"
      to: "All OBS-01..OBS-05 deliverables"
      via: "D-62 checklist closes the phase"
      pattern: "OBS-"
---

<objective>
Close Phase 2 with: (a) the composite parse-failure CloudWatch alarm in Terraform (aggregate across all adapters, `RunType=live` filter, Phase 3 converts to per-adapter via `for_each` — D-29/D-30), (b) the Crawler Drift Runbook in `.planning/codebase/CONCERNS.md` that the alarm description points to, and (c) the 02-HUMAN-UAT.md checklist that gates the staging → prod bake → prod promotion per D-58.

Purpose: OBS-03 success criterion — "A CloudWatch alarm triggers an SNS → SES email when any adapter's parse-failure rate exceeds 50%." D-24 interprets "SNS → SES" as "alarm reaches operator email" which the existing `aws_sns_topic.alarms` (subscribed to `tyler@webbpulse.com` + `tylert2610@gmail.com` via email protocol) satisfies identically — deviation captured in UAT. Phase 2 ships aggregate granularity; Phase 3 converts to per-adapter after CRAWL-01/02 adapter auto-discovery lands (D-29).

This plan is the phase-closing plan: it ships the alarm, the runbook the alarm emails you to, and the 7-item UAT checklist that validates every OBS-0X requirement end-to-end in staging before prod promotion.

Output:
1. `terraform/monitoring.tf` — new `aws_cloudwatch_metric_alarm.crawler_parse_failure_composite` resource with metric_query blocks (Landmine 7: NO top-level period), `# TODO(phase-3)` marker
2. `.planning/codebase/CONCERNS.md` — new `## Crawler Drift Runbook` section (anchor `#crawler-drift-runbook`)
3. `.planning/phases/02-observability/02-HUMAN-UAT.md` (NEW) — D-62 7-item checklist + staging→prod bake gate

`autonomous: false` — `terraform plan` (staging dry-run) is automated; `terraform apply` to prod and the live-alarm HUMAN-UAT verification are manual operator actions gated on 24-hour staging bake per D-58.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/02-observability/02-CONTEXT.md
@.planning/phases/02-observability/02-RESEARCH.md
@.planning/phases/02-observability/02-PATTERNS.md
@.planning/phases/02-observability/02-VALIDATION.md

@.planning/phases/02-observability/02-01-PLAN.md
@.planning/phases/02-observability/02-02-PLAN.md
@.planning/phases/02-observability/02-03-PLAN.md
@.planning/phases/02-observability/02-04-PLAN.md

@terraform/monitoring.tf
@terraform/variables.tf
@.planning/codebase/CONCERNS.md
@.planning/phases/01-safety-nets-ci-hardening/01-HUMAN-UAT.md

<interfaces>
<!-- Composite alarm resource (02-RESEARCH.md §1 L146-L195) -->
<!-- Landmine 7: NO top-level period. Landmine 9: datapoints_to_alarm ≤ evaluation_periods. -->
<!-- Landmine 10: GreaterThanThreshold (strict) — NOT GreaterThanOrEqualToThreshold. -->

```hcl
resource "aws_cloudwatch_metric_alarm" "crawler_parse_failure_composite" {
  alarm_name        = "${local.prefix}-crawler-parse-failure-composite"
  alarm_description = "Parse-failure rate >50% across all live-mode crawlers. Runbook: .planning/codebase/CONCERNS.md#crawler-drift-runbook"

  comparison_operator = "GreaterThanThreshold"  # strict > (Landmine 10)
  evaluation_periods  = 1
  datapoints_to_alarm = 1                       # 1 of 1 (Landmine 9)
  threshold           = 0.5
  treat_missing_data  = "notBreaching"           # idle adapters stay quiet

  # NO top-level `period` — bug terraform-provider-aws#29398 / Landmine 7
  # period lives only inside each metric_query.metric {} block below

  metric_query {
    id = "ingested"
    metric {
      metric_name = "Ingested"
      namespace   = "CarModPicker/Crawlers"
      period      = 3600                        # 1 hour — matches hourly crawl cadence
      stat        = "Sum"
      dimensions = {
        Environment = var.app_environment
        RunType     = "live"                     # exclude rescrape (D-21)
      }
    }
  }

  metric_query {
    id = "failures"
    metric {
      metric_name = "ParseFailures"
      namespace   = "CarModPicker/Crawlers"
      period      = 3600
      stat        = "Sum"
      dimensions = {
        Environment = var.app_environment
        RunType     = "live"
      }
    }
  }

  metric_query {
    id          = "rate"
    expression  = "IF((ingested + failures) < 10, 0, failures / (ingested + failures))"
    label       = "Parse failure rate (suppressed below 10 samples)"
    return_data = true                          # exactly one query has return_data=true
  }

  alarm_actions = [aws_sns_topic.alarms.arn]
  ok_actions    = [aws_sns_topic.alarms.arn]

  # TODO(phase-3): convert composite alarm to per-adapter via:
  #   for_each = toset(setsubtract(file("${path.module}/adapter_names.txt"), var.disabled_parse_alarms))
  # after CRAWL-01/02 adapter auto-discovery lands; add AdapterName dimension
  # to each metric_query.metric.dimensions map. Cost: 114 alarms × $0.10/mo = $11.40/mo.
}
```

<!-- The existing pre-Phase-2 apprunner_5xx alarm at monitoring.tf L56-L73 is the reference analog. Same alarm_actions + ok_actions + notBreaching shape. -->
</interfaces>
</context>

<tasks>

<task id="02-05-01" type="auto">
  <name>Task 1: Add composite parse-failure alarm to terraform/monitoring.tf + Phase 3 TODO marker</name>
  <files>terraform/monitoring.tf</files>
  <read_first>
    - terraform/monitoring.tf (read full — find the existing `aws_sns_topic.alarms` at top + existing 5 alarms starting at L56; new alarm goes at the end for clean append)
    - terraform/variables.tf (post-02-02 — confirm `disabled_parse_alarms` variable exists from plan 02-02 task 3 STEP A; also confirm `app_environment` variable exists — if not, note that the alarm uses `var.app_environment` which must be declared elsewhere already. If `app_environment` does NOT exist, substitute `local.env` or similar based on actual file contents.)
    - .planning/phases/02-observability/02-CONTEXT.md §D-22 (metric-math expression m2/(m1+m2) > 0.5)
    - .planning/phases/02-observability/02-CONTEXT.md §D-23 (NaN-via-0 small-sample suppression below 10 samples, matches runner.py total>=10 drift threshold)
    - .planning/phases/02-observability/02-CONTEXT.md §D-24 (SNS topic reuse — treat "SNS→SES" as "alarm reaches operator email" per SNS→email protocol already in place; deviation noted)
    - .planning/phases/02-observability/02-CONTEXT.md §D-25 (evaluation window — period=3600, evaluation_periods=1, notBreaching)
    - .planning/phases/02-observability/02-CONTEXT.md §D-26 (ok_actions symmetry — recovery emails)
    - .planning/phases/02-observability/02-CONTEXT.md §D-27 (description surfaces runbook anchor in SNS email)
    - .planning/phases/02-observability/02-CONTEXT.md §D-29 (Phase 2 = composite, Phase 3 = per-adapter via for_each)
    - .planning/phases/02-observability/02-CONTEXT.md §D-30 (Phase 3 handoff TODO marker)
    - .planning/phases/02-observability/02-CONTEXT.md §D-31 (var.disabled_parse_alarms mute mechanism; Phase 3 consumes it, Phase 2 declares it)
    - .planning/phases/02-observability/02-RESEARCH.md §1 L146-L195 (verbatim terraform skeleton — lift it)
    - .planning/phases/02-observability/02-RESEARCH.md §2 L621-L649 (metric-math semantics + Landmines 7, 8, 9, 10)
    - .planning/phases/02-observability/02-RESEARCH.md §5 Landmine 7 (NO top-level period — terraform-provider-aws#29398)
    - .planning/phases/02-observability/02-RESEARCH.md §5 Landmine 8 (NaN vs 0 — use 0 for portable comparison)
    - .planning/phases/02-observability/02-RESEARCH.md §5 Landmine 9 (datapoints_to_alarm ≤ evaluation_periods)
    - .planning/phases/02-observability/02-RESEARCH.md §5 Landmine 10 (GreaterThanThreshold strict > NOT GreaterThanOrEqualToThreshold)
    - .planning/phases/02-observability/02-PATTERNS.md §terraform/monitoring.tf (primary analog: apprunner_5xx at L56-L73)
    - .planning/phases/02-observability/02-VALIDATION.md OBS-03 rows 1-6
  </read_first>
  <action>
    Edit `terraform/monitoring.tf`. APPEND the new alarm at the end of the file (after all existing alarms).

    Use the exact HCL from the `<interfaces>` block above. Critical invariants:
    - Resource name: `aws_cloudwatch_metric_alarm` `crawler_parse_failure_composite`
    - `alarm_name = "${local.prefix}-crawler-parse-failure-composite"` (matches existing `${local.prefix}-apprunner-5xx` etc. convention per PATTERNS.md L801)
    - `alarm_description` MUST contain the literal string `"Runbook: .planning/codebase/CONCERNS.md#crawler-drift-runbook"` (D-27 — this is what operator sees in SNS email)
    - `comparison_operator = "GreaterThanThreshold"` — Landmine 10, strict > (NOT >=)
    - `evaluation_periods = 1`, `datapoints_to_alarm = 1` — 1-of-1 semantics, Landmine 9
    - `threshold = 0.5`
    - `treat_missing_data = "notBreaching"` — idle adapters quiet
    - NO top-level `period =` attribute — Landmine 7 / terraform-provider-aws#29398 (period lives ONLY inside each `metric_query.metric {}` block)
    - Three `metric_query` blocks:
      - `id = "ingested"` — `metric { metric_name = "Ingested", namespace = "CarModPicker/Crawlers", period = 3600, stat = "Sum", dimensions = { Environment = var.app_environment, RunType = "live" } }`
      - `id = "failures"` — same shape with `metric_name = "ParseFailures"`
      - `id = "rate"` — `expression = "IF((ingested + failures) < 10, 0, failures / (ingested + failures))"`, `label = "Parse failure rate (suppressed below 10 samples)"`, `return_data = true` (exactly one metric_query MUST have `return_data = true`)
    - `alarm_actions = [aws_sns_topic.alarms.arn]` — reuse existing SNS topic (D-24 deviation noted in description)
    - `ok_actions = [aws_sns_topic.alarms.arn]` — recovery emails (D-26)
    - Include the `# TODO(phase-3):` comment block verbatim (D-30 — Phase 3 for_each conversion marker using `var.disabled_parse_alarms`)

    If `var.app_environment` does not exist in `terraform/variables.tf` (check via grep first), substitute the actual environment-naming variable used by other resources in monitoring.tf — check how existing alarms filter dimensions. The RESEARCH skeleton assumes `var.app_environment`; verify against codebase and adjust if needed. If neither works, the dimensions map may use a hard-coded string (e.g., `"staging"` or `"production"`) — but prefer a variable for environment flexibility.

    Also: plan 02-02 already added `variable "disabled_parse_alarms"` to terraform/variables.tf. Do NOT re-declare it here. This plan consumes it only via the Phase 3 TODO comment (not active in Phase 2).
  </action>
  <verify>
    <automated>cd terraform && terraform validate && terraform fmt -check && terraform plan -no-color | grep -c "crawler_parse_failure_composite"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "resource \"aws_cloudwatch_metric_alarm\" \"crawler_parse_failure_composite\"" terraform/monitoring.tf` returns 1 match
    - `grep -n "\${local.prefix}-crawler-parse-failure-composite" terraform/monitoring.tf` returns 1 match
    - `grep -n "Runbook: .planning/codebase/CONCERNS.md#crawler-drift-runbook" terraform/monitoring.tf` returns 1 match (D-27)
    - `grep -n "comparison_operator = \"GreaterThanThreshold\"" terraform/monitoring.tf` returns >= 1 match (strict > — Landmine 10)
    - `grep -n "threshold           = 0.5" terraform/monitoring.tf` returns 1 match (spacing may vary — accept `threshold  = 0.5` etc.)
    - `grep -n "treat_missing_data  = \"notBreaching\"" terraform/monitoring.tf` returns >= 1 match
    - `grep -nE "^  period " terraform/monitoring.tf` — no TOP-LEVEL `period` on the new alarm (Landmine 7). Top-level period is allowed on the pre-existing 5 alarms that use inline namespace+metric_name shape — the NEW alarm resource must NOT have it. Verify by reading the `crawler_parse_failure_composite` block: `awk '/resource \"aws_cloudwatch_metric_alarm\" \"crawler_parse_failure_composite\"/,/^}/' terraform/monitoring.tf | grep -cE "^  period = "` returns 0.
    - `grep -n "metric_query {" terraform/monitoring.tf` returns >= 3 matches (ingested + failures + rate)
    - `grep -n "id = \"ingested\"" terraform/monitoring.tf` returns 1 match
    - `grep -n "id = \"failures\"" terraform/monitoring.tf` returns 1 match
    - `grep -n "id          = \"rate\"" terraform/monitoring.tf` returns 1 match (or similar alignment — the id value matters, not the whitespace)
    - `grep -n "CarModPicker/Crawlers" terraform/monitoring.tf` returns >= 2 matches (one per source metric)
    - `grep -n "metric_name = \"Ingested\"" terraform/monitoring.tf` returns 1 match
    - `grep -n "metric_name = \"ParseFailures\"" terraform/monitoring.tf` returns 1 match
    - `grep -n "period      = 3600" terraform/monitoring.tf` returns >= 2 matches (one per metric_query.metric block)
    - `grep -n "RunType     = \"live\"" terraform/monitoring.tf` returns >= 2 matches (one per metric_query; exclude rescrape)
    - `grep -nE 'expression.*IF\\(\\(ingested \\+ failures\\) < 10, 0, failures / \\(ingested \\+ failures\\)\\)' terraform/monitoring.tf` returns 1 match (the EXACT literal expression — D-22/D-23)
    - `grep -n "return_data = true" terraform/monitoring.tf` returns >= 1 match in the new resource (exactly one per resource)
    - `grep -n "alarm_actions = \\[aws_sns_topic.alarms.arn\\]" terraform/monitoring.tf` returns >= 1 new match (existing alarms already use this, so expect count went up by 1)
    - `grep -n "ok_actions    = \\[aws_sns_topic.alarms.arn\\]" terraform/monitoring.tf` returns >= 1 new match
    - `grep -n "TODO(phase-3)" terraform/monitoring.tf` returns 1 match (D-30 marker)
    - `grep -n "for_each = toset" terraform/monitoring.tf` returns 1 match (inside the TODO comment)
    - `grep -n "var.disabled_parse_alarms" terraform/monitoring.tf` returns 1 match (in TODO comment — Phase 3 consumes)
    - `cd terraform && terraform validate` exits 0 — Landmine 7 would cause this to fail if top-level period slipped in
    - `cd terraform && terraform fmt -check` exits 0
    - `cd terraform && terraform plan -no-color 2>&1 | grep -c "crawler_parse_failure_composite"` returns >= 1 (plan renders the new alarm — note: requires valid terraform.tfvars or auto-applied defaults; if plan fails on missing vars, document in SUMMARY)
  </acceptance_criteria>
  <done>
    Composite alarm resource lands in monitoring.tf with correct metric_query structure, NaN-via-0 expression, strict `>` threshold, 1-of-1 evaluation, `notBreaching` missing-data, BOTH alarm_actions and ok_actions wired to existing SNS topic, description contains runbook anchor, `# TODO(phase-3)` marker present. `terraform validate` + `fmt -check` both pass. Landmines 7, 8, 9, 10 all pinned by acceptance criteria grep patterns.
  </done>
</task>

<task id="02-05-02" type="auto">
  <name>Task 2: Add Crawler Drift Runbook section to CONCERNS.md</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (read full — find existing sections + append location)
    - .planning/phases/02-observability/02-CONTEXT.md §D-28 (runbook section — check adapter HTML fixture, re-run Phase 1 SAFE-07 characterization test, patch selectors, redeploy)
    - .planning/phases/02-observability/02-CONTEXT.md §D-27 (description surfaces anchor `#crawler-drift-runbook` in SNS email)
    - .planning/phases/01-safety-nets-ci-hardening/01-07-SUMMARY.md (read if exists — SAFE-07 details the 5 adapter characterization tests the runbook references)
  </read_first>
  <action>
    APPEND a new section to `.planning/codebase/CONCERNS.md` at the end of the file. The anchor MUST be `#crawler-drift-runbook` (exact, lowercase-kebab) because terraform/monitoring.tf description strings point to it literally:

    ```markdown
    ## Crawler Drift Runbook

    <a id="crawler-drift-runbook"></a>

    **Triggered by:** `${local.prefix}-crawler-parse-failure-composite` CloudWatch alarm (per D-22/D-27). SNS email arrives at `tyler@webbpulse.com` + `tylert2610@gmail.com`. Recovery email follows when aggregate parse-failure rate drops back below 50%.

    Alarm fires when, in the last hour, aggregate `ParseFailures / (Ingested + ParseFailures) > 0.5` across all adapters in `RunType=live`. Below 10 combined samples the rate is suppressed (returns 0) so idle / low-traffic periods don't trigger false positives (D-23).

    **Likely cause:** one or more retailer sites changed their HTML structure and the adapter's selectors no longer match product pages. `skipped_not_product` goes up; `ingested` goes down; ratio breaches threshold.

    **Response steps:**

    1. **Identify the drifting adapter(s).** In Phase 2 (aggregate alarm), inspect the CloudWatch namespace `CarModPicker/Crawlers` grouped by `AdapterName`:
       ```
       aws cloudwatch list-metrics --namespace "CarModPicker/Crawlers" \
         --dimensions "Name=RunType,Value=live" "Name=Environment,Value=production"
       ```
       Pull each adapter's `ParseFailures` + `Ingested` sums over the last hour; adapters with ratio > 0.5 are the suspects. **After Phase 3** (per-adapter alarms land via CRAWL-01/02 auto-discovery per D-29), the alarm name itself names the adapter.

    2. **Pull the failing adapter's most recent archived HTML.** Archives live in the crawl-archive S3 bucket (local: `carmodpicker-local-crawl`; prod: `carmodpicker-production-crawl-data` per SAFE-07 D-21):
       ```
       aws s3 cp s3://carmodpicker-production-crawl-data/<adapter>/<date>/<sample>.html ./inspect.html
       ```

    3. **Re-run the adapter characterization test** (SAFE-07 from Phase 1) against the archived fixture. If the test still passes, the drift is in pages not covered by the fixture — proceed to step 4 with a NEW sample. If the test fails, the adapter code needs patching:
       ```
       cd backend && pytest -n auto tests/crawlers/test_characterization_<adapter>.py -xvs
       ```

    4. **Patch the adapter.** Adapter lives in `backend/app/crawlers/adapters/<name>.py`. Update selectors in `parse_product_page()` / `discover_product_urls()` to match the new HTML shape. Re-run the characterization test locally.

    5. **(Optional) Mute the alarm** during the fix window using the `var.disabled_parse_alarms` terraform list (D-31 — active only after Phase 3 per-adapter conversion; in Phase 2 the composite alarm cannot be muted per-adapter). For Phase 2 emergency mute, disable the alarm in the AWS console and open a follow-up PR to persist. Never comment out terraform.

    6. **Deploy the fix** via the normal PR → GHA → staging → bake 24h → prod flow (D-58). Alarm should auto-recover and send an `OK` SNS email (D-26) once the next hourly evaluation window sees healthy ratios.

    7. **Post-fix:** update the adapter's HTML fixture in `backend/tests/crawlers/fixtures/` so the characterization test would have caught this drift. This closes the loop that SAFE-07 established.

    **Phase 3 handoff note:** Phase 3 (CRAWL-01/02/03) ships adapter auto-discovery + `ADAPTER_NAME: ClassVar[str]` enforcement. After Phase 3, the terraform TODO marker in `terraform/monitoring.tf` converts this composite alarm into 114 per-adapter alarms via `for_each = toset(file("${path.module}/adapter_names.txt"))`. Cost: ~$11.40/mo (user explicitly accepted this cost for per-adapter precision — D-29).
    ```

    Ensure the anchor line `<a id="crawler-drift-runbook"></a>` appears immediately after the section header so GitHub + raw-markdown viewers both resolve `#crawler-drift-runbook` as expected.
  </action>
  <verify>
    <automated>grep -c "crawler-drift-runbook" .planning/codebase/CONCERNS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "## Crawler Drift Runbook" .planning/codebase/CONCERNS.md` returns 1 match
    - `grep -n "id=\"crawler-drift-runbook\"" .planning/codebase/CONCERNS.md` returns 1 match (anchor)
    - `grep -n "pytest -n auto tests/crawlers/test_characterization_" .planning/codebase/CONCERNS.md` returns 1 match (links to SAFE-07 from Phase 1)
    - `grep -n "CarModPicker/Crawlers" .planning/codebase/CONCERNS.md` returns 1 match
    - `grep -n "RunType" .planning/codebase/CONCERNS.md` returns >= 1 match
    - `grep -n "var.disabled_parse_alarms" .planning/codebase/CONCERNS.md` returns 1 match (D-31 reference)
    - `grep -n "TODO marker in \`terraform/monitoring.tf\`" .planning/codebase/CONCERNS.md` returns 1 match (closes the D-30 handoff loop)
    - Terraform alarm description in monitoring.tf points to this anchor: `grep -c "crawler-drift-runbook" terraform/monitoring.tf` returns >= 1 (verifies cross-file link is live)
  </acceptance_criteria>
  <done>
    Crawler Drift Runbook section exists at anchor `#crawler-drift-runbook`, contains 7 numbered response steps, references SAFE-07 characterization tests, names the crawl-archive S3 bucket, and points to the Phase 3 for_each conversion. The terraform alarm description's runbook URL resolves to this section.
  </done>
</task>

<task id="02-05-03" type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Create 02-HUMAN-UAT.md (D-62 checklist) + staging→prod bake gate</name>
  <files>.planning/phases/02-observability/02-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/02-observability/02-CONTEXT.md §D-62 (7-item UAT checklist spec)
    - .planning/phases/02-observability/02-CONTEXT.md §D-58 (staging first, bake 24h, then prod)
    - .planning/phases/02-observability/02-CONTEXT.md §D-24 (SNS→email deviation from SNS→SES REQUIREMENTS wording — document in UAT)
    - .planning/phases/02-observability/02-VALIDATION.md §Manual-Only Verifications (the 9-item manual table mirrors UAT items)
    - .planning/phases/01-safety-nets-ci-hardening/01-HUMAN-UAT.md (sibling Phase 1 analog per PATTERNS.md — match structure)
  </read_first>
  <what-built>
    A 7-item human UAT checklist for Phase 2, matching D-62 item list:
    1. Backend 500 → Sentry event w/ request_id + user_id
    2. Crawler logger.error → Sentry event w/ adapter tag
    3. HTTPException NOT in Sentry (ignored)
    4. EMF → CloudWatch metrics (staging crawl → `aws cloudwatch list-metrics` returns ≥1 metric)
    5. Alarm fires → SNS email → alarm recovers → recovery email
    6. Frontend unhandled error → Sentry event w/ resolved sourcemap + on-error replay attached
    7. OBS-04 spot-check: staging request log shows `request_id=<uuid>` on every line

    Plus a section capturing the D-58 staging → bake 24h → prod gate.

    Plus a note on the D-24 deviation (SNS→email vs REQUIREMENTS wording "SNS→SES") for the verifier.
  </what-built>
  <how-to-verify>
    Create `.planning/phases/02-observability/02-HUMAN-UAT.md` with the 7 items. Use the Phase 1 sibling file structure as a template. Each UAT item MUST contain:
    - **What to do** (command or browser action)
    - **Expected result** (exact observable behavior)
    - **Pass/fail checkbox**
    - **Evidence** field (screenshot path or CloudWatch insights query URL — operator fills on verify)

    File structure:

    ```markdown
    ---
    phase: 02-observability
    document: HUMAN-UAT
    derived_from: [D-62, D-58]
    total_items: 7
    ---

    # Phase 2 — Human UAT Checklist

    > Per D-62: every Phase 2 deliverable must be verified end-to-end in staging before the 24-hour bake (D-58) and subsequent prod promotion. This checklist is the gate.

    ## Pre-conditions

    Before starting the checklist:
    - [ ] All 5 Phase 2 plans (02-01, 02-02, 02-03, 02-04, 02-05) merged to `main`
    - [ ] GitHub Actions deployed staging successfully (`terraform apply` ran against staging; App Runner staging redeployed; ECS crawler task definition updated)
    - [ ] Operator populated `SENTRY_DSN` via `aws secretsmanager put-secret-value` (terraform/README.md "Bootstrap: Sentry" steps)
    - [ ] Operator populated GitHub Actions secrets: `VITE_SENTRY_DSN`, `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT` for the frontend build
    - [ ] At least one staging crawl has run (manual trigger: invoke the ECS crawler task once) so CloudWatch has EMF data

    ## Deviation Note

    **SNS → email (not SNS → SES):** REQUIREMENTS OBS-03 says "SNS → SES." Phase 2 reuses the existing `aws_sns_topic.alarms` which is subscribed to emails directly via SNS → email protocol (not via SES). Operator experience is identical (email arrives). D-24 captures this deviation.

    ## Checklist Items

    ### Item 1 — Backend 500 reaches Sentry with context (OBS-01)

    - **Do:** Trigger a 500 against staging. Recommended: add `?trigger_500=1` test endpoint, or use the admin panel to invoke a known-broken path, or make a signed request with invalid payload that raises `RuntimeError` post-validation.
    - **Expected:** Within ~30 seconds, a new event appears in the Sentry backend project. Open the event:
      - [ ] `tags.request_id` is a UUID
      - [ ] `user.id` is the authenticated user's ID (if call was auth'd)
      - [ ] No email, no username visible on the event (send_default_pii=False + scope processor per D-03 + D-09)
      - [ ] Environment tag = `staging`
      - [ ] Server name tag = `apprunner-backend`
    - **Evidence:** Sentry event permalink: _______

    ### Item 2 — Crawler logger.error reaches Sentry with adapter tag (OBS-01)

    - **Do:** Pick one adapter (e.g., `amsperformance`) that currently works. Temporarily break its HTML selector in a staging branch OR manually invoke `logger.error(...)` inside a `with bg_log_context("crawler", "staging-test"):` block from a one-off script, with `Sentry.add_tag("adapter", "amsperformance")` in scope.
    - **Expected:** Sentry event captured. Open it:
      - [ ] `tags.request_id` starts with `bg:crawler:` (from plan 02-01 bg_log_context)
      - [ ] `tags.adapter == "amsperformance"`
      - [ ] Server name tag = `ecs-crawler`
    - **Evidence:** Sentry event permalink: _______

    ### Item 3 — HTTPException NOT in Sentry (OBS-01 ignore_errors)

    - **Do:** Trigger a 404 against staging (visit `/api/users/0` or any known-missing resource).
    - **Expected:**
      - [ ] HTTP response is 404 (normal client-visible behavior)
      - [ ] NO new Sentry event appears within 2 minutes (HTTPException is in `ignore_errors` list per D-07)
    - **Evidence:** Sentry project "events today" count at time T and time T+2min: _______ / _______

    ### Item 4 — EMF metrics reach CloudWatch (OBS-02)

    - **Do:** Run one staging crawl of a live adapter. Wait ~5 minutes for CloudWatch Logs extraction.
    - **Verify:**
      ```
      aws cloudwatch list-metrics \
        --namespace CarModPicker/Crawlers \
        --dimensions "Name=Environment,Value=staging" "Name=RunType,Value=live"
      ```
    - **Expected:**
      - [ ] Result contains at least 1 metric for the adapter just crawled
      - [ ] Metric names include `Ingested`, `ParseFailures`, `ElapsedSeconds`
      - [ ] Dimensions include `AdapterName`, `Environment`, `RunType`
    - **Cardinality sanity check:**
      ```
      aws cloudwatch list-metrics --namespace CarModPicker/Crawlers | jq '.Metrics | length'
      ```
      - [ ] Result ≤ 456 (114 adapters × 2 envs × 2 run types — D-19 budget)
    - **Evidence:** list-metrics JSON saved to: _______

    ### Item 5 — Parse-failure alarm fires + recovers (OBS-03)

    - **Do:** Simulate a parse-failure wave in staging. Two options:
      - **Option A (preferred):** Point a staging adapter at a known-broken selector so `skipped_not_product` >> `ingested` over 10+ samples for an hour.
      - **Option B (fast):** `aws cloudwatch put-metric-data` bulk-write synthetic values matching the alarm's dimensions (Environment=staging, RunType=live):
        ```
        aws cloudwatch put-metric-data --namespace CarModPicker/Crawlers \
          --metric-name ParseFailures --unit Count --value 50 \
          --dimensions AdapterName=simulated,Environment=staging,RunType=live
        aws cloudwatch put-metric-data --namespace CarModPicker/Crawlers \
          --metric-name Ingested --unit Count --value 5 \
          --dimensions AdapterName=simulated,Environment=staging,RunType=live
        ```
    - **Expected:**
      - [ ] Within 2× alarm period (2h) the `${prefix}-crawler-parse-failure-composite` alarm transitions to ALARM
      - [ ] SNS email arrives at `tyler@webbpulse.com` AND `tylert2610@gmail.com`
      - [ ] Email body contains `Runbook: .planning/codebase/CONCERNS.md#crawler-drift-runbook`
    - **Then:** Stop the synthetic traffic / revert the broken selector.
      - [ ] Within 2× alarm period the alarm transitions back to OK
      - [ ] Recovery SNS email arrives (D-26 `ok_actions` symmetry)
    - **Evidence:** Screenshot or forwarded email headers: _______

    ### Item 6 — Frontend error reaches Sentry with sourcemap + on-error replay (OBS-05)

    - **Do:** Trigger an unhandled error in the staging frontend. Recommended: temporarily add `throw new Error('UAT-test')` inside a page-level effect, deploy to staging, visit the page.
    - **Expected:**
      - [ ] Sentry frontend project receives the event within 30 seconds
      - [ ] Stack trace points to real `.tsx` file paths (not minified `main-ABC123.js`) — sourcemap upload worked (D-34)
      - [ ] Session Replay attached to the error event (click "View Replay" in Sentry)
      - [ ] No replays for user sessions that did NOT throw (ambient replay = 0 per D-32)
      - [ ] `user.id` attached if a user was logged in; nothing if anonymous (D-40)
    - **Deferred separate test — auth-route replay block (D-37):**
      - [ ] Trigger a frontend error on `/login` → event still reports to Sentry, but NO replay is attached
    - **Evidence:** Sentry event permalink + "Replay attached" screenshot: _______

    ### Item 7 — OBS-04 request-id on every CloudWatch log line

    - **Do:** Make a signed request to staging (`curl -H "Authorization: Bearer ..." https://staging.carmodpicker.com/api/users/me`). Note the `X-Request-ID` echo header from the response.
    - **Verify:** In CloudWatch Logs Insights for `/aws/apprunner/.../application`:
      ```
      filter @message like /<the-request-id>/
      | sort @timestamp
      ```
    - **Expected:**
      - [ ] Every log line emitted during that request carries `request_id=<uuid>` OR the JSON log record has `"request_id": "<uuid>"` (depending on formatter)
      - [ ] Any sqlalchemy query logs fired during that request also carry the same request_id (third-party propagation per D-48)
      - [ ] Every log line during the request also has `user_id=<id>`, never `user_id=-`
    - **Evidence:** Logs Insights query URL: _______

    ## Staging → Prod Promotion Gate (D-58)

    All 7 UAT items above MUST be marked pass AND at least 24 hours MUST have elapsed since staging deploy AND zero unexplained Sentry events from staging during that window before prod apply.

    - [ ] Items 1-7 all checked pass
    - [ ] `staging_deploy_timestamp`: _______
    - [ ] `prod_apply_approved_at`: _______ (must be ≥ staging_deploy_timestamp + 24h)
    - [ ] Staging Sentry project shows no unexplained events during the bake window (test events from this UAT are expected)
    - [ ] Operator runs `terraform -chdir=terraform apply -var-file=prod.tfvars` after approval

    ## Deviation Log

    - D-24: SNS → email (not SNS → SES). Operator impact: identical — email arrives at the same addresses. Noted for verifier traceability.
    ```

    This file replaces the placeholder UAT reference in 02-VALIDATION.md Manual-Only Verifications.
  </how-to-verify>
  <resume-signal>Type "approved" when the UAT checklist is reviewed, the 7 items are clear, the staging → prod gate is understood, and the file has been committed. Describe any issues (e.g., "item 5 needs a specific put-metric-data command template", "add a rollback step") if changes are needed.</resume-signal>
  <acceptance_criteria>
    - `test -f .planning/phases/02-observability/02-HUMAN-UAT.md` succeeds
    - `grep -c "^### Item " .planning/phases/02-observability/02-HUMAN-UAT.md` returns 7 (D-62 items)
    - `grep -n "OBS-01" .planning/phases/02-observability/02-HUMAN-UAT.md` returns >= 3 matches (items 1, 2, 3)
    - `grep -n "OBS-02" .planning/phases/02-observability/02-HUMAN-UAT.md` returns >= 1 match (item 4)
    - `grep -n "OBS-03" .planning/phases/02-observability/02-HUMAN-UAT.md` returns >= 1 match (item 5)
    - `grep -n "OBS-04" .planning/phases/02-observability/02-HUMAN-UAT.md` returns >= 1 match (item 7)
    - `grep -n "OBS-05" .planning/phases/02-observability/02-HUMAN-UAT.md` returns >= 1 match (item 6)
    - `grep -n "Staging → Prod Promotion Gate" .planning/phases/02-observability/02-HUMAN-UAT.md` returns 1 match (D-58)
    - `grep -n "24 hours" .planning/phases/02-observability/02-HUMAN-UAT.md` returns >= 1 match (bake window)
    - `grep -n "SNS → email" .planning/phases/02-observability/02-HUMAN-UAT.md` returns >= 1 match (D-24 deviation documented)
    - `grep -n "crawler-drift-runbook" .planning/phases/02-observability/02-HUMAN-UAT.md` returns >= 1 match (item 5 expects runbook link in SNS email)
    - `grep -n "CarModPicker/Crawlers" .planning/phases/02-observability/02-HUMAN-UAT.md` returns >= 1 match
  </acceptance_criteria>
  <done>
    02-HUMAN-UAT.md exists with 7 numbered items spanning OBS-01..OBS-05, a staging→prod bake gate per D-58, the SNS→email deviation per D-24 captured as a deviation log entry. Operator receives approval signal to proceed with the 24h staging bake and prod terraform apply.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CloudWatch metric → SNS topic → subscriber emails | Existing SNS subscriptions (already confirmed) carry alarm notifications |
| Alarm description string → SNS email body | Runbook anchor appears in email; anyone reading email sees the path |
| `terraform apply` (prod) → live AWS resources | Manual gate — 24h staging bake + HUMAN-UAT approval required |
| AdapterName dimension values → metric cardinality | Bounded by code-defined adapter list (114); no user input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan / Acceptance |
|-----------|----------|-----------|----------|-------------|------------------------------|
| T-02-METRIC-CARD | Denial of Service (cost) | CloudWatch alarm metric expansion | low | mitigate | Phase 2 ships exactly ONE alarm (composite). Phase 3 TODO marker explicitly references `var.disabled_parse_alarms` for Phase 3 mute mechanism — Phase 2 itself has nothing to mute. Acceptance: `grep -c "aws_cloudwatch_metric_alarm\" \"crawler_parse_failure" terraform/monitoring.tf` returns 1 (single new alarm). Cost = $0.10/mo per AWS pricing (D-59). |
| T-02-IAM-DRIFT | Elevation of Privilege | SNS topic publish permissions | low | mitigate | Reuses existing `aws_sns_topic.alarms` — already has IAM grants for CloudWatch alarm publishing (existing 5 alarms use same topic). No new IAM surface introduced. Acceptance: `grep -n "aws_sns_topic_subscription.*alarms" terraform/monitoring.tf` returns >= 2 (pre-existing subscriptions unchanged). |
| T-02-ALARM-FLAPPING | Availability (signal noise) | treat_missing_data + small-sample window | medium | mitigate | `treat_missing_data = "notBreaching"` + NaN-via-0 expression below 10 samples. Alarm stays quiet on idle adapters / startup periods. Acceptance: `grep -n "IF((ingested + failures) < 10, 0" terraform/monitoring.tf` returns 1 (suppression wired — D-23). HUMAN-UAT item 5 verifies the fire+recover cycle. |
| T-02-PROD-MISAPPLY | Tampering | prod terraform apply without bake | high | mitigate | `autonomous: false` on this plan + HUMAN-UAT Task 3 checkpoint + staging→prod gate section in 02-HUMAN-UAT.md requires 24h bake + all 7 items pass before prod apply. Acceptance: plan frontmatter has `autonomous: false`; UAT doc has "Staging → Prod Promotion Gate" section. |
</threat_model>

<verification>
Automated (executor can complete independently):
- `cd terraform && terraform validate` exits 0
- `cd terraform && terraform fmt -check` exits 0
- `cd terraform && terraform plan -no-color | grep -c crawler_parse_failure_composite` returns >= 1
- `grep -c "crawler-drift-runbook" .planning/codebase/CONCERNS.md` returns >= 2 (header + anchor)
- `grep -c "crawler-drift-runbook" terraform/monitoring.tf` returns 1 (description ref)
- `grep -c "^### Item " .planning/phases/02-observability/02-HUMAN-UAT.md` returns 7

Manual (operator gated — `autonomous: false`):
- `terraform -chdir=terraform apply -var-file=staging.tfvars` against staging first
- 24h staging bake observation
- 7-item HUMAN-UAT checklist items all checked pass
- `terraform -chdir=terraform apply -var-file=prod.tfvars` after approval
- Post-prod HUMAN-UAT re-run to confirm prod parity
</verification>

<success_criteria>
- OBS-03 satisfied: composite parse-failure alarm exists, wired to existing SNS topic (alarm + ok actions), description points to runbook, Phase 3 for_each TODO marker present
- Landmines 7, 8, 9, 10 all pinned: no top-level period, NaN-via-0 suppression, 1-of-1 evaluation, strict `>` threshold
- D-24 deviation (SNS→email vs wording "SNS→SES") captured in UAT deviation log
- D-27 cross-file link is live: alarm description → CONCERNS.md anchor both exist and match
- D-28 Crawler Drift Runbook exists with 7 numbered response steps + Phase 3 handoff note + SAFE-07 characterization-test reference
- D-30 Phase 3 handoff marker present in terraform (for_each conversion ready when CRAWL-01/02 land adapter_names.txt)
- D-58 staging-first bake gate codified in 02-HUMAN-UAT.md (24h + 7 items all pass before prod apply)
- D-62 7-item UAT checklist covers all five OBS requirements (OBS-01 ×3 items, OBS-02, OBS-03, OBS-04, OBS-05)
- Zero Phase 1 regressions (this plan touches only terraform + docs — no backend/frontend code; coverage/OpenAPI/auth/crawler characterization gates all preserved by construction)
- Phase 2 closes cleanly: all 5 OBS requirements addressed, all 62 decisions honored, all 18 landmines pinned across the 5 plans
</success_criteria>

<output>
After completion, create `.planning/phases/02-observability/02-05-SUMMARY.md` documenting:
- Alarm resource name + metric-math expression literal (traceability for Phase 3 conversion)
- `var.app_environment` vs substitute (whichever was used in the alarm dimensions)
- Terraform validate + fmt + plan output summary
- Crawler Drift Runbook anchor confirmation (both CONCERNS.md anchor + monitoring.tf description match)
- HUMAN-UAT checklist delivered — operator sign-off link
- `autonomous: false` step explicitly: the prod `terraform apply` is NOT executed by this plan; operator runs it after 24h staging bake per D-58
- Phase 2 closure log: list each OBS-0X → closing plan, list each D-XX → closing plan, list each Landmine 1-18 → closing plan
</output>
