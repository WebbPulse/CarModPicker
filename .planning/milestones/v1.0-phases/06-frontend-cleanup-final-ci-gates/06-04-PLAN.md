---
phase: 06-frontend-cleanup-final-ci-gates
plan: 04
type: execute
wave: 4
depends_on: []
files_modified:
  - backend/requirements.txt
  - .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md
autonomous: true
requirements:
  - QUAL-05
  - QUAL-06
requirements_addressed:
  - QUAL-05
  - QUAL-06
must_haves:
  truths:
    - "backend/requirements.txt pins fastapi==0.136.1 and pydantic==2.13.3"
    - "pydantic_core explicit pin is either removed or updated to the version Pydantic 2.13.3 resolves to"
    - "cd backend && pytest -n auto exits 0 (full suite green under FastAPI 0.136 + Pydantic 2.13)"
    - "cd backend && pytest -n auto backend/tests/test_openapi_snapshot.py exits 0 (SAFE-05 backward-compat oracle clean)"
    - "cd backend && pytest -n auto -k 'auth and characterization' exits 0 (SAFE-06 7-flow auth tests clean)"
    - "Phase 3 Pydantic v1 grep guard (test_pydantic_v1_regression.py) reports zero deprecation warnings under Pydantic 2.13"
    - "QUAL-06 extension Content-Type compliance: vitest grep guard (Plan 06-01) still passes on FastAPI 0.136"
    - "06-HUMAN-UAT.md contains a checklist item for manual chrome-extension smoke test against FastAPI 0.136 local backend"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "pinned fastapi==0.136.1 + pydantic==2.13.3 (PR-A bump)"
      contains: "fastapi=="
    - path: ".planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md"
      provides: "human-only UAT checklist for chrome extension + route-group Sentry verification"
  key_links:
    - from: "backend/requirements.txt"
      to: "backend/tests/test_openapi_snapshot.py"
      via: "SAFE-05 schema snapshot fails CI if FastAPI 0.136 route metadata drifts"
      pattern: "openapi_snapshot"
    - from: "backend/requirements.txt"
      to: "backend/tests/test_pydantic_v1_regression.py"
      via: "catch_warnings pytest guard (plan 03-05) fires on any v1 deprecation surfaced by Pydantic 2.13"
      pattern: "PydanticDeprecatedSince"
    - from: "backend/requirements.txt"
      to: "backend/tests/auth/characterization/"
      via: "SAFE-06 7-flow characterization pinned before refactor; PR-A gate"
      pattern: "auth.*characterization"
---

<objective>
Wave 4 ships PR-A of the two-PR upgrade train (D-11): FastAPI 0.128 to 0.136.1 + Pydantic 2.11 to 2.13.3. This pair has the highest coupling and highest blast radius (FastAPI 0.132 introduced strict Content-Type, Pydantic 2.13 surfaces new deprecation signals). Rides the existing guards (D-13): Phase 3 Pydantic-v1-grep + catch_warnings, Phase 1 OpenAPI snapshot, Phase 1 auth characterization. QUAL-06 extension Content-Type compliance is already locked statically by Plan 06-01's vitest grep guard; this plan adds a manual chrome-extension smoke-test checklist item to 06-HUMAN-UAT.md and runs the auth characterization suite against the upgraded backend as the gate.

Purpose: D-11 mandates separating the FastAPI + Pydantic bump from the SQLAlchemy/Alembic/Uvicorn + jose removal (Plan 06-05 / PR-B) for bisect hygiene. Landing PR-A green unblocks PR-B.
Output: requirements.txt bumped for fastapi + pydantic only; full backend test suite green; OpenAPI snapshot unchanged; auth characterization 7-flow suite passing; 06-HUMAN-UAT.md seeded with the manual extension smoke-test item.
</objective>

<execution_context>
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/workflows/execute-plan.md
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-CONTEXT.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-RESEARCH.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-PATTERNS.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-VALIDATION.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-01-wave0-infra-parallel-small-PLAN.md
@backend/requirements.txt
@backend/tests/test_pydantic_v1_regression.py
@backend/tests/test_openapi_snapshot.py
@backend/pytest.ini
@chrome-extension/src/background.ts

<interfaces>
Current `backend/requirements.txt` pins (relevant lines — executor verifies with grep at edit time):
- `fastapi==0.128.0` (or latest 0.128.x)
- `pydantic==2.11.3` (or latest 2.11.x)
- `pydantic_core==2.33.1` (explicit pin — Pydantic 2.13 ships with a newer pydantic_core; executor must either update or remove this pin)

Target pins (RESEARCH.md Standard Stack section, verified 2026-04-23 via `pip index versions`):
- `fastapi==0.136.1` — highest patch of 0.136
- `pydantic==2.13.3` — highest patch of 2.13

**pydantic_core handling:** Pydantic 2.13.x ships with an updated pydantic_core requirement. Two options:
(a) Remove the explicit `pydantic_core==2.33.1` line and let Pydantic 2.13.3 pull its pinned version transitively.
(b) Run `pip install pydantic==2.13.3` locally, then `pip show pydantic_core` to capture the new version, then update the explicit pin in requirements.txt.

Executor picks. Recommendation: option (a) — fewer manual pins is better; the one-pin-per-top-level-dep convention is healthier.

Existing guards this plan rides (D-13):
- `backend/tests/test_pydantic_v1_regression.py` (Phase 3 plan 03-05): grep for v1 patterns + `catch_warnings` pytest guard that fires on any `PydanticDeprecatedSince` warning. Pydantic 2.13 may surface new deprecations that were silent in 2.11. The guard catches them; executor fixes them inline during this plan.
- `backend/tests/test_openapi_snapshot.py` (Phase 1 SAFE-05): compares `/openapi.json` against `backend/tests/fixtures/openapi_snapshot.json`. FastAPI 0.136 may alter schema metadata (e.g., `additionalProperties: false` defaults, example generation). If the snapshot diverges the test fails; executor regenerates the snapshot ONLY if the delta is benign (e.g., added `examples` field on a response) and documents the regeneration in the SUMMARY.
- `backend/tests/auth/characterization/*` (Phase 1 SAFE-06): 7-flow auth characterization suite (signup, verify-email, login, 2FA-TOTP, WebAuthn, OAuth, password-reset). Pinned behavior gate for PR-A per D-12(b). Suite must be green before merge.

FastAPI 0.132+ strict Content-Type compatibility (QUAL-06, D-12):
- The chrome-extension POST compliance grep guard is ALREADY in place from Plan 06-01 (`frontend/src/test/extension-content-type.test.ts`). It passes on current source (`chrome-extension/src/background.ts:81-96` sets `'Content-Type': 'application/json'` in the shared `apiRequest` helper). No code changes needed here — the guard is preventive.
- Manual component: D-12(b) requires loading the unpacked extension against a local FastAPI 0.136 backend and exercising login + at least one crawl endpoint to confirm no 400 "Content-Type" errors surface. This is a human-gated smoke test, not automatable (VALIDATION.md Manual-Only Verifications).

Diagnostic escape hatch (RESEARCH.md): if pytest xdist output is misleading (interleaved failures), re-run with `pytest -p no:xdist` to bisect. The Pydantic v1 grep guard is xdist-safe per plan 03-05 lines 49-74 pattern.

OpenAPI snapshot regeneration procedure (only if FastAPI 0.136 introduces benign schema additions):
```bash
cd backend
# Diff old vs new
curl -s http://localhost:8000/openapi.json > /tmp/new-openapi.json
diff tests/fixtures/openapi_snapshot.json /tmp/new-openapi.json
# If diff is only additive (new properties, new examples, no breaking changes):
cp /tmp/new-openapi.json tests/fixtures/openapi_snapshot.json
```
Document any regeneration in SUMMARY with the exact diff.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump fastapi + pydantic in requirements.txt; reinstall; run full test gauntlet</name>
  <read_first>
    - backend/requirements.txt (full file — identify current fastapi, pydantic, pydantic_core lines)
    - .planning/phases/06-frontend-cleanup-final-ci-gates/06-RESEARCH.md Standard Stack (verified version pins)
    - .planning/phases/06-frontend-cleanup-final-ci-gates/06-PATTERNS.md Wave 4 requirements.txt section
    - backend/tests/test_pydantic_v1_regression.py (guard this task's upgrade rides)
    - backend/tests/test_openapi_snapshot.py (second guard)
    - backend/pytest.ini (coverage floor 51; xdist auto; rate limiting disabled in tests)
  </read_first>
  <behavior>
    - backend/requirements.txt has `fastapi==0.136.1`
    - backend/requirements.txt has `pydantic==2.13.3`
    - pydantic_core pin is either removed OR updated to whatever Pydantic 2.13.3 resolves to (executor picks; removal is cleaner)
    - cd backend + pip install -r requirements.txt completes without dependency-resolution errors
    - cd backend + pytest -n auto exits 0 with no new deprecation warnings caught by test_pydantic_v1_regression.py
    - cd backend + pytest -n auto backend/tests/test_openapi_snapshot.py exits 0 (OR snapshot is regenerated after reviewable diff inspection)
    - cd backend + pytest -n auto -k "auth and characterization" exits 0 (7-flow suite green)
    - No other dependency in requirements.txt is modified (sqlalchemy/alembic/uvicorn stay pinned at current versions — Plan 06-05 owns those)
    - `python-jose[cryptography]==3.5.0` STAYS in requirements.txt for this plan (Plan 06-05 removes it per D-14 / D-11)
  </behavior>
  <action>
STEP 1 — Read `backend/requirements.txt`. Identify the exact current pinned versions for fastapi, pydantic, pydantic_core. Record them in memory for the commit message.

STEP 2 — Apply the bumps. Keep exact pin syntax (`==`) preserved:

```
fastapi==0.136.1       # was: fastapi==0.128.0 (or current)
pydantic==2.13.3       # was: pydantic==2.11.3 (or current)
# pydantic_core: pick option A or B
# Option A (recommended): DELETE the `pydantic_core==2.33.1` line entirely
# Option B: update to whatever `pip show pydantic_core` reports after install
```

DO NOT edit any other line in requirements.txt. Specifically:
- `sqlalchemy==2.0.41` STAYS (Plan 06-05)
- `alembic==1.16.x` STAYS (Plan 06-05)
- `uvicorn==0.34.x` STAYS (Plan 06-05)
- `python-jose[cryptography]==3.5.0` STAYS (Plan 06-05 per D-14)

STEP 3 — Install:

```bash
cd backend
pip install -r requirements.txt
pip show fastapi pydantic pydantic_core   # confirm resolved versions
```

If option B was picked and pydantic_core resolved to a NEW version, update the pin in requirements.txt to match and re-install to verify clean.

STEP 4 — Run the guards in order, fastest first (diagnostic funnel):

```bash
cd backend

# 4a: Pydantic v1 deprecation guard (Phase 3 plan 03-05)
pytest -n auto -x backend/tests/test_pydantic_v1_regression.py

# 4b: OpenAPI snapshot (Phase 1 SAFE-05)
pytest -n auto -x backend/tests/test_openapi_snapshot.py

# 4c: Auth characterization (Phase 1 SAFE-06)
pytest -n auto -k "auth and characterization"

# 4d: Full suite
pytest -n auto --cov=app --cov-report=term-missing
```

If 4a fails: a new Pydantic 2.13 deprecation surfaced. Read the failure's warning message, locate the offending file (usually a backend/app/api/schemas/*.py or model file), migrate to the v2 equivalent (see CONVENTIONS.md + Phase 3 plan 03-05 SUMMARY for the canonical migration list). Re-run 4a until green. DO NOT weaken the test.

If 4b fails: inspect the snapshot diff. If the only changes are benign additions (new examples, new response schemas for deep-nested models), regenerate the snapshot and commit the regeneration in a SEPARATE commit from the version bump, with a clear message referencing D-13 guard regeneration. If the diff contains breaking changes (removed routes, changed status codes, altered required fields), STOP — this is a regression, not a benign drift. Document and escalate.

If 4c fails: the FastAPI 0.136 auth flow broke. This is THE most critical signal; QUAL-06 extension compatibility hinges on auth. Read the failure, fix the underlying issue (likely a middleware ordering or Content-Type-related issue), commit the fix, re-run until green. DO NOT disable any characterization test.

If 4d fails on anything not caught by 4a/4b/4c: fix the underlying issue. This plan does not merge until 4d is green.

STEP 5 — Diagnostic escape hatch: If xdist output is hard to read, re-run with `pytest -p no:xdist -x` to serialize output and bisect. Never merge an xdist-only green — always confirm under plain pytest at least once.

STEP 6 — Commit. Single commit for the version bumps (and pydantic_core removal/update if applicable). If snapshot regeneration was needed, SEPARATE commit per STEP 4b guidance.
  </action>
  <verify>
    <automated>grep -q "^fastapi==0.136" backend/requirements.txt && grep -q "^pydantic==2.13" backend/requirements.txt && cd backend && pytest -n auto -x tests/test_pydantic_v1_regression.py tests/test_openapi_snapshot.py && pytest -n auto -k "auth and characterization"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q '^fastapi==0.136' backend/requirements.txt exits 0
    - grep -q '^pydantic==2.13' backend/requirements.txt exits 0
    - grep -q '^sqlalchemy==2.0.41' backend/requirements.txt still exits 0 (NOT touched by this plan — Plan 06-05 owns)
    - grep -q '^python-jose' backend/requirements.txt still exits 0 (NOT touched by this plan — Plan 06-05 owns)
    - cd backend && python -c "import fastapi; print(fastapi.__version__)" prints a version starting with 0.136
    - cd backend && python -c "import pydantic; print(pydantic.VERSION)" prints a version starting with 2.13
    - cd backend && pytest -n auto -x tests/test_pydantic_v1_regression.py exits 0
    - cd backend && pytest -n auto -x tests/test_openapi_snapshot.py exits 0
    - cd backend && pytest -n auto -k "auth and characterization" exits 0
    - cd backend && pytest -n auto --cov=app --cov-report=term-missing exits 0 (full suite green under upgraded stack)
  </acceptance_criteria>
  <done>fastapi==0.136.1 and pydantic==2.13.3 pinned; full test suite green; OpenAPI snapshot unchanged (or regenerated with benign diff documented); auth characterization 7-flow suite green; pydantic_core pin resolved cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Seed 06-HUMAN-UAT.md with QUAL-06 extension smoke test + route-group Sentry manual items</name>
  <read_first>
    - .planning/phases/05-structural-router-splits/ for any existing 05-HUMAN-UAT.md checklist pattern (template source)
    - .planning/phases/06-frontend-cleanup-final-ci-gates/06-VALIDATION.md Manual-Only Verifications section
    - chrome-extension/src/background.ts (confirm the apiRequest helper still sets Content-Type; sanity check)
    - .planning/phases/06-frontend-cleanup-final-ci-gates/06-CONTEXT.md D-12 (extension audit strategy)
  </read_first>
  <behavior>
    - File .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md exists
    - Contains checklist items for: (a) QUAL-06 chrome extension smoke test against local FastAPI 0.136 backend, (b) Sentry route-group tag verification in staging (tied to FE-03 / OBS-05, surfaced in VALIDATION.md Manual-Only Verifications), (c) Terraform QUAL-08 apply confirmation (inherited from Plan 06-01), (d) parts-catalog polish visual acceptance (forward reference to Plan 06-06)
    - Each item specifies: scope, prerequisites, step-by-step instructions, expected result, sign-off line
    - Markdown format; no frontmatter required
  </behavior>
  <action>
Create `.planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md` with this content:

```markdown
# Phase 6 — Human UAT Checklist

Manual verifications required for Phase 6 completion. Each item is gated by a human; no CI substitute is available per VALIDATION.md Manual-Only Verifications.

## 1. Chrome extension smoke test against FastAPI 0.136 (QUAL-06 / D-12b)

**Scope:** Prove the extension still works end-to-end after the FastAPI 0.128 -> 0.136.1 upgrade. FastAPI 0.132 introduced strict Content-Type checking; the vitest grep guard (Plan 06-01) proves extension source already sets `Content-Type: application/json` in the shared `apiRequest` helper, but this manual step confirms real runtime behavior.

**Prerequisites:**
- Local backend running against FastAPI 0.136.1 (`cd backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000`)
- Local docker-compose Postgres up (`cd backend && docker-compose up -d`)
- Chrome extension built: `cd chrome-extension && npm run build`
- Chrome extension loaded unpacked from `chrome-extension/dist` at `chrome://extensions/`

**Steps:**
1. Open the extension popup. Verify it loads without a console error (F12 → Console; inspect the popup).
2. Click "Log in" in the popup. Complete the login flow against the local backend.
3. After login, visit a supported retailer product page (e.g., any page handled by a tier0_http adapter).
4. Trigger the extension's scrape + POST to the backend.
5. Open the extension's background console (chrome://extensions → service worker "Inspect"). Confirm NO 400/415 "Content-Type" errors from the backend.
6. Verify the new part appears in the local backend DB (`psql` or via the frontend `/my-parts` page).

**Expected result:** Login + scrape-and-save flow round-trips successfully. No HTTP 400 or 415 errors in extension logs. No 400 "Content-Type" errors in backend logs (`uvicorn` terminal).

**Sign-off:** _____________________ (date: ____________)

***

## 2. Sentry route-group tag verification in staging (FE-03 / OBS-05)

**Scope:** Confirm that an exception thrown in each of the 4 route groups (admin, authentication, builder, public) produces a Sentry event tagged with the correct `route_group`. Automated test (`App.coverage.test.tsx`) verifies the wrapper structure; this item verifies the downstream Sentry integration under real conditions.

**Prerequisites:**
- Phase 6 deployed to staging (App Runner staging environment).
- Sentry project access for staging.
- A way to trigger a component throw per group (one option: add a temporary `?crash=1` query-param debug toggle to one page per group for the test, then revert; document in PR if used).

**Steps:**
1. Visit staging URL for each group's flagship page:
   - public: `/`
   - authentication: `/login`
   - builder: `/profile` (requires logged-in test user)
   - admin: `/admin` (requires admin test user)
2. Trigger a render-time throw in each (temporary toggle or known broken page).
3. Verify the RouteGroupBoundary fallback UI renders: "Something went wrong in the <group> section" heading + Event ID + Retry / Go Home buttons.
4. Open the Sentry staging project. Confirm 4 distinct events arrived, each tagged `route_group=<name>` matching the group where the throw occurred.
5. Click "Retry" on one fallback; confirm the UI recovers once the trigger is removed.
6. Click "Go Home" on another fallback; confirm navigation to `/`.

**Expected result:** 4 Sentry events with correct `route_group` tag; Retry resets the boundary; Go Home navigates to root.

**Sign-off:** _____________________ (date: ____________)

***

## 3. Terraform QUAL-08 apply confirmation (inherited from Plan 06-01)

**Scope:** Operator confirms the Glacier Deep Archive lifecycle rule landed in AWS state and takes effect on the correct bucket.

**Prerequisites:**
- Plan 06-01 merged (terraform/s3.tf contains `aws_s3_bucket_lifecycle_configuration.crawl_data`).
- AWS SSO credentials for the prod account.

**Steps:**
1. `cd terraform && terraform plan -target=aws_s3_bucket_lifecycle_configuration.crawl_data -no-color`
2. Confirm the plan shows "1 resource to add" (on first apply) or "No changes" (after apply).
3. `terraform apply -target=aws_s3_bucket_lifecycle_configuration.crawl_data`
4. Verify in AWS console: S3 → `carmodpicker-production-crawl-data` → Management → Lifecycle rules → rule `archive-old-snapshots` is Enabled, transitioning to Deep Archive at 90 days.
5. Confirm `carmodpicker-prod-user-images` has NO lifecycle rule (D-19).

**Expected result:** Exactly one lifecycle rule on `carmodpicker-production-crawl-data`; user-images bucket untouched.

**Sign-off:** _____________________ (date: ____________)

***

## 4. Parts-catalog polish visual acceptance (FE-07 — forward reference to Plan 06-06)

Placeholder — will be populated by Plan 06-06. Parts-catalog polish checklist (Card variants, spacing, typography, responsive grid) drafted by the planner, approved here by the operator.

**Sign-off:** _____________________ (date: ____________)

***

## Summary sign-off

All four items signed -> Phase 6 manual gates closed. Record the date of final sign-off in the phase SUMMARY.

Phase 6 manual verification complete: _____________________ (date: ____________)
```

No other files touched in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md && grep -q "QUAL-06" .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md && grep -q "FastAPI 0.136" .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md && grep -q "route_group" .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md && grep -q "Deep Archive" .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - test -f .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md exits 0
    - grep -q 'QUAL-06' .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md exits 0
    - grep -q 'FastAPI 0.136' .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md exits 0
    - grep -q 'route_group' .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md exits 0
    - grep -q 'carmodpicker-production-crawl-data' .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md exits 0
    - grep -cE '^## [0-9]\.' .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md returns at least 4 (four numbered sections)
  </acceptance_criteria>
  <done>06-HUMAN-UAT.md drafted with 4 manual checklist items (extension smoke test, Sentry route-group tags, Terraform apply, parts-catalog polish placeholder).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip index -> backend runtime | Pydantic 2.13 / FastAPI 0.136 are freshly installed; their advisories + release notes are the threat surface. |
| Chrome extension -> FastAPI | FastAPI 0.132 strict Content-Type is the known regression vector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Tampering | Pydantic v1 -> v2 deprecation creep | mitigate | Phase 3 plan 03-05 guard (test_pydantic_v1_regression.py + catch_warnings) fires on any new PydanticDeprecatedSince warning under 2.13; executor fixes inline. |
| T-06-17 | Tampering | FastAPI route metadata drift | mitigate | Phase 1 SAFE-05 OpenAPI snapshot test (test_openapi_snapshot.py) flags any schema drift from 0.128 -> 0.136. Regeneration requires a separate, reviewer-inspectable diff commit. |
| T-06-18 | Denial of Service | Chrome extension POST -> FastAPI 0.132+ strict Content-Type | mitigate | Plan 06-01 vitest grep guard (extension-content-type.test.ts) already enforces the invariant statically. Human UAT item 1 verifies runtime behavior end-to-end. |
| T-06-19 | Elevation of Privilege | Auth flow regression under FastAPI 0.136 | mitigate | Phase 1 SAFE-06 7-flow characterization suite (signup, verify-email, login, 2FA-TOTP, WebAuthn, OAuth, password-reset) gates PR-A merge per D-12b. |
| T-06-20 | Spoofing | pydantic_core pin drift | accept | Removing the explicit pin (option A) accepts transitive resolution — safe because Pydantic itself pins pydantic_core precisely per release. |
</threat_model>

<verification>
Before merging Plan 06-04:
1. cd backend && pytest -n auto --cov=app --cov-report=term-missing — exit 0
2. cd backend && pytest -n auto -x tests/test_pydantic_v1_regression.py — exit 0
3. cd backend && pytest -n auto -x tests/test_openapi_snapshot.py — exit 0 (or snapshot regenerated with documented benign diff in separate commit)
4. cd backend && pytest -n auto -k "auth and characterization" — exit 0
5. grep -q '^fastapi==0.136' backend/requirements.txt — exit 0
6. grep -q '^pydantic==2.13' backend/requirements.txt — exit 0
7. grep -q '^sqlalchemy==2.0.41' backend/requirements.txt — still exit 0 (Plan 06-05 owns this change)
8. grep -q '^python-jose' backend/requirements.txt — still exit 0 (Plan 06-05 owns this change per D-14)
9. .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md exists with 4 numbered sections
10. Frontend extension-content-type vitest guard (Plan 06-01) still green under FastAPI 0.136 by static analysis (no source changes needed)
</verification>

<success_criteria>
- QUAL-05 PR-A complete: FastAPI 0.136.1 + Pydantic 2.13.3 pinned; full test suite green
- QUAL-06 extension compliance statically locked (Plan 06-01 guard unchanged); manual UAT item drafted for runtime verification
- OpenAPI snapshot stable (or regenerated with documented benign diff)
- Auth characterization 7-flow suite green under FastAPI 0.136
- No Pydantic v1 deprecation warnings surfaced under 2.13 (or surfaced + fixed inline)
- SQLAlchemy / Alembic / Uvicorn / python-jose UNCHANGED (Plan 06-05 handles those per D-11)
- All acceptance_criteria on both tasks pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-cleanup-final-ci-gates/06-04-SUMMARY.md` per template. SUMMARY must record: (a) which pydantic_core option was chosen (removal vs pinned-update), (b) whether OpenAPI snapshot required regeneration (if so, include the diff summary), (c) any inline Pydantic v2 deprecation fixes applied, (d) confirmation that python-jose and sqlalchemy/alembic/uvicorn pins were NOT touched.
</output>
