---
phase: 06-frontend-cleanup-final-ci-gates
plan: 06
type: execute
wave: 6
depends_on:
  - 02
  - 03
  - 04
  - 05
files_modified:
  - frontend/src/pages/parts/PartsCatalog.tsx
  - frontend/src/pages/parts/ViewPart.tsx
  - frontend/src/pages/parts/EditPart.tsx
  - frontend/src/pages/parts/UserParts.tsx
  - frontend/src/components/parts/PartList.tsx
  - frontend/src/components/parts/PartCard.tsx
  - frontend/src/components/parts/CreatePartForm.tsx
  - frontend/src/components/parts/EditPartForm.tsx
  - frontend/src/components/parts/AddToBuildListDialog.tsx
  - frontend/src/components/parts/ImageGalleryManage.tsx
  - .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md
autonomous: false
requirements:
  - FE-07
requirements_addressed:
  - FE-07
must_haves:
  truths:
    - "Every page/component touched by Plans 06-01 through 06-05 was reviewed for opportunistic polish opportunities; polish applied only where it follows existing patterns (Tailwind class cleanup, Card.tsx variant alignment, consistent spacing)"
    - "A bounded parts-catalog polish pass has been applied to frontend/src/pages/parts/* and frontend/src/components/parts/* ONLY — no pages outside these two directories modified"
    - "The polish checklist is committed to 06-HUMAN-UAT.md as an expanded Section 4, replacing the Plan 06-04 placeholder"
    - "No information-architecture changes: no route changes, no navigation restructure, no component removed or combined/split, no behavior change"
    - "Final sweep green: cd frontend && npm run lint && npm test -- --run && npm run build && npx madge --circular --extensions ts,tsx src/"
    - "A human UAT sign-off line in 06-HUMAN-UAT.md is ready for the operator to tick"
  artifacts:
    - path: "frontend/src/pages/parts/*"
      provides: "polish-pass-applied parts catalog pages (PartsCatalog, ViewPart, EditPart, UserParts)"
    - path: "frontend/src/components/parts/*"
      provides: "polish-pass-applied parts components (PartList, PartCard, CreatePartForm, EditPartForm, AddToBuildListDialog, ImageGalleryManage)"
    - path: ".planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md"
      provides: "expanded Section 4 with parts-catalog polish checklist and sign-off"
  key_links:
    - from: "frontend/src/components/parts/PartCard.tsx (and similar)"
      to: "frontend/src/components/common/Card.tsx"
      via: "shared shadow/rounded variant vocabulary"
      pattern: "shadow-|rounded-|glass-card"
---

<objective>
Wave 6 closes FE-07 with a two-part polish pass: (a) opportunistic polish on every page/component touched by Plans 06-01 through 06-05, bounded to those files only (D-17); (b) one targeted bounded polish pass on the parts catalog — `frontend/src/pages/parts/*` + `frontend/src/components/parts/*` ONLY, with a checklist drafted here and UAT-approved via 06-HUMAN-UAT.md. Per D-17 and CONTEXT.md Specifics, there is no redesign of information architecture, no pages outside those two directories get touched beyond what earlier plans changed, and no existing component is restructured (combined/split).

Purpose: Every prior plan in this phase has a reason to adjust a page's class set in passing (e.g., Plan 06-02's FE-04 split rewrote 56 import sites — those files' visible surfaces were touched in the process). FE-07 collects opportunistic polish that naturally travels with structural work, plus lands the single dedicated visual polish pass for the parts catalog this milestone allows.
Output: polished parts-catalog pages + components; opportunistic polish commits on touched files; expanded 06-HUMAN-UAT.md section 4 with checklist + operator sign-off.
</objective>

<execution_context>
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/workflows/execute-plan.md
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-CONTEXT.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-RESEARCH.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-PATTERNS.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-VALIDATION.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-02-PLAN.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-03-PLAN.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-04-PLAN.md
@.planning/phases/06-frontend-cleanup-final-ci-gates/06-05-PLAN.md
@frontend/src/components/common/Card.tsx
@frontend/src/components/common/Button.tsx
@frontend/src/pages/Home.tsx

<interfaces>
Parts-catalog scope (bounded per D-17, REQUIREMENTS.md FE-07):
- `frontend/src/pages/parts/PartsCatalog.tsx` — public catalog list view
- `frontend/src/pages/parts/ViewPart.tsx` — individual part detail view
- `frontend/src/pages/parts/EditPart.tsx` — part edit page
- `frontend/src/pages/parts/UserParts.tsx` — authenticated user's parts list
- `frontend/src/components/parts/PartList.tsx` — shared part-list rendering
- `frontend/src/components/parts/PartCard.tsx` (or whatever the catalog item card is named — grep `components/parts/` at edit time)
- `frontend/src/components/parts/CreatePartForm.tsx`
- `frontend/src/components/parts/EditPartForm.tsx`
- `frontend/src/components/parts/AddToBuildListDialog.tsx`
- `frontend/src/components/parts/ImageGalleryManage.tsx`
- Any other file under `frontend/src/components/parts/` discovered via `ls` at edit time

Style reference patterns:
- `frontend/src/components/common/Card.tsx` — canonical shadow/rounded/padding variants. The polish pass aligns parts-catalog cards to match Card.tsx's existing vocabulary rather than introducing new variants.
- `frontend/src/pages/Home.tsx` — typography hierarchy (text-3xl landing -> text-2xl section -> text-xl card).
- `frontend/src/components/common/Button.tsx` — btn-primary / btn-secondary / btn-outline global class usage.

Touched-file opportunistic scope (D-17): files modified by Plans 06-01 through 06-05 (per their `files_modified` frontmatter). Plan 06-01 touched ~30 files (gradient rename sites + CI + tests). Plan 06-02 touched ~20 files (utils/lazyWithReload + api/*.ts + services/Api.ts shim). Plan 06-03 touched 4 files (RouteGroupBoundary + App.tsx + App.coverage.test). Plans 06-04/05 touched only backend files. Net opportunistic scope for FE-07 polish: the gradient-renamed files + App.tsx + any newly-touched component.

Opportunistic polish allowed (D-17):
- Tailwind class cleanup: dedupe redundant classes, enforce design-token consistency (spacing scale: p-2 / p-4 / p-6 / p-8; never p-3 / p-5 unless there is an existing design-system precedent).
- Alignment with Card.tsx existing shadow/rounded variants on any part-card or page-card element.
- Consistent button sizes (btn-primary / btn-secondary / btn-outline — do NOT invent new variants).
- Responsive grid consistency on PartsCatalog (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` pattern — match other catalog pages if applicable).
- Typography hierarchy: page titles text-3xl, section headings text-2xl, card headings text-xl (match Home.tsx scale).

Opportunistic polish FORBIDDEN (D-17):
- Adding any new page, route, component, or navigation entry.
- Removing or combining any existing page, component, or flow.
- Changing information architecture (how parts are grouped, filtered, or displayed in terms of data shape).
- Touching any page outside `pages/parts/*` + `components/parts/*` + files already-touched by Plans 06-01 through 06-05.
- Adding animations, transitions, or interactivity not already present.
- Changing copy (microcopy, labels, error messages) — out of scope for visual polish.
- Adding dependencies (no new icon packs, no new utility libraries).

Final sweep gate (before UAT sign-off):
```bash
cd frontend
npm run lint
npm test -- --run
npm run build
npx madge --circular --extensions ts,tsx src/
```
All must exit 0. Any failure halts the task before 06-HUMAN-UAT.md is updated.

autonomous: false — this plan has a human UAT gate (the expanded Section 4 of 06-HUMAN-UAT.md). Executor completes the polish pass automatically, then pauses at the checkpoint for operator sign-off.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Opportunistic polish on touched files from Plans 06-01 through 06-05</name>
  <read_first>
    - .planning/phases/06-frontend-cleanup-final-ci-gates/06-01-wave0-infra-parallel-small-PLAN.md frontmatter files_modified (~30 files)
    - .planning/phases/06-frontend-cleanup-final-ci-gates/06-02-PLAN.md frontmatter files_modified
    - .planning/phases/06-frontend-cleanup-final-ci-gates/06-03-PLAN.md frontmatter files_modified
    - frontend/src/components/common/Card.tsx (canonical shadow/rounded vocabulary)
    - frontend/src/components/common/Button.tsx (canonical button variants)
    - frontend/src/pages/Home.tsx (canonical typography scale)
  </read_first>
  <behavior>
    - Every touched file from Plans 06-01 through 06-05 (excluding backend-only plans 06-04/05) has been reviewed for opportunistic polish opportunities
    - Where obvious inconsistencies exist with Card.tsx / Button.tsx / Home.tsx patterns, they have been aligned
    - Where no inconsistency exists, the file is left untouched — polish is opportunistic, not mandatory
    - No information-architecture changes
    - cd frontend && npm run lint exits 0
    - cd frontend && npm test -- --run exits 0
    - cd frontend && npm run build exits 0
  </behavior>
  <action>
STEP 1 — Enumerate the touched-file set. Read each prior plan's frontmatter `files_modified` list:
- Plan 06-01: ~30 files (mostly gradient-rename sites — App.tsx, Card.tsx, Button.tsx, layout, multiple pages)
- Plan 06-02: `frontend/src/utils/lazyWithReload.ts`, `frontend/src/api/*.ts` (new), `frontend/src/services/Api.ts` (shim), any FE-01 fix files
- Plan 06-03: `frontend/src/components/common/RouteGroupBoundary.tsx`, `.../RouteGroupBoundary.test.tsx`, `frontend/src/App.tsx`, `frontend/src/App.coverage.test.tsx`
- Plans 06-04/05: backend-only — skip

Deduplicate into one sorted list. Expected surface: ~35 unique frontend files.

STEP 2 — For each file in the touched set, open it and apply this three-part review checklist:

**(a) Tailwind class dedupe + token consistency:**
```typescript
// CHECK: redundant classes (rare but possible — Tailwind merges inconsistently when classes conflict)
// CHECK: spacing scale (prefer p-2 / p-4 / p-6 / p-8 over p-3 / p-5 unless a DS precedent exists)
// CHECK: color token consistency (neutral-XXX shades for surfaces; blue/purple/red for accents matching existing sites)
```

**(b) Card.tsx variant alignment:**
```typescript
// CHECK: any component that renders a card-shaped surface (bg-neutral-800, rounded, shadow)
//        uses the same shadow (shadow-md / shadow-lg) and rounded (rounded-xl / rounded-2xl) variants as Card.tsx
// If Card.tsx component itself is available, USE IT rather than hand-rolling a card shape.
// NOTE: do NOT force use of the <Card> component — only align class choices when a hand-rolled surface already exists.
```

**(c) Typography hierarchy:**
```typescript
// CHECK: page-level headings (h1) use text-3xl or larger
// CHECK: section headings (h2) use text-2xl
// CHECK: card/item headings (h3) use text-xl
// If a file uses text-lg for a page title or text-4xl for a card heading, align to the Home.tsx scale.
```

STEP 3 — Apply polish ONLY where the file currently violates one of the three checklist patterns. If a file is already consistent, COMMIT NO CHANGES to it.

Examples of acceptable polish (small, mechanical):
- `className="p-3"` inside a card that uses `p-4` elsewhere -> `className="p-4"`
- `className="rounded-md shadow"` on a card-like surface -> `className="rounded-xl shadow-md"` (align to Card.tsx)
- `className="text-lg"` on an `<h1>` page title -> `className="text-3xl"` (align to Home.tsx scale)

Examples of UNACCEPTABLE polish (reject if tempted):
- Renaming a component
- Moving markup into a different file
- Adding a new Tailwind utility or custom CSS
- Changing copy, ARIA labels, or semantic tags
- Adding transitions/animations

STEP 4 — Validate after each file (or after small batches — executor picks) by running:
```bash
cd frontend
npm run lint -- path/to/file.tsx   # may only lint specific files; full lint at end of task
npm run type-check
```

STEP 5 — Final sweep for Task 1:
```bash
cd frontend
npm run lint
npm test -- --run
npm run build
```

All must exit 0. Commit the opportunistic polish as ONE OR MORE commits (executor picks — one per logical group is fine, e.g., `refactor(06-06): align Card shadows across touched files`).

If Task 1 finds that NO touched file needs polish (the codebase is already internally consistent), that's a valid outcome — commit nothing in Task 1, and the SUMMARY documents "no opportunistic polish required — touched files were already consistent."
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test -- --run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - cd frontend && npm run lint exits 0
    - cd frontend && npm test -- --run exits 0
    - cd frontend && npm run build exits 0
    - cd frontend && npx madge --circular --extensions ts,tsx src/ exits 0
    - No file outside the touched-file set from Plans 06-01 through 06-05 has been modified by this task
    - git log since Plan 06-05 HEAD shows only opportunistic polish commits (or zero commits if no polish was needed) in Task 1
  </acceptance_criteria>
  <done>Opportunistic polish pass complete; touched files from prior plans reviewed; polish applied only where consistency requires it; lint/test/build/madge all green.</done>
</task>

<task type="auto">
  <name>Task 2: Bounded parts-catalog polish pass — pages/parts/* + components/parts/* ONLY</name>
  <read_first>
    - frontend/src/pages/parts/ (ls — enumerate all files)
    - frontend/src/components/parts/ (ls — enumerate all files)
    - frontend/src/components/common/Card.tsx (canonical card vocabulary)
    - frontend/src/pages/Home.tsx (canonical landing-page typography + grid)
    - .planning/phases/06-frontend-cleanup-final-ci-gates/06-CONTEXT.md D-17 (polish scope)
  </read_first>
  <behavior>
    - Every file under frontend/src/pages/parts/ and frontend/src/components/parts/ has been reviewed against the polish checklist (below)
    - Polish applied where a checklist item is violated; no file touched if already compliant
    - No file outside these two directories modified by this task
    - Checklist committed to 06-HUMAN-UAT.md Section 4 (expanding the Plan 06-04 placeholder)
    - Final sweep green: lint + tests + build + madge all exit 0
  </behavior>
  <action>
STEP 1 — Enumerate the scope:
```bash
ls frontend/src/pages/parts/
ls frontend/src/components/parts/
```

Expected files under pages/parts/:
- PartsCatalog.tsx (public catalog)
- ViewPart.tsx (individual part view)
- EditPart.tsx (part edit)
- UserParts.tsx (user's own parts)

Expected files under components/parts/:
- PartList.tsx
- PartCard.tsx (or similar — grep at edit time)
- CreatePartForm.tsx
- EditPartForm.tsx
- AddToBuildListDialog.tsx
- ImageGalleryManage.tsx
- (any other files present)

If files are discovered that aren't in the frontmatter `files_modified` list, add them to the list at end of task via a plan-frontmatter edit. DO NOT modify files outside these two directories.

STEP 2 — Apply the bounded polish checklist (D-17). For each file:

**(a) Spacing scale consistency:**
- Use `p-2 / p-4 / p-6 / p-8` (multiples of 0.5rem) for padding
- Use `gap-2 / gap-4 / gap-6` for flex/grid gaps
- Use `mt-/mb-/ml-/mr-` with the same scale
- Avoid `p-3 / p-5 / p-7` unless matching an existing common-component precedent (Card.tsx already uses `p-6` — align to it)

**(b) Typography hierarchy:**
- PartsCatalog page title: `text-3xl` (match Home.tsx landing scale)
- Section headings inside a part page: `text-2xl`
- Part card titles: `text-xl`
- Body copy: `text-sm text-neutral-400` for metadata, `text-base text-neutral-200` for description
- Avoid `text-lg` on headings — it reads as body in the current scale

**(c) Card.tsx shadow/rounded variants:**
- Any hand-rolled "card" surface (not using the <Card> component directly) should use `rounded-xl shadow-md` matching Card.tsx
- Do NOT replace hand-rolled surfaces with the `<Card>` component wholesale — that is structural refactor, not polish
- Backgrounds: `bg-neutral-800/50` for surfaces; `hover:bg-neutral-800/70` for hover affordance

**(d) Button consistency:**
- Primary CTA: `btn-primary` (global class, defined in index.css)
- Secondary: `btn-secondary`
- Outline / tertiary: `btn-outline` (if exists) or plain dark variant
- Do NOT introduce custom-class buttons

**(e) Responsive grid on PartsCatalog:**
- Currently: inspect the grid classes at edit time. Target pattern (match other catalog pages):
  ```tsx
  <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
  ```
- If the grid uses a different breakpoint set (e.g., `md:grid-cols-2 xl:grid-cols-4`), leave it — only align if obviously inconsistent.

**(f) Link-card affordance (ViewPart, PartCard):**
- Hover state: `hover:bg-neutral-800/70` or equivalent one-shade-lighter
- Cursor: `cursor-pointer` on clickable cards
- Focus ring: rely on global focus-visible styles; do NOT add custom focus classes

STEP 3 — Apply fixes where violations exist. DO NOT touch files that already comply. Commit each logical group separately (e.g., `refactor(06-06): align parts-catalog typography to Home scale`, `refactor(06-06): align parts-catalog card shadows to Card.tsx`).

STEP 4 — Final sweep:
```bash
cd frontend
npm run lint
npm test -- --run
npm run build
npx madge --circular --extensions ts,tsx src/
```

All must exit 0. Any failure halts before Task 3's UAT update.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm test -- --run && npm run build && npx madge --circular --extensions ts,tsx src/</automated>
  </verify>
  <acceptance_criteria>
    - cd frontend && npm run lint exits 0
    - cd frontend && npm test -- --run exits 0
    - cd frontend && npm run build exits 0
    - cd frontend && npx madge --circular --extensions ts,tsx src/ exits 0
    - No file outside frontend/src/pages/parts/ and frontend/src/components/parts/ has been modified by this task (opportunistic-only scope preserved)
    - At least one of the following patterns appears in changes (indicating polish was applied OR confirmation of no-change): (a) `text-3xl` on PartsCatalog page title; (b) `rounded-xl` or `shadow-md` alignment on a part card; (c) spacing scale normalization (p-3 -> p-4, etc.); OR (d) the SUMMARY documents zero polish changes with justification
  </acceptance_criteria>
  <done>Parts-catalog polish pass complete; files under pages/parts/* and components/parts/* reviewed and polished per checklist; final sweep green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Update 06-HUMAN-UAT.md Section 4 + pause for operator sign-off on parts-catalog polish</name>
  <read_first>
    - .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md (Plan 06-04 seeded; Section 4 currently placeholder)
    - Git log since Plan 06-05 HEAD (shows what polish Tasks 1 and 2 actually applied — informs checklist wording)
    - frontend/src/pages/parts/ + frontend/src/components/parts/ (final post-polish state)
  </read_first>
  <action>
STEP 1 — Edit `.planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md`. Replace the Plan 06-04 Section 4 placeholder ("Placeholder — will be populated by Plan 06-06...") with this expanded section:

```markdown
## 4. Parts-catalog polish visual acceptance (FE-07 / D-17)

**Scope:** Plan 06-06 applied a bounded visual polish pass to `frontend/src/pages/parts/*` + `frontend/src/components/parts/*`. This item verifies the polish reads correctly in a real browser and no regression occurred.

**Prerequisites:**
- Phase 6 Plans 06-01 through 06-06 merged to staging OR running locally via `npm run dev`.
- Logged-in test user with at least 3 parts saved.

**Steps:**
1. Visit `/parts` (public catalog). Verify:
   - [ ] Page title uses `text-3xl` size (matches Home.tsx landing scale).
   - [ ] Responsive grid: 1 column on mobile, 2 on tablet, 3 on desktop.
   - [ ] Part cards use consistent shadow/rounded corners (matching `Card.tsx` vocabulary).
   - [ ] Spacing between cards is consistent (`gap-4` or similar).
   - [ ] No visual regression vs. pre-Phase-6 baseline (spot-check 5 random cards).
2. Click into any part -> `/parts/:partId`. Verify:
   - [ ] Section headings use `text-2xl` for the part title.
   - [ ] Body/metadata text is readable (text-sm text-neutral-400 for metadata, text-base for description).
   - [ ] No layout shift or overflow on mobile viewport (DevTools -> Responsive Mode -> iPhone 14 Pro).
3. Visit `/my-parts`. Verify:
   - [ ] User's parts render with the same card pattern as PartsCatalog.
   - [ ] Create / Edit / Delete affordances are clearly identifiable.
4. Click "Edit" on one part -> `/parts/:partId/edit`. Verify:
   - [ ] Form fields use consistent spacing (no visual squeeze or overflow).
   - [ ] Primary button is `btn-primary` (correct color, weight).
   - [ ] Secondary button is `btn-secondary`.
5. Trigger "Add to Build List" dialog. Verify:
   - [ ] Dialog uses the existing DangerousActionDialog-style dark panel.
   - [ ] Close / Cancel / Confirm affordances are clear.

**Polish checklist applied (source: Plan 06-06 Task 2):**
- [x] Spacing scale consistency (p-4 / gap-4 / mb-4)
- [x] Typography hierarchy (text-3xl page / text-2xl section / text-xl card)
- [x] Card.tsx shadow + rounded alignment
- [x] Button variant consistency (btn-primary / btn-secondary)
- [x] Responsive grid on PartsCatalog

**Expected result:** Every checkbox above ticked by the operator. No visual regression vs. pre-Phase-6 staging screenshots.

**Out of scope (explicitly NOT verified here):**
- Information-architecture changes (none made per D-17)
- New components or pages (none added)
- Copy changes (none made)
- Full parts-catalog redesign (deferred to UX-V2-01)

**Sign-off:** _____________________ (date: ____________)
```

STEP 2 — Commit the 06-HUMAN-UAT.md update:

```bash
git add .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md
git commit -m "docs(06-06): expand 06-HUMAN-UAT.md Section 4 with parts-catalog polish checklist"
```

STEP 3 — Pause for operator. Operator runs the 5 verification steps in a real browser (staging or local dev), ticks the checkboxes, and signs off on Section 4. If any step fails, operator replies "issues: <list>" and a follow-up cycle (not a new plan) fixes them.

STEP 4 — Once the operator signs off on Section 4, the plan is complete. No further code changes expected.
  </action>
  <what-built>
    Plan 06-06 Tasks 1 and 2 applied opportunistic polish across touched files (Plans 06-01..05 scope) and a bounded polish pass on pages/parts/* + components/parts/*. Task 3 expands 06-HUMAN-UAT.md Section 4 from its Plan 06-04 placeholder into a concrete 5-step checklist and pauses for operator visual verification.
  </what-built>
  <how-to-verify>
Follow STEPS 1-4 in the <action> block above:
1. The 06-HUMAN-UAT.md file has been edited to include the full Section 4 checklist (replace the placeholder).
2. The file has been committed with the message specified above.
3. The operator then opens the staging or local dev site and ticks each checkbox in a real browser.
4. Operator reply of "approved" signals plan completion; "issues: <list>" triggers a follow-up fix cycle.
  </how-to-verify>
  <resume-signal>Reply "approved" once Section 4 sign-off is complete OR "issues: &lt;list&gt;" if a checklist item fails</resume-signal>
  <verify>
    <automated>grep -q "Parts-catalog polish visual acceptance" .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md && ! grep -q "Placeholder — will be populated by Plan 06-06" .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Parts-catalog polish visual acceptance (FE-07 / D-17)" .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md exits 0
    - grep -q "Placeholder" .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md returns nothing (placeholder replaced)
    - grep -c "\[ \]" .planning/phases/06-frontend-cleanup-final-ci-gates/06-HUMAN-UAT.md returns at least 5 (5 checklist items in Section 4 at minimum)
    - Operator resume-signal ("approved" or "issues: ...") received
  </acceptance_criteria>
  <done>06-HUMAN-UAT.md Section 4 expanded from placeholder to concrete 5-step checklist; committed; operator has signed off on parts-catalog polish.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Polish scope -> scope creep | D-17 explicitly bounds this plan; an undisciplined polish pass is the threat. |
| Visual acceptance -> user perception | UAT Section 4 is the operator's visual check; subjective but the only gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-26 | Tampering | Scope creep into parts-catalog redesign | mitigate | D-17 explicitly bounds polish to pages/parts/* + components/parts/* with no IA change. Acceptance criterion enforces no file modified outside the scope. Full redesign is UX-V2-01 (v2 milestone). |
| T-06-27 | Denial of Service | Polish introduces layout regression | mitigate | `npm run lint && npm test -- --run && npm run build` sweep catches compile + test regressions. UAT Section 4's 5-step checklist catches visual regressions in a real browser. |
| T-06-28 | Information Disclosure | N/A — visual polish has no privacy surface | accept | N/A |
| T-06-29 | Spoofing | UAT sign-off without real verification | accept | Operator-gated; relies on sign-off discipline. No technical mitigation available. |
</threat_model>

<verification>
Before merging Plan 06-06:
1. cd frontend && npm run lint — exit 0
2. cd frontend && npm test -- --run — exit 0
3. cd frontend && npm run build — exit 0
4. cd frontend && npx madge --circular --extensions ts,tsx src/ — exit 0
5. No file outside pages/parts/* + components/parts/* + files-touched-by-Plans-06-01..05 has been modified in this plan's commits
6. 06-HUMAN-UAT.md Section 4 has been expanded from placeholder to concrete checklist with 5-step verification
7. Operator has signed off on Section 4 (checkpoint resume-signal received)
</verification>

<success_criteria>
- FE-07 closes: opportunistic polish applied where consistency required; bounded parts-catalog polish pass complete
- D-17 observed: no scope creep outside the defined polish boundaries
- 06-HUMAN-UAT.md Section 4 expanded to concrete checklist; operator sign-off captured
- Final lint + test + build + madge sweep all green
- All acceptance_criteria on Tasks 1 and 2 pass; Task 3 checkpoint resolved
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-cleanup-final-ci-gates/06-06-SUMMARY.md` per template. SUMMARY must record: (a) which touched files required opportunistic polish in Task 1 (or "none — already consistent"), (b) which parts-catalog files were polished in Task 2 + the specific pattern each fix aligned to (spacing / typography / card shadow / button / grid), (c) operator sign-off date + any UAT follow-up issues that surfaced.
</output>
