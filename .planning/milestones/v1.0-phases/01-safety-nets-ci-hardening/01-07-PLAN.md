---
phase: 01-safety-nets-ci-hardening
plan: 07
type: execute
wave: 5
depends_on: [04]
files_modified:
  - backend/tests/crawlers/fixtures/briantooleyracing/product.html
  - backend/tests/crawlers/fixtures/briantooleyracing/expected.json
  - backend/tests/crawlers/fixtures/amsperformance/product.html
  - backend/tests/crawlers/fixtures/amsperformance/expected.json
  - backend/tests/crawlers/fixtures/summitracing/product.html
  - backend/tests/crawlers/fixtures/summitracing/expected.json
  - backend/tests/crawlers/fixtures/ecstuning/product.html
  - backend/tests/crawlers/fixtures/ecstuning/expected.json
  - backend/tests/crawlers/fixtures/texasspeed/product.html
  - backend/tests/crawlers/fixtures/texasspeed/expected.json
  - backend/tests/crawlers/test_characterization_briantooleyracing.py
  - backend/tests/crawlers/test_characterization_amsperformance.py
  - backend/tests/crawlers/test_characterization_summitracing.py
  - backend/tests/crawlers/test_characterization_ecstuning.py
  - backend/tests/crawlers/test_characterization_texasspeed.py
autonomous: false
requirements:
  - SAFE-07
tags:
  - crawlers
  - characterization
  - fixtures
  - tier0
  - tier1
  - tier2

must_haves:
  truths:
    - "5 adapter characterization tests exist: 2× tier0_http (briantooleyracing, amsperformance — post-fix 7831fda per D-20), 2× tier2_browser (summitracing, ecstuning — FlareSolverr, 'most production-critical' interpretation of D-20's `tier1_flaresolverr` label per Pitfall 7), 1× tier1_tls (texasspeed — 'most stable' per Open Question 5)"
    - "Each test feeds a committed `product.html` through the adapter's `parse_product_page()` and asserts the returned `ScrapedPayload` fields match a committed `expected.json` (D-22)"
    - "Tests key adapters by class name (e.g. `BrianTooleyRacingAdapter()`), NOT by `ADAPTER_NAME` — per D-23 `ADAPTER_NAME` may not exist until Phase 3 CRAWL-02 lands"
    - "Tests exercise ONLY `parse_product_page()` — NOT `discover_product_urls()` (deferred to Phase 3 per D-22 and 'Deferred Ideas')"
    - "Each `expected.json` is deterministically generated: `json.dumps(asdict(payload), indent=2, sort_keys=True)`"
    - "The 5 HTML fixtures are real archived product pages from the crawl-archive source — NOT inline synthetic HTML (D-21)"
  artifacts:
    - path: "backend/tests/crawlers/fixtures/<adapter>/product.html"
      provides: "Real archived product page for each of 5 adapters"
    - path: "backend/tests/crawlers/fixtures/<adapter>/expected.json"
      provides: "Committed expected-output payload (sort_keys=True, indent=2) for each of 5 adapters"
    - path: "backend/tests/crawlers/test_characterization_<adapter>.py"
      provides: "Characterization test pinning parse_product_page output for each of 5 adapters"
  key_links:
    - from: "backend/tests/crawlers/test_characterization_briantooleyracing.py"
      to: "backend/app/crawlers/adapters/tier0_http/briantooleyracing.py `parse_product_page`"
      via: "Direct method call with HTML + URL arguments"
      pattern: "BrianTooleyRacingAdapter().parse_product_page"
    - from: "each test_characterization_*.py"
      to: "backend/tests/crawlers/fixtures/<adapter>/{product.html, expected.json}"
      via: "Path(__file__).parent / 'fixtures' / '<adapter>' / ... read_text / json.load"
      pattern: "fixtures"
---

<objective>
Implement SAFE-07: pin current `parse_product_page()` output for 5 representative adapters across fetcher tiers, so Phase 3's crawler refactor (CRAWL-01…CRAWL-07) has a regression guardrail.

Adapter picks, locked by D-20 + Pitfall 7 interpretation (actual directory layout is `tier0_http/`, `tier1_tls/`, `tier2_browser/` — `tier1_flaresolverr` in D-20 is a typo for `tier2_browser` which hosts FlareSolverr adapters):
- **2× tier0_http (post-7831fda fix):** `briantooleyracing`, `amsperformance`
- **2× tier2_browser (most production-critical — FlareSolverr):** `summitracing`, `ecstuning`
- **1× tier1_tls (most stable — curl_cffi):** `texasspeed`

The 2× tier2_browser and 1× tier1_tls picks are planner interpretations per RESEARCH Open Questions 4/5. If the user disagrees, they can swap any of them via the checkpoint in Task 1.

Purpose: Phase 3 CRAWL-01 (auto-discovery replaces hand-maintained registry) and CRAWL-05 (ThreadPoolExecutor parallelization) will refactor shared crawler infrastructure. Silent regression on any retailer's parse would corrupt production data. These 5 tests pin current output so the refactor must either reproduce it or consciously break it.

Output:
- 5 `product.html` fixtures under `backend/tests/crawlers/fixtures/<adapter>/` (archived HTML, D-21).
- 5 `expected.json` files — deterministic JSON matching current `parse_product_page()` output.
- 5 test files under `backend/tests/crawlers/` named `test_characterization_<adapter>.py`.
</objective>

<execution_context>
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/workflows/execute-plan.md
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/REQUIREMENTS.md
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/phases/01-safety-nets-ci-hardening/01-CONTEXT.md
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/phases/01-safety-nets-ci-hardening/01-RESEARCH.md
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/phases/01-safety-nets-ci-hardening/01-PATTERNS.md
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/phases/01-safety-nets-ci-hardening/01-VALIDATION.md
@/home/tyler-webb/Documents/Github/CarModPicker/CLAUDE.md
@/home/tyler-webb/Documents/Github/CarModPicker/backend/app/crawlers/base.py
@/home/tyler-webb/Documents/Github/CarModPicker/backend/app/crawlers/adapters/base.py
@/home/tyler-webb/Documents/Github/CarModPicker/backend/app/crawlers/adapters/tier0_http/briantooleyracing.py
@/home/tyler-webb/Documents/Github/CarModPicker/backend/app/crawlers/adapters/tier0_http/amsperformance.py
@/home/tyler-webb/Documents/Github/CarModPicker/backend/app/crawlers/adapters/tier2_browser/summitracing.py
@/home/tyler-webb/Documents/Github/CarModPicker/backend/app/crawlers/adapters/tier2_browser/ecstuning.py
@/home/tyler-webb/Documents/Github/CarModPicker/backend/app/crawlers/adapters/tier1_tls/texasspeed.py
@/home/tyler-webb/Documents/Github/CarModPicker/backend/tests/crawlers/test_briantooleyracing_adapter.py

<interfaces>
Adapter contract (from backend/app/crawlers/adapters/base.py):

    class RetailerCrawlerAdapter:
        FETCHER_TIER: Literal["http", "tls", "browser"]
        def parse_product_page(self, html: str, url: str) -> ScrapedPayload | None: ...
        def discover_product_urls(self, ...) -> list[str]: ...  # NOT tested in this plan

ScrapedPayload shape (from backend/app/crawlers/base.py — verified during research):

    @dataclass
    class ScrapedPayload:
        name: str
        product_url: str
        description: str | None
        price_cents: int | None
        part_manufacturer: str | None
        part_number: str | None
        image_urls: list[str] | None
        gtin: str | None
        # (verify actual field names + any additional fields at implementation time)

Each adapter is a subclass and exports one class:

    from app.crawlers.adapters.tier0_http.briantooleyracing import BrianTooleyRacingAdapter
    from app.crawlers.adapters.tier0_http.amsperformance import AmsPerformanceAdapter
    from app.crawlers.adapters.tier2_browser.summitracing import SummitRacingAdapter
    from app.crawlers.adapters.tier2_browser.ecstuning import EcsTuningAdapter
    from app.crawlers.adapters.tier1_tls.texasspeed import TexasSpeedAdapter

(Exact class names MUST be re-verified by reading each adapter module at implementation time.)
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm 5 adapter picks + source of archived HTML</name>
  <decision>
(a) Confirm the 5 adapter picks: `briantooleyracing`, `amsperformance`, `summitracing`, `ecstuning`, `texasspeed`.
(b) Confirm the source of archived product-page HTML: the `carmodpicker-prod-user-images` bucket per CONTEXT.md D-21 — OR a different bucket if CONCERNS.md has a more specific crawl-archive bucket name.
  </decision>
  <context>
CONTEXT.md D-20 specifies "2× tier1_flaresolverr" — RESEARCH.md Pitfall 7 resolves this to `tier2_browser` (the actual directory name for FlareSolverr-using adapters in this codebase). Open Questions 4 and 5 in RESEARCH leave the specific picks to the planner or a subsequent checkpoint.

Planner's rationale for the 5 picks:
- `briantooleyracing`, `amsperformance`: tier0_http, recently fixed in commit 7831fda (D-20) so pinning current behavior is valuable.
- `summitracing`: tier2_browser, high-volume retailer; FlareSolverr is essential due to Imperva challenge; any parse regression is high-impact.
- `ecstuning`: tier2_browser, second FlareSolverr adapter; diverse product catalog (European car parts) covers a different JSON-LD shape than Summit.
- `texasspeed`: tier1_tls, Cloudflare-gated; curl_cffi-stable; the two sibling tier1_tls adapters (`cobbtuning`, `turnermotorsport`) are the alternatives — both viable. `texasspeed` chosen for stable traffic per Open Question 5.

The archived HTML source bucket is the second open question. RESEARCH.md lines 58-60 reference `carmodpicker-prod-user-images`, but CONCERNS.md may specify a separate crawl-archive bucket (e.g. `carmodpicker-crawl-archive-<env>` or similar). The user is best positioned to confirm which bucket currently holds archived retailer HTML.
  </context>
  <options>
    <option id="pick-default">
      <name>Accept planner's picks + confirm bucket name</name>
      <pros>Maximizes representativeness per D-20's intent; each pick is defensible. Fastest path forward.</pros>
      <cons>User may have stronger signal about which adapter is most stable/most critical.</cons>
    </option>
    <option id="pick-swap-tier1">
      <name>Swap texasspeed for apr (tier1_tls)</name>
      <pros>apr.py is older/more stable per intuition; user may prefer.</pros>
      <cons>Requires the user to retrieve `apr` archived HTML instead of `texasspeed`.</cons>
    </option>
    <option id="pick-swap-tier2">
      <name>Swap ecstuning or summitracing for a different tier2_browser adapter</name>
      <pros>User knows production traffic; "most critical" is an empirical call.</pros>
      <cons>Delay while user provides the new pick + archived HTML.</cons>
    </option>
  </options>
  <read_first>
    - .planning/phases/01-safety-nets-ci-hardening/01-CONTEXT.md D-20, D-21 (locked picks + HTML source)
    - .planning/phases/01-safety-nets-ci-hardening/01-RESEARCH.md Open Questions 4, 5 + Pitfall 7
    - `backend/app/crawlers/adapters/tier2_browser/` (inventory — 11 adapters)
    - `backend/app/crawlers/adapters/tier1_tls/` (inventory — 16 adapters)
    - `.planning/codebase/CONCERNS.md` (crawl-archive bucket name)
  </read_first>
  <resume-signal>
Reply with one of:
- `ACCEPT: picks default + bucket=<name>` to use the planner's picks and confirm bucket.
- `SWAP: tier1_tls=<name>` or `SWAP: tier2_browser=[<name1>,<name2>]` with the new pick(s), plus bucket name.
- `ACCEPT: picks default + bucket=<name> + I will fetch HTML fixtures locally and hand you the 5 files at paths <list>`

Planner resumes Task 2 using the confirmed 5 adapter names + bucket name.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Generate 5 HTML fixtures + 5 expected.json files</name>
  <files>backend/tests/crawlers/fixtures/{briantooleyracing,amsperformance,summitracing,ecstuning,texasspeed}/product.html, backend/tests/crawlers/fixtures/{briantooleyracing,amsperformance,summitracing,ecstuning,texasspeed}/expected.json</files>
  <read_first>
    - Task 1 resume signal (confirmed adapter names + bucket name)
    - backend/app/crawlers/base.py (`ScrapedPayload` dataclass exact fields — this is the source of truth for what fields expected.json captures)
    - The 5 confirmed adapter modules under `backend/app/crawlers/adapters/` (so the executor understands the product-URL pattern for each)
    - .planning/phases/01-safety-nets-ci-hardening/01-CONTEXT.md D-21, D-22 (archived HTML source + parse_product_page-only testing)
    - .planning/phases/01-safety-nets-ci-hardening/01-PATTERNS.md §15, §16 (fixture dir layout and deterministic JSON format)
  </read_first>
  <action>
For EACH of the 5 adapters:

**Step A — fetch archived HTML.** Use the bucket name confirmed in Task 1. Example (replace `<BUCKET>` and `<KEY>`):

    mkdir -p /home/tyler-webb/Documents/Github/CarModPicker/backend/tests/crawlers/fixtures/<adapter>
    aws s3 cp s3://<BUCKET>/<KEY_FOR_ONE_PRODUCT_PAGE>.html \
      /home/tyler-webb/Documents/Github/CarModPicker/backend/tests/crawlers/fixtures/<adapter>/product.html

Alternatively, if archived HTML is accessed via a different utility (e.g. `backend/scripts/sync_crawl_archive_to_prod.py` — read it at implementation time), use that utility.

One representative product page per adapter. Pick one that:
- Has a stable JSON-LD Product block (parse-critical).
- Has price + description + at least one image in gallery (so expected.json is non-trivial).
- Was fetched AFTER the most recent parse-fix commit for that adapter (for the 2 tier0_http adapters, this means post-7831fda).

If no archived HTML is available (bucket empty / pre-merge milestone / user lacks access): the executor MUST NOT synthesize HTML from scratch. Instead, STOP and escalate back to Task 1's checkpoint: "No archived HTML available for adapter `<name>` — request user provide via manual upload, or agree to swap adapter."

**Step B — compute expected.json from parse_product_page output.**

Write a small regen helper (as a throwaway script or one-liner) that:
1. Loads the committed HTML.
2. Instantiates the adapter.
3. Calls `parse_product_page(html, canonical_url)`.
4. Serializes the result via `dataclasses.asdict` + `json.dumps(..., indent=2, sort_keys=True)`.
5. Writes to `expected.json`.

One-liner template (substitute class name, module, and canonical URL for each adapter):

    cd /home/tyler-webb/Documents/Github/CarModPicker/backend && python -c "
    import json
    from dataclasses import asdict
    from pathlib import Path
    from app.crawlers.adapters.tier0_http.briantooleyracing import BrianTooleyRacingAdapter

    FIXTURE = Path('tests/crawlers/fixtures/briantooleyracing')
    html = (FIXTURE/'product.html').read_text(encoding='utf-8')
    url = 'https://briantooleyracing.com/<CANONICAL_PRODUCT_URL>'
    payload = BrianTooleyRacingAdapter().parse_product_page(html, url)
    assert payload is not None, 'parse_product_page returned None — fixture HTML does not match adapter expectations'
    out = asdict(payload)
    # Ensure product_url matches what we passed in (some adapters rewrite this field).
    (FIXTURE/'expected.json').write_text(json.dumps(out, indent=2, sort_keys=True) + '\n')
    print('wrote', FIXTURE/'expected.json')
    "

Repeat for each of the 5 adapters. Substitute the correct class name and import path.

**Step C — verify determinism.**

Run the regen command twice for the same adapter. Diff the two outputs:

    python -c "... (regen)" && cp expected.json /tmp/a.json
    python -c "... (regen)" && diff /tmp/a.json backend/tests/crawlers/fixtures/<adapter>/expected.json
    # Expect: no diff output, exit 0.

If there's a diff, the payload has a non-deterministic field (e.g. a datetime or a randomly-ordered set). Investigate and either (a) fix the adapter (out of scope — escalate to user) or (b) exclude the non-deterministic field from the committed JSON at `assert` time (test code, NOT the expected.json shape).

**Step D — sanity-check each expected.json for correctness.**

Open each expected.json and verify:
- `name` is non-empty (actual product name, not "None" or garbage).
- `price_cents` is a positive integer (or `null` if the page has no price block — but for the 5 picked pages we prefer pages WITH prices).
- `image_urls` is a non-empty list of HTTPS URLs.
- `product_url` equals the canonical URL passed to `parse_product_page`.

If any field is obviously wrong for the HTML fixture, the fixture may be a bot-challenge page rather than a real product page — re-fetch a different URL.

Format: no code files in this task; HTML + JSON only. No black/isort/pyright required. However:
- JSON files MUST end with a single newline (many editors add this automatically; the one-liner above appends `'\n'`).
- HTML files are committed exactly as-fetched (do NOT reformat).
  </action>
  <verify>
    <automated>cd /home/tyler-webb/Documents/Github/CarModPicker/backend && for a in briantooleyracing amsperformance summitracing ecstuning texasspeed; do test -s tests/crawlers/fixtures/$a/product.html && test -s tests/crawlers/fixtures/$a/expected.json && python -c "import json; d = json.load(open('tests/crawlers/fixtures/$a/expected.json')); assert d.get('name') and d.get('product_url'), 'empty critical field in $a'" || { echo FAIL $a; exit 1; }; done; echo "ALL 5 FIXTURES OK"</automated>
  </verify>
  <acceptance_criteria>
    - 5 `product.html` files exist at the 5 paths listed in `files_modified` (command: `find backend/tests/crawlers/fixtures -name 'product.html' | wc -l` returns 5)
    - 5 `expected.json` files exist at the 5 paths listed in `files_modified` (command: `find backend/tests/crawlers/fixtures -name 'expected.json' | wc -l` returns 5)
    - Each HTML file is non-empty (size > 5 KB — real product pages are typically > 50 KB; < 5 KB is likely a bot-challenge page)
    - Each expected.json is valid JSON and contains at minimum a non-empty `"name"` field and a non-empty `"product_url"` field (verified by the Python command above)
    - Each expected.json is deterministically serialized: `python -c "import json; d = json.load(open('<path>')); s = json.dumps(d, indent=2, sort_keys=True); assert open('<path>').read().rstrip() == s"` exits 0 (the committed file equals `json.dumps(..., indent=2, sort_keys=True)`)
    - No inline synthetic HTML: `grep -l 'this is a synthetic test page' backend/tests/crawlers/fixtures/**/product.html` returns no results (real archived HTML, not hand-written)
  </acceptance_criteria>
  <done>5 HTML fixtures + 5 expected.json files committed; each JSON is deterministically sorted; every `name` and `product_url` field is non-empty; Python one-liner exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Write 5 characterization test files</name>
  <files>
    backend/tests/crawlers/test_characterization_briantooleyracing.py,
    backend/tests/crawlers/test_characterization_amsperformance.py,
    backend/tests/crawlers/test_characterization_summitracing.py,
    backend/tests/crawlers/test_characterization_ecstuning.py,
    backend/tests/crawlers/test_characterization_texasspeed.py
  </files>
  <read_first>
    - backend/tests/crawlers/test_briantooleyracing_adapter.py lines 229-247 (the `TestParseProductPage` class — literal skeleton for the new test)
    - Each of the 5 adapter modules (confirm class name + exact import path)
    - backend/app/crawlers/base.py (ScrapedPayload field set — for `_payload_to_dict` helper to dump)
    - .planning/phases/01-safety-nets-ci-hardening/01-PATTERNS.md §14 (test shape, class-name keying per D-23)
    - .planning/phases/01-safety-nets-ci-hardening/01-RESEARCH.md lines 962-1026 (Code Example 8 — literal test body)
  </read_first>
  <action>
Create 5 test files, one per adapter. Each follows the same template; the only differences are:
- Adapter import path + class name
- Fixture directory path

Template (substitute `<ADAPTER_MODULE>`, `<ADAPTER_CLASS>`, `<ADAPTER_DIR>`):

    """SAFE-07 characterization for <ADAPTER_CLASS>.

    Pins the current `parse_product_page()` output shape against a committed
    product HTML fixture. If the parse output changes intentionally, regenerate
    `expected.json` using the one-liner at the bottom of this file.

    Per D-22 we test ONLY `parse_product_page()`, NOT `discover_product_urls()`.
    Per D-23 we key by class name (not ADAPTER_NAME, which doesn't land until
    Phase 3 CRAWL-02).
    """

    from __future__ import annotations

    import json
    from dataclasses import asdict
    from pathlib import Path
    from typing import Any

    from app.crawlers.adapters.<ADAPTER_MODULE> import <ADAPTER_CLASS>

    FIXTURE_DIR = Path(__file__).parent / "fixtures" / "<ADAPTER_DIR>"
    HTML_PATH = FIXTURE_DIR / "product.html"
    EXPECTED_PATH = FIXTURE_DIR / "expected.json"


    def _payload_to_dict(payload: Any) -> dict:
        return asdict(payload)


    def test_parse_product_page_matches_expected() -> None:
        html = HTML_PATH.read_text(encoding="utf-8")
        expected = json.loads(EXPECTED_PATH.read_text(encoding="utf-8"))

        adapter = <ADAPTER_CLASS>()
        payload = adapter.parse_product_page(html, expected["product_url"])

        assert payload is not None, "parse_product_page returned None — fixture / adapter mismatch"
        actual = _payload_to_dict(payload)

        # image_urls: compare as SETS (order is CDN-dependent, not contract-stable).
        actual_images = set(actual.pop("image_urls") or [])
        expected_images = set(expected.pop("image_urls") or [])
        assert actual_images == expected_images, (
            f"image_urls drift: actual={actual_images} expected={expected_images}"
        )
        assert actual == expected, f"parse output drift: {actual!r} vs {expected!r}"


    # To regenerate expected.json after an intentional parse change:
    #
    #   cd backend && python -c "
    #   import json
    #   from dataclasses import asdict
    #   from pathlib import Path
    #   from app.crawlers.adapters.<ADAPTER_MODULE> import <ADAPTER_CLASS>
    #   FIXTURE = Path('tests/crawlers/fixtures/<ADAPTER_DIR>')
    #   html = (FIXTURE/'product.html').read_text()
    #   expected = json.loads((FIXTURE/'expected.json').read_text())
    #   payload = <ADAPTER_CLASS>().parse_product_page(html, expected['product_url'])
    #   (FIXTURE/'expected.json').write_text(json.dumps(asdict(payload), indent=2, sort_keys=True) + '\n')
    #   "

Substitution table for the 5 files (confirm exact class names by reading each module):

| File | `<ADAPTER_MODULE>` | `<ADAPTER_CLASS>` | `<ADAPTER_DIR>` |
|------|--------------------|-------------------|-----------------|
| test_characterization_briantooleyracing.py | tier0_http.briantooleyracing | BrianTooleyRacingAdapter | briantooleyracing |
| test_characterization_amsperformance.py | tier0_http.amsperformance | AmsPerformanceAdapter | amsperformance |
| test_characterization_summitracing.py | tier2_browser.summitracing | SummitRacingAdapter | summitracing |
| test_characterization_ecstuning.py | tier2_browser.ecstuning | EcsTuningAdapter | ecstuning |
| test_characterization_texasspeed.py | tier1_tls.texasspeed | TexasSpeedAdapter | texasspeed |

(Class names above are PLAUSIBLE based on Python camelCase convention. VERIFY each by reading the adapter module's class definition — e.g. `grep -nE '^class ' backend/app/crawlers/adapters/tier0_http/amsperformance.py` — and correct the template before committing.)

Format: `black --config pyproject.toml --line-length 120` + `isort` + pyright strict.

Worker safety: each test reads committed files only; no state. Safe under `-n auto --dist=loadfile`.

No test fixtures (the `client`, `db_session`, etc. fixtures from conftest.py are NOT needed — these are pure-function tests against HTML input).
  </action>
  <verify>
    <automated>cd /home/tyler-webb/Documents/Github/CarModPicker/backend && pytest -n auto tests/crawlers/test_characterization_briantooleyracing.py tests/crawlers/test_characterization_amsperformance.py tests/crawlers/test_characterization_summitracing.py tests/crawlers/test_characterization_ecstuning.py tests/crawlers/test_characterization_texasspeed.py --no-cov -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - 5 test files exist at the 5 paths listed in `files_modified`
    - Each file contains exactly one `def test_parse_product_page_matches_expected()` function
    - Each file instantiates the correct adapter class (command: `grep 'parse_product_page' backend/tests/crawlers/test_characterization_*.py | wc -l` returns 5)
    - Each file references `FIXTURE_DIR = Path(__file__).parent / "fixtures" / "<adapter>"` with the correct adapter dir name
    - Each file imports from the correct adapter module path (command: for each file `grep -c 'from app.crawlers.adapters' backend/tests/crawlers/test_characterization_<adapter>.py` returns at least 1)
    - None of the 5 files tests `discover_product_urls` (command: `grep -l 'discover_product_urls' backend/tests/crawlers/test_characterization_*.py` returns no results)
    - `cd backend && pytest -n auto tests/crawlers/test_characterization_*.py --no-cov` exits 0 (all 5 pass)
    - `cd backend && pyright tests/crawlers/test_characterization_briantooleyracing.py tests/crawlers/test_characterization_amsperformance.py tests/crawlers/test_characterization_summitracing.py tests/crawlers/test_characterization_ecstuning.py tests/crawlers/test_characterization_texasspeed.py` reports 0 errors
    - `cd backend && black --config pyproject.toml --line-length 120 --check tests/crawlers/test_characterization_*.py` exits 0
    - Full backend suite still green under coverage floor: `cd backend && pytest -n auto -x` exits 0
  </acceptance_criteria>
  <done>5 test files exist; each asserts ScrapedPayload dict equality against committed expected.json; image_urls compared as sets to tolerate CDN ordering; all 5 pass under -n auto; pyright/black clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archive bucket → committed HTML | Archived retailer HTML pulled from S3 may contain embedded tracking/analytics scripts; committed as-is |
| Committed HTML → test parse | `parse_product_page` runs against committed HTML in CI; pure function, no network |
| Parser implementation ↔ expected.json | A parser-refactor PR must explicitly regenerate expected.json if output changes; the file diff is the review artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | Committed retailer HTML containing user-agent strings, analytics cookies, or session IDs baked into the archived page body | accept | Archived HTML is a SNAPSHOT from S3. The bucket policy does not permit recording user-specific sessions. Residual risk: if the crawler was (or will be) authenticated against a retailer, the archived page may contain the authenticated user's ID. Mitigation at recording time is out of scope for this plan — the crawler is anonymous today. If that changes, extend check: a grep for `<retailer>-session-id` patterns in committed HTML is a future guardrail. |
| T-07-02 | Tampering | Parse-output drift hidden behind non-deterministic serialization | mitigate | `json.dumps(..., indent=2, sort_keys=True)` locks serialization order. image_urls compared as SETS so CDN reordering is tolerated but drift in URL content still fails the test. Acceptance criterion on Task 2 runs the regen twice and asserts zero diff. |
| T-07-03 | Tampering | Regenerating expected.json without actually reviewing the diff | accept | D-22-27 pattern relies on reviewer discipline — the diff file IS the review artifact. No technical enforcement beyond commit-message convention. Phase 3 refactor PRs that regenerate expected.json must cite "regenerated intentionally" in the commit message. |
| T-07-04 | Elevation of Privilege | Adapter class name spoofing (e.g. a malicious PR renames `BrianTooleyRacingAdapter` to `BrianTooleyRacingAdapter2` and tests no longer cover it) | mitigate | Tests import the class by name; a rename would fail import with `ImportError`, caught immediately in CI. Phase 3 refactors should add a sanity test that asserts `ADAPTER_REGISTRY` contains at least the 5 characterized adapters. |
| T-07-05 | Denial of Service | A pathologically large product.html fixture slowing CI | accept | Real archived retailer pages are typically 50-500 KB. Even at 1 MB each, 5 files = 5 MB — negligible in CI. |

Severity summary: all threats low/medium after mitigations. T-07-02 (parse drift) is the highest-value mitigation — sort_keys + set-compare on image_urls + regenerate-diff-in-PR is the core contract.
</threat_model>

<verification>
- 5 `product.html` files under `backend/tests/crawlers/fixtures/` each > 5 KB
- 5 `expected.json` files with valid JSON, non-empty `name` and `product_url` fields, deterministic serialization
- 5 `test_characterization_<adapter>.py` files, each passing in isolation and under full `pytest -n auto`
- `cd backend && pytest -n auto tests/crawlers/test_characterization_*.py --no-cov` exits 0
- `cd backend && pytest -n auto -x` exits 0 (full suite under coverage floor)
- `cd backend && pyright tests/crawlers/test_characterization_*.py` reports 0 errors
- `cd backend && black --config pyproject.toml --line-length 120 --check tests/crawlers/test_characterization_*.py` exits 0
- No test touches `discover_product_urls` (D-22 boundary)
- No test uses `ADAPTER_NAME` keying (D-23 — defer to Phase 3)
</verification>

<success_criteria>
1. 5 adapter characterization tests exist, each pinning `parse_product_page()` against a committed HTML fixture
2. All 5 tests pass under `pytest -n auto` on the current codebase
3. Adapter picks cover all 3 fetcher tiers (2× http + 1× tls + 2× browser per Pitfall 7 interpretation of D-20)
4. Each `expected.json` is deterministically serialized (sort_keys=True, indent=2)
5. Regeneration one-liner is embedded in each test file as a comment for developer convenience
6. Full backend suite still green under Plan 04's `--cov-fail-under` floor
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-nets-ci-hardening/01-07-SUMMARY.md` with:
- The 5 adapter classes characterized (with exact import paths — Phase 3 CRAWL-02 will switch these to `ADAPTER_NAME` keys)
- HTML fixture sizes (sanity data for Phase 3's refactor PR size estimates)
- Citation of which archive bucket was used (handoff to Phase 3 for re-fetching if needed)
- Handoff note for Phase 3: these 5 tests are the only CI guardrail against silent parse regression during the auto-discovery + circuit-breaker + parallelization refactor. Phase 3 PRs MUST keep all 5 green; any intentional parse change requires regenerating the corresponding expected.json.
</output>
