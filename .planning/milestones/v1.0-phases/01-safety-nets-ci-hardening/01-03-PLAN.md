---
phase: 01-safety-nets-ci-hardening
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - backend/scripts/check_migrations.py
  - backend/tests/test_check_migrations.py
  - .github/workflows/backend-ci.yml
autonomous: true
requirements:
  - SAFE-04
tags:
  - ci
  - migrations
  - drop-guard
  - static-analysis

must_haves:
  truths:
    - "`backend/scripts/check_migrations.py` exits 1 and prints the offending file + line number when a migration in `backend/alembic/versions/*.py` contains `op.drop_column`, `op.drop_table`, or `op.drop_constraint` without a `# SAFE: <reason>` annotation on the same line or the immediately preceding line"
    - "The script exits 0 on the current state of `backend/alembic/versions/` (after Plan 02's repair annotations land)"
    - "`.github/workflows/backend-ci.yml` invokes `python backend/scripts/check_migrations.py` as a CI step placed BEFORE `Run tests with coverage` so a destructive-op regression fails CI before any coverage baseline measurement runs"
    - "A unit test (`backend/tests/test_check_migrations.py`) exercises PASS cases (same-line, preceding-line) and FAIL cases (no annotation, annotation 2 lines above, SAFE-in-docstring false-negative) using tmp-path fixtures"
    - "Phase 1 Success Criterion #3 (`Any migration containing drop_column, drop_table, or drop_constraint without a # SAFE: annotation fails the CI lint step`) is satisfied by this plan"
  artifacts:
    - path: "backend/scripts/check_migrations.py"
      provides: "SAFE-04 migration DROP guard script (non-interactive, stdout-only, file-scanning CLI)"
      min_lines: 60
      contains: "DESTRUCTIVE_OP_RE"
    - path: "backend/tests/test_check_migrations.py"
      provides: "Unit test coverage for the DROP-guard regex and line-above lookback"
      min_lines: 70
    - path: ".github/workflows/backend-ci.yml"
      provides: "New CI step `Check migrations for unannotated destructive operations`"
      contains: "check_migrations.py"
  key_links:
    - from: ".github/workflows/backend-ci.yml `Check migrations` step"
      to: "backend/scripts/check_migrations.py"
      via: "bash invocation in run: block"
      pattern: "python backend/scripts/check_migrations.py"
    - from: "backend/scripts/check_migrations.py"
      to: "backend/alembic/versions/*.py"
      via: "file glob scan via pathlib"
      pattern: "MIGRATIONS_DIR.glob"
---

<objective>
Implement SAFE-04: a CI-gated migration DROP guard that fails any PR adding an unannotated `drop_column` / `drop_table` / `drop_constraint` to `backend/alembic/versions/*.py`. Per D-07 this is a CI step (not a pre-commit hook) so developer iteration stays fast and enforcement lives at the merge gate.

Purpose: this guard must land BEFORE the SAFE-01/02/03 coverage gates (Plan 04) so a regression PR cannot slip a `drop_*` in while the coverage baseline is being measured. Plan 02's repair annotations (or legacy-superseded annotations) are already in-tree — this plan writes the guard that enforces that format going forward.

Output:
- New `backend/scripts/check_migrations.py` — non-interactive, read-only, exit 0/1/2 semantics, pure pathlib + regex.
- New `backend/tests/test_check_migrations.py` — unit test with tmp_path fixtures covering PASS/FAIL cases + ReDoS defense + docstring-SAFE defense.
- Modified `.github/workflows/backend-ci.yml` — new step inserted between `Scan dependencies for vulnerabilities` and `Run tests with coverage`.
</objective>

<execution_context>
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/workflows/execute-plan.md
@/home/tyler-webb/Documents/Github/CarModPicker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/REQUIREMENTS.md
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/phases/01-safety-nets-ci-hardening/01-CONTEXT.md
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/phases/01-safety-nets-ci-hardening/01-RESEARCH.md
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/phases/01-safety-nets-ci-hardening/01-PATTERNS.md
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/phases/01-safety-nets-ci-hardening/01-VALIDATION.md
@/home/tyler-webb/Documents/Github/CarModPicker/.planning/phases/01-safety-nets-ci-hardening/01-02-SUMMARY.md
@/home/tyler-webb/Documents/Github/CarModPicker/CLAUDE.md
@/home/tyler-webb/Documents/Github/CarModPicker/backend/scripts/flatten_migrations.py
@/home/tyler-webb/Documents/Github/CarModPicker/.github/workflows/backend-ci.yml

<interfaces>
check_migrations.py public surface (what this plan creates):

    DESTRUCTIVE_OP_RE: re.Pattern[str]   # matches op.(drop_column|drop_table|drop_constraint)(
    SAFE_ANNOTATION_RE: re.Pattern[str]  # matches # SAFE: <non-empty reason> anchored to start-of-meaningful-line
    REPO_ROOT: Path
    MIGRATIONS_DIR: Path                 # = REPO_ROOT / "backend" / "alembic" / "versions"

    def check_file(path: Path) -> list[tuple[int, str]]: ...
    def main() -> int: ...               # 0 on pass, 1 on violations, 2 on missing dir

Existing CI step ordering in backend-ci.yml (new step inserts between #7 and #8):
- Step 6: "Run security scan" (bandit)
- Step 7: "Scan dependencies for vulnerabilities" (pip-audit)
- NEW Step 7.5: "Check migrations for unannotated destructive operations"
- Step 8: "Run tests with coverage" (pytest --cov)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write backend/scripts/check_migrations.py DROP-guard script</name>
  <files>backend/scripts/check_migrations.py</files>
  <read_first>
    - backend/scripts/flatten_migrations.py (path-resolution pattern — lines 1-28 — copy the shebang + module-docstring + `Path(__file__).parent.parent` idiom; do NOT copy the interactive `input()` call or subprocess calls)
    - .planning/phases/01-safety-nets-ci-hardening/01-RESEARCH.md lines 622-698 (Code Example 2 — the literal body to lift)
    - .planning/phases/01-safety-nets-ci-hardening/01-CONTEXT.md D-07, D-08, D-09, D-10 (CI-only, scan path, annotation format, alembic-versions-only)
    - backend/alembic/versions/ (confirm glob target is populated, 30+ files)
    - .planning/phases/01-safety-nets-ci-hardening/01-02-SUMMARY.md (SAFE annotations now in-tree — this script must return 0 against them)
  </read_first>
  <action>
Create `backend/scripts/check_migrations.py` with the exact contents below. The body is the literal source from RESEARCH.md Code Example 2 with one robustness tweak: `REPO_ROOT = Path(__file__).resolve().parents[2]` so the script works regardless of CWD. The same-line and preceding-line regexes are distinct: same-line allows the `# SAFE:` to appear AFTER code on the line, while preceding-line requires `^\s*#\s*SAFE:` (anchored to start of line, leading whitespace allowed) to defend against the T-03-02 docstring-embedded-SAFE false negative.

    #!/usr/bin/env python3
    """
    SAFE-04: Migration DROP guard.

    Fails CI if any file in `backend/alembic/versions/*.py` contains
    `op.drop_column`, `op.drop_table`, or `op.drop_constraint` without a
    `# SAFE: <reason>` annotation on the SAME line or the IMMEDIATELY PRECEDING
    line.

    Per D-07 this guard runs at the merge gate (CI), not on developer machines.
    Per D-09 the annotation format is exactly `# SAFE: <human-readable reason>`.
    Per D-10 `backend/alembic/versions/*.py` is the ONLY scan path.

    Exit codes:
        0 — no violations
        1 — one or more violations (printed with filename + line number)
        2 — migrations directory not found (setup error)

    Usage:
        python backend/scripts/check_migrations.py

    Tests:
        pytest -n auto backend/tests/test_check_migrations.py
    """
    from __future__ import annotations

    import re
    import sys
    from pathlib import Path

    # <repo>/backend/scripts/check_migrations.py -> parents[2] = <repo>
    REPO_ROOT = Path(__file__).resolve().parents[2]
    MIGRATIONS_DIR = REPO_ROOT / "backend" / "alembic" / "versions"

    # Matches `op.drop_column(`, `op.drop_table(`, `op.drop_constraint(`.
    # The `\bop\.` prefix avoids false positives on variable names or function
    # defs. Bounded quantifiers only (ReDoS-safe — see T-03-01).
    DESTRUCTIVE_OP_RE = re.compile(r"\bop\.(drop_column|drop_table|drop_constraint)\s*\(")

    # Preceding-line annotation: `# SAFE:` anchored to start-of-line (leading
    # whitespace allowed; a docstring body line containing "SAFE:" does NOT
    # match — T-03-02 defense).
    SAFE_ANNOTATION_RE = re.compile(r"^\s*#\s*SAFE:\s*\S")

    # Same-line (inline-comment) annotation: the `#` can appear after code.
    INLINE_SAFE_RE = re.compile(r"#\s*SAFE:\s*\S")


    def check_file(path: Path) -> list[tuple[int, str]]:
        """Return list of (1-indexed line number, offending line text) for violations."""
        lines = path.read_text(encoding="utf-8").splitlines()
        violations: list[tuple[int, str]] = []
        for idx, line in enumerate(lines):
            if not DESTRUCTIVE_OP_RE.search(line):
                continue
            # Same-line inline annotation?
            if INLINE_SAFE_RE.search(line):
                continue
            # Immediately-preceding-line annotation?
            if idx > 0 and SAFE_ANNOTATION_RE.search(lines[idx - 1]):
                continue
            violations.append((idx + 1, line.rstrip()))
        return violations


    def main() -> int:
        if not MIGRATIONS_DIR.is_dir():
            print(f"ERROR: migrations dir not found: {MIGRATIONS_DIR}", file=sys.stderr)
            return 2

        migration_files = sorted(MIGRATIONS_DIR.glob("*.py"))
        failures: list[tuple[Path, int, str]] = []
        for py in migration_files:
            for lineno, text in check_file(py):
                failures.append((py, lineno, text))

        if not failures:
            print(f"check_migrations: OK ({len(migration_files)} files scanned)")
            return 0

        print("check_migrations: FAILURES")
        print()
        for path, lineno, text in failures:
            rel = path.relative_to(REPO_ROOT)
            print(f"  {rel}:{lineno}")
            print(f"    {text}")
            print("    --> Add `# SAFE: <reason>` on this line or the line above.")
            print()
        return 1


    if __name__ == "__main__":
        sys.exit(main())

Formatting: `black --config pyproject.toml --line-length 120` and `isort .` after write; pyright strict. Make the file executable: `chmod +x backend/scripts/check_migrations.py`.

Sanity-run against current tree (must pass after Plan 02's annotations landed):

    cd /home/tyler-webb/Documents/Github/CarModPicker && python backend/scripts/check_migrations.py
    # Expected: "check_migrations: OK (30+ files scanned)" and exit 0.
  </action>
  <verify>
    <automated>cd /home/tyler-webb/Documents/Github/CarModPicker && python backend/scripts/check_migrations.py && echo "SCRIPT OK"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/scripts/check_migrations.py` exists and is executable (`test -x backend/scripts/check_migrations.py` exits 0)
    - File contains the literal string `DESTRUCTIVE_OP_RE = re.compile(r"\bop\.(drop_column|drop_table|drop_constraint)\s*\(")`
    - File contains the literal string `SAFE_ANNOTATION_RE = re.compile(r"^\s*#\s*SAFE:\s*\S")`
    - File contains a `def check_file(path: Path) -> list[tuple[int, str]]:` definition
    - File contains a `def main() -> int:` definition
    - File contains `sys.exit(main())` inside an `if __name__ == "__main__":` guard
    - Running `cd /home/tyler-webb/Documents/Github/CarModPicker && python backend/scripts/check_migrations.py` prints a line beginning with `check_migrations: OK` and exits 0 (current tree is clean after Plan 02)
    - Running `cd /tmp && python /home/tyler-webb/Documents/Github/CarModPicker/backend/scripts/check_migrations.py` also exits 0 — CWD-independent
    - `cd backend && pyright scripts/check_migrations.py` reports 0 errors
    - `cd backend && black --config pyproject.toml --line-length 120 --check scripts/check_migrations.py` exits 0
    - `cd backend && isort --check-only scripts/check_migrations.py` exits 0
  </acceptance_criteria>
  <done>check_migrations.py exists, is executable, runs in < 1s, exits 0 on current tree, and its regex surface (`DESTRUCTIVE_OP_RE`, `SAFE_ANNOTATION_RE`, `INLINE_SAFE_RE`) is importable for the unit test.</done>
</task>

<task type="auto">
  <name>Task 2: Write unit test covering PASS and FAIL cases of the DROP guard</name>
  <files>backend/tests/test_check_migrations.py</files>
  <read_first>
    - backend/scripts/check_migrations.py (the newly-created script — test imports `check_file` directly)
    - backend/tests/conftest.py (fixture conventions + env-var ordering)
    - backend/tests/test_main.py (top-level-tests directory convention)
    - .planning/phases/01-safety-nets-ci-hardening/01-RESEARCH.md lines 700-724 (the 4 worked examples)
  </read_first>
  <action>
Create `backend/tests/test_check_migrations.py` with the exact contents below. Uses pytest's `tmp_path` fixture to construct fake migration files; does NOT scan the real `alembic/versions/` directory (decoupled).

    """SAFE-04 unit tests for backend/scripts/check_migrations.py.

    Covers:
    - PASS: destructive op with same-line SAFE annotation
    - PASS: destructive op with preceding-line SAFE annotation
    - FAIL: destructive op with no annotation
    - FAIL: destructive op with annotation 2 lines above (not immediately preceding)
    - FAIL defense (T-03-02): "SAFE:" token embedded in a docstring/text above
      the destructive op does NOT satisfy the annotation requirement
    - DoS defense (T-03-01): script completes in < 1 second on a pathological
      input (many destructive ops in one file)
    """

    from __future__ import annotations

    import sys
    import time
    from pathlib import Path

    import pytest

    # backend/scripts/ is not on sys.path by default; add it at collection time.
    SCRIPTS_DIR = Path(__file__).resolve().parents[1] / "scripts"
    if str(SCRIPTS_DIR) not in sys.path:
        sys.path.insert(0, str(SCRIPTS_DIR))

    from check_migrations import check_file  # noqa: E402


    def _write(tmp_path: Path, body: str) -> Path:
        path = tmp_path / "001_test_migration.py"
        path.write_text(body, encoding="utf-8")
        return path


    def test_pass_same_line_annotation(tmp_path: Path) -> None:
        path = _write(
            tmp_path,
            'op.drop_column("users", "legacy_avatar_path")  # SAFE: column is empty on prod; see ADR-007\n',
        )
        assert check_file(path) == []


    def test_pass_preceding_line_annotation(tmp_path: Path) -> None:
        path = _write(
            tmp_path,
            "# SAFE: table has zero rows on prod (verified via COUNT(*) 2026-04-18)\n"
            'op.drop_table("abandoned_experiments")\n',
        )
        assert check_file(path) == []


    def test_fail_no_annotation(tmp_path: Path) -> None:
        path = _write(
            tmp_path,
            'op.drop_constraint("fk_parts_canonical", "parts", type_="foreignkey")\n',
        )
        violations = check_file(path)
        assert len(violations) == 1
        assert violations[0][0] == 1
        assert "drop_constraint" in violations[0][1]


    def test_fail_annotation_two_lines_above(tmp_path: Path) -> None:
        """Annotation must be IMMEDIATELY preceding — a 1-line gap is a miss."""
        path = _write(
            tmp_path,
            "# SAFE: this is the destructive sweep\n"
            "some_other_line = 1\n"
            'op.drop_column("users", "legacy_field")\n',
        )
        violations = check_file(path)
        assert len(violations) == 1
        assert violations[0][0] == 3


    def test_fail_safe_in_docstring_does_not_count(tmp_path: Path) -> None:
        """T-03-02 defense: 'SAFE:' inside a docstring or non-comment line
        does not satisfy the annotation requirement."""
        path = _write(
            tmp_path,
            '"""This migration SAFE: does a thing."""\n'
            'op.drop_table("abandoned")\n',
        )
        violations = check_file(path)
        assert len(violations) == 1
        assert violations[0][0] == 2


    def test_pass_drop_column_with_inline_comment_and_trailing_whitespace(tmp_path: Path) -> None:
        path = _write(
            tmp_path,
            'op.drop_column("t", "c")   #   SAFE:   reason text here   \n',
        )
        assert check_file(path) == []


    def test_multiple_violations_collected(tmp_path: Path) -> None:
        path = _write(
            tmp_path,
            'op.drop_column("t", "a")\n'
            'op.drop_table("old")\n'
            'op.drop_constraint("fk", "t", type_="foreignkey")\n',
        )
        violations = check_file(path)
        assert len(violations) == 3
        assert [v[0] for v in violations] == [1, 2, 3]


    def test_non_destructive_ops_ignored(tmp_path: Path) -> None:
        """op.add_column, op.create_table etc. are NOT destructive and need no annotation."""
        path = _write(
            tmp_path,
            'op.add_column("t", sa.Column("new", sa.String()))\n'
            'op.create_table("new_t", sa.Column("id", sa.Integer()))\n'
            'op.create_foreign_key("fk_name", "t", "u", ["c"], ["id"])\n',
        )
        assert check_file(path) == []


    def test_redos_safe_on_pathological_input(tmp_path: Path) -> None:
        """T-03-01 defense: script handles a large pathological input in < 1s."""
        body_lines: list[str] = []
        for i in range(2000):
            body_lines.append("# SAFE: batch cleanup reason " + str(i))
            body_lines.append(f'op.drop_column("t{i}", "c{i}")')
        path = _write(tmp_path, "\n".join(body_lines) + "\n")

        start = time.perf_counter()
        violations = check_file(path)
        elapsed = time.perf_counter() - start

        assert violations == []
        assert elapsed < 1.0, f"check_file took {elapsed:.3f}s on 4000-line input — ReDoS risk"

Formatting: `black --config pyproject.toml --line-length 120` and `isort .` after write; pyright strict.

Worker safety: all tests use `tmp_path` (pytest-provided per-test directory); no shared state. Safe under `-n auto --dist=loadfile`.
  </action>
  <verify>
    <automated>cd /home/tyler-webb/Documents/Github/CarModPicker/backend && pytest -n auto -x tests/test_check_migrations.py --no-cov -v</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_check_migrations.py` exists
    - `grep -c '^def test_' backend/tests/test_check_migrations.py` returns at least 9
    - `cd backend && pytest -n auto -x tests/test_check_migrations.py --no-cov` exits 0 (all tests pass)
    - `cd backend && pytest -n auto -x tests/test_check_migrations.py::test_redos_safe_on_pathological_input --no-cov` completes in < 3 seconds wall time
    - File contains the literal string `test_fail_safe_in_docstring_does_not_count`
    - File contains the literal string `test_redos_safe_on_pathological_input`
    - `cd backend && pyright tests/test_check_migrations.py` reports 0 errors
    - `cd backend && black --config pyproject.toml --line-length 120 --check tests/test_check_migrations.py` exits 0
  </acceptance_criteria>
  <done>All unit tests pass under -n auto; coverage includes both PASS shapes, both FAIL shapes, the docstring-SAFE false-negative defense, the ReDoS defense, and the non-destructive-ops-ignored behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the DROP guard into backend-ci.yml</name>
  <files>.github/workflows/backend-ci.yml</files>
  <read_first>
    - .github/workflows/backend-ci.yml (entire file — executor must understand step ordering)
    - .planning/phases/01-safety-nets-ci-hardening/01-PATTERNS.md §4 (exact insertion point — between `Scan dependencies for vulnerabilities` and `Run tests with coverage`)
    - .planning/phases/01-safety-nets-ci-hardening/01-RESEARCH.md §Code Example 3 (literal YAML shape to insert)
    - .planning/phases/01-safety-nets-ci-hardening/01-CONTEXT.md D-07 (CI-only; no pre-commit)
  </read_first>
  <action>
Edit `.github/workflows/backend-ci.yml`. Insert the new step between the existing `Scan dependencies for vulnerabilities` step and the existing `Run tests with coverage` step.

Locate the exact text block:

        - name: Scan dependencies for vulnerabilities
          run: |
            cd backend
            # Ignore ecdsa CVE-2024-23342: Not exploitable in this codebase as we use HS256 (HMAC) algorithm, not ECDSA-based algorithms
            pip-audit -r requirements.txt --ignore-vuln CVE-2024-23342

        - name: Run tests with coverage

Replace with:

        - name: Scan dependencies for vulnerabilities
          run: |
            cd backend
            # Ignore ecdsa CVE-2024-23342: Not exploitable in this codebase as we use HS256 (HMAC) algorithm, not ECDSA-based algorithms
            pip-audit -r requirements.txt --ignore-vuln CVE-2024-23342

        - name: Check migrations for unannotated destructive operations
          run: |
            python backend/scripts/check_migrations.py

        - name: Run tests with coverage

The script is executed from the repo root (the default `actions/checkout@v5` working directory — NO `cd backend` prefix). The script's own `REPO_ROOT = Path(__file__).resolve().parents[2]` is CWD-independent.

Do NOT:
- Move this step elsewhere. Placement is locked by CONTEXT.md D-07 rationale: BEFORE coverage so a regression cannot slip through while the coverage baseline is being measured.
- Add extra flags or env vars. The script takes no arguments and reads no env vars.
- Change any other step.

YAML validity check: `yamllint .github/workflows/backend-ci.yml` (if yamllint is available) — else rely on GitHub's built-in schema validation at PR time.
  </action>
  <verify>
    <automated>python3 -c "import yaml; y = yaml.safe_load(open('/home/tyler-webb/Documents/Github/CarModPicker/.github/workflows/backend-ci.yml')); steps = y['jobs']['test']['steps']; names = [s.get('name','') for s in steps]; i_scan = next(i for i,n in enumerate(names) if 'Scan dependencies' in n); i_check = next(i for i,n in enumerate(names) if 'Check migrations' in n); i_tests = next(i for i,n in enumerate(names) if 'Run tests with coverage' in n); assert i_scan < i_check < i_tests, f'Order: scan={i_scan} check={i_check} tests={i_tests}'; print('OK order:', names[i_check])"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/backend-ci.yml` parses as valid YAML (`python -c "import yaml; yaml.safe_load(open('.github/workflows/backend-ci.yml'))"` exits 0)
    - The workflow contains a step named exactly `Check migrations for unannotated destructive operations` (command: `grep -c 'Check migrations for unannotated destructive operations' .github/workflows/backend-ci.yml` returns 1)
    - The step runs `python backend/scripts/check_migrations.py` (command: `grep -c 'python backend/scripts/check_migrations.py' .github/workflows/backend-ci.yml` returns 1)
    - Step ordering: the new step appears AFTER `Scan dependencies for vulnerabilities` and BEFORE `Run tests with coverage` (verified by the Python-yaml verification command above)
    - No other step was modified (`git diff .github/workflows/backend-ci.yml` shows only the new 3-line step block inserted)
    - Simulated-CI invocation works locally: `cd /home/tyler-webb/Documents/Github/CarModPicker && python backend/scripts/check_migrations.py` exits 0
  </acceptance_criteria>
  <done>backend-ci.yml has the new step placed between Scan dependencies and Run tests with coverage; YAML parses; local invocation of the script exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author → CI | Untrusted migration file content flows into the DROP-guard regex |
| CI runner → check_migrations.py | Script reads files from disk but performs no network/DB I/O and writes only to stdout |
| Future PR migration → legacy annotations | The guard must distinguish legitimate `# SAFE:` annotations from accidental `SAFE:` occurrences in docstrings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | Regex matching in check_migrations.py | mitigate | Both regexes (`DESTRUCTIVE_OP_RE`, `SAFE_ANNOTATION_RE`, `INLINE_SAFE_RE`) use ONLY bounded quantifiers (`\s*`, `\s+`). No catastrophic-backtracking constructs. `test_redos_safe_on_pathological_input` asserts `check_file` on a 4000-line pathological input completes in < 1 second. If a PR introduces a pathological migration (e.g. a huge file with many destructive ops), CI cost is bounded. |
| T-03-02 | Tampering (false-negative) | Preceding-line SAFE detection | mitigate | `SAFE_ANNOTATION_RE = r"^\s*#\s*SAFE:\s*\S"` is anchored to start-of-line (leading whitespace allowed). A `SAFE:` token embedded in a docstring line or comment-after-code does NOT match. `test_fail_safe_in_docstring_does_not_count` exercises this explicitly. |
| T-03-03 | Elevation of Privilege | check_migrations.py execution surface | mitigate | Script is read-only (only `.read_text()` calls). No subprocess, no network, no DB, no environment mutation. `REPO_ROOT` is derived from `__file__` only — no `os.environ` lookups for paths. Script cannot be tricked into scanning an unrelated directory. |
| T-03-04 | Information Disclosure | stdout from the script on failure | accept | Failure output includes offending line text. If a migration file contains a secret committed by mistake (already a separate problem), the guard would print it. Acceptable: any secret-in-migration is a higher-order problem, and CI logs are already read-access-gated to repo collaborators. |
| T-03-05 | Tampering | `.github/workflows/backend-ci.yml` step reordering | accept | If a future PR moves the new step to after `Run tests with coverage`, the coverage baseline could be measured on a drop-regression PR. Mitigation: acceptance criterion in Task 3 asserts strict `i_scan < i_check < i_tests` ordering, caught by any future similar planning phase. |

Severity summary: all threats low/medium. The highest-value mitigations are T-03-01 (unit-tested) and T-03-02 (unit-tested) — both land as acceptance criteria in Task 2.
</threat_model>

<verification>
- `cd /home/tyler-webb/Documents/Github/CarModPicker && python backend/scripts/check_migrations.py` exits 0 on the current tree (after Plan 02's annotations landed)
- `cd backend && pytest -n auto -x tests/test_check_migrations.py --no-cov` exits 0 (all unit tests pass)
- `cd backend && pyright scripts/check_migrations.py tests/test_check_migrations.py` reports 0 errors
- `cd backend && black --config pyproject.toml --line-length 120 --check scripts/check_migrations.py tests/test_check_migrations.py` exits 0
- `cd backend && isort --check-only scripts/check_migrations.py tests/test_check_migrations.py` exits 0
- `.github/workflows/backend-ci.yml` parses as valid YAML AND contains a step named `Check migrations for unannotated destructive operations` between `Scan dependencies for vulnerabilities` and `Run tests with coverage`
- Full backend test suite still green: `cd backend && pytest -n auto -x --no-cov` exits 0
</verification>

<success_criteria>
1. `backend/scripts/check_migrations.py` exists, is executable, runs in < 1s on the current repo, and exits 0 (tree is clean after Plan 02)
2. `backend/tests/test_check_migrations.py` covers the 4 canonical cases + ReDoS + docstring-SAFE defense + non-destructive-ops-ignored
3. `.github/workflows/backend-ci.yml` has the new CI step placed BEFORE `Run tests with coverage`
4. All formatters/type-checkers pass on all new/modified files
5. No other code in the repo is modified (no drive-by edits to migrations, base_class.py, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-nets-ci-hardening/01-03-SUMMARY.md` with:
- The exact regex patterns committed (`DESTRUCTIVE_OP_RE`, `SAFE_ANNOTATION_RE`, `INLINE_SAFE_RE`) — downstream plans may grep for them
- Proof that `python backend/scripts/check_migrations.py` exits 0 on the current tree
- CI step name and placement (between `Scan dependencies` and `Run tests with coverage`)
- Handoff note for Plan 04 (coverage gates): the DROP guard is now live, so the baseline-measurement PR can land safely without accidentally admitting an unannotated `drop_*` in the same change window.
</output>
