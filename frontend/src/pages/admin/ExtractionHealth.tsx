// S11/T03 will replace this body with the real Extraction Health page
// (compliance counts, per-tier coverage gradient, 7d per-adapter failure rates).
// T02 ships the lazy-importable export so the /admin/extraction-health route
// resolves and the App.coverage drift-guard test sees the page.
export default function ExtractionHealth() {
  return null;
}
