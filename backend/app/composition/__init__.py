"""What the two composition roots share.

`domains` names the nine domains and their routers, `wiring` assembles the shared
application, and `app` is the every-domain process `app/main.py` serves.
"""
