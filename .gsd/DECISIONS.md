# Decisions

<!-- Append-only register of architectural and pattern decisions -->

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|

---

## Decisions Table

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M002/S01 | architecture | Schema contract for structured extraction | Per-category Pydantic models registered in a central SpecRegistry; adapters declare category targets via class attribute | Pydantic v2 is the project's validation library across api/schemas/. Type safety + IDE autocomplete inside adapters; errors surface at the adapter boundary, not silently at ingest. Adapters fail silently today (junk extraction → null fields → still ingests) — strongly typed extraction puts a hard barrier between 'tried to extract' and 'ingest got something useful'. Contract is extractor-agnostic so M003 can drop in an LLM extractor without restructuring. | No — foundational contract for all 111 adapters | collaborative |
| D002 | M002/S02 | architecture | Universal-field extraction strategy | Hybrid: shared utilities in crawlers/parsing.py, auto-run by RetailerCrawlerAdapter base class; adapters can override or suppress per field | Universal coverage (weight, material, finish, warranty, fitment notes) across all 111 adapters without per-adapter retrofit. S3 self-archive lets us re-run the extractor against stored HTML cheaply when tuning; iteration is cheap. False-positive risk (extracting 'weight' from a shipping table) mitigated by confidence flags on extracted fields. | Yes — confidence threshold and per-field suppression conventions can evolve based on real-world false-positive rates | collaborative |
| D003 | M002/S08 | library | Frontend design system substrate | shadcn-style copy-into-repo Radix primitives on Tailwind 4 with CSS-variable token layer; dark palette locked in M002, light mode deferred unless free | Radix brings accessibility, keyboard nav, focus management, and dark-mode-friendly behavior for free. Copy-into-repo means we own the code and can restyle freely. Replaces accumulated hand-rolled drift in components/common/. Tailwind 4 already migrated in v1.0; React 19 + TS is shadcn-compatible. | No — substrate that all 20 pages will consume; switching after the ripple would be churn | collaborative |
| D004 | M002/S05 | architecture | Price-history aggregation strategy | Query-time aggregation with explicit perf gate at 10x current traffic; materialized part_price_summary table opens as fix-task only if perf gate misses | Write path already exists in part_listing_service.py. Building denormalized table in M002 is premature given Redis and read replicas are deferred (R042/R043). Well-indexed Postgres handles this access pattern fine to 1M+ rows. (part_listing_id, observed_at) composite is already in place. Phrasing the perf gate against 10x traffic (not localhost feel) keeps the decision honest as the user scales toward real users. | Yes — materialized table opens as fix-task R036 if R019 perf gate fails | collaborative |
| D005 | M002 | scope | LLM extraction strategy | Deterministic-only extraction in M002; schema contract designed to be extractor-agnostic so an LLM extractor drops into M003 alongside LLM build helper/planner | Cost analysis (200k catalog × 20% × Haiku-cached ≈ $150–$200 one-time, ~$2k/year recurring) is acceptable but commits M002 to LLM cost, eval harness, and prompt management — infrastructure that should be shared with M003's user-facing LLM tools, not built twice. Deferring keeps M002 focused on enrichment + UX without LLM-extraction operational burden. | Yes — if M002 deterministic coverage proves too sparse for the comparative-display UX to feel alive, LLM-as-fallback could be pulled forward | collaborative |
| D006 | M002/S08 | pattern | Frontend visual-regression strategy | Playwright toHaveScreenshot() at three breakpoints (mobile/tablet/desktop) for kitchen-sink + three priority pages; manual UAT smoke checklist for the other ~17 ripple pages | Three breakpoints (not two) is the honest bar for a product scaling to real users. Kitchen-sink catches primitive-level regressions across all 20 pages from one spec. Priority pages (build-list view, parts catalog, admin) get full Playwright coverage because they're highest-blast-radius and most likely to regress when other pages are reskinned. Manual UAT for the ~17 ripple pages avoids flake debt and trusts that primitive-level coverage handles 90% of real regressions. | Yes — if a regression pattern emerges mid-ripple, expand Playwright coverage to additional pages | collaborative |
| D007 | M002/S02 | architecture | Bridge from infer_category() DB names to SpecRegistry sub-slugs, with a UniversalSpec catch-all | Add a category_to_subslug() bridge that maps DB category names ('suspension'/'brakes'/'engine') to registry sub-slugs ('coilover'/'brake'/'turbo') using keyword scoring over the part name+description, falling through to a 'universal' catch-all slug bound to a UniversalSpec(CategorySpec) model that declares only the 5 universal fields. ingest_payload uses the bridge before SpecRegistry.resolve(). | S01 left a gap (MEM016): infer_category returns DB category names like 'suspension', SpecRegistry was keyed on sub-slugs like 'coilover', so the validation hook never fired in production. Two options: (a) bridge category-name to sub-slug at resolve-time, or (b) re-key the registry to category names. Option (a) keeps slugs sub-categorical (room for 'damper' / 'bush_kit' under suspension later without forcing a re-key) and preserves S01's existing slug semantics. UniversalSpec catch-all means even unmapped categories validate their universal fields cleanly, so coverage grows continuously across the catalog rather than only on the 3 sub-categories with concrete specs. | Yes — when sub-category coverage expands in S03 (more concrete specs per category), the bridge keyword dict grows accordingly. Re-keying the registry on category names becomes attractive only if every DB category gets exactly one concrete spec. | agent |
| D008 | M002/S04 | architecture | Source of admin extraction-health failure-rate signal | Read failure rate from crawled_pages.parse_status (DB) over a 7-day window, NOT from the existing CloudWatch ExtractionFailureRate EMF metric. | EMF is fire-and-forget and gated to staging/production (cloudwatch_emf.py D-20). Reading it back requires CloudWatch GetMetricData IAM + a network round-trip and yields no signal in dev/test. crawled_pages.parse_status is the same authoritative state — both archive_rescrape.rescrape_crawled_page_from_archive and runner.py write parse_status='failed' when ingest fails (the SAME path that emits ExtractionFailureRate), so the DB count is exact. Single SQL round-trip per request, indexed on (source, last_parsed_at), works in dev + tests + prod uniformly. Keeps the admin endpoint dependency-free of CloudWatch — operator can use the page even during a CloudWatch outage. The EMF stream remains the alerting/dashboard surface for cross-region aggregation. | Yes — if multi-region read replicas land we may shift to a CloudWatch read for cross-region aggregation; or add a denormalized adapter_failure_rate_7d materialized count if the per-request 6-query budget becomes a hot path. | agent |
| D009 |  | architecture | Source of admin extraction-health failure-rate signal | Read failure rate from crawled_pages.parse_status (DB) over a 7-day rolling window, NOT from the existing CloudWatch ExtractionFailureRate EMF metric. | EMF is fire-and-forget and gated to staging/production (cloudwatch_emf.py D-20). Reading it back requires CloudWatch GetMetricData IAM + a network round-trip and yields no signal in dev/test. crawled_pages.parse_status is the same authoritative state — both archive_rescrape.rescrape_crawled_page_from_archive and runner.py write parse_status='failed' when ingest fails (the same path that emits EMF). One SQL round-trip per request, works in dev + tests, no IAM/network dependency.</rationale>
<parameter name="when_context">M002/S04 T02: admin extraction-health endpoint | yes — if multi-region read replicas land we may shift to a CloudWatch read for cross-region aggregation | agent |
| D010 |  | M002/S07/T03 | SES send-failure handling in price-drop alert evaluator: leave last_fired_at unchanged on False return; log price_alert_email_sent: success=False; emit price_alert_evaluated: verdict=fired anyway | On send-failure, leave last_fired_at unchanged so the next observation retries (idempotent retry semantics) — but still log the verdict as 'fired' since this was a fire-attempt evaluation, not a suppression or skip. | Plan said "send_price_drop_alert_email returns False on SES failure (matches send_verify_email contract); the evaluator logs the False and leaves last_fired_at unchanged so the next observation retries (idempotent retry)." The retry semantics MUST hit on the next observation — if we set last_fired_at on a False send, the user would never get notified once SES recovered. Logging verdict=fired reflects evaluator intent (we attempted to fire); the success/failure of the actual send is already logged separately as price_alert_email_sent: success=&lt;bool&gt;. | true | agent |
| D011 | M002/S13/T01 — at M002 close, when the live UAT walkthrough demanded an actual SES send to verify the full extraction → ingest → UI → alert email loop end-to-end | milestone-close | Whether to verify M002's price-drop alert path with a live SES round-trip vs. a unit-test-only path | Live SES send to a `+`-suffix fixture inbox (`tylert2610+m002-uat@gmail.com`) at M002 close, with operator-driven UAT walkthrough during S13/T01 | R010 explicitly requires 'an integration test fires a real email to a fixture address' — unit-only would miss SES IAM-role auth, React Email rendering, and unsubscribe-token redirect behavior. The `+`-suffix fixture inbox lets the operator clean up the M002 UAT mail without polluting the primary inbox; redaction constraints (no recipient addresses or unsubscribe tokens in committed evidence) prevent leakage. | no — established the M002 close-gate pattern; future milestones touching SES should reuse the same `+`-suffix fixture inbox approach | agent |
| D012 | M003 | arch | How to retire the legacy CSS substrate in `frontend/src/index.css` so design-system drift cannot recur | Hard-delete in two passes inside one slice (S04): pass 1 removes the `:root` legacy palette block + `@theme` palette mirror + `.glass*` + `.btn-primary/secondary/outline` + `.card*` + `.input-modern`; pass 2 removes `.text-gradient` + `.shadow-glow` + `.border-gradient` + `.skeleton` + `.hero-gradient` + all 11 keyframes + their `.animate-*` consumer classes. Targeted tokenized replacements (e.g. Home entrance animation) land as atomic commits in `tokens.css` BEFORE the deletion. | As long as both substrates coexist, drift recurs because devs (and LLM agents) keep reaching for the legacy classes. Two-pass keeps each deletion cliff bounded — pass 1 is the high-traffic surface this milestone is explicitly about; pass 2 is where targeted gap-fill additions are most likely. `vite build` succeeding with the `@theme` palette removed is the load-bearing proof that no consumer references the deleted utilities. | No | collaborative |
| D013 | M003 | convention | Whether the legacy Tailwind `@theme` palette in `index.css` should remain as utilities (`bg-primary-500`, `text-neutral-300`, `text-accent-emerald`, etc.) or be deleted entirely so the legacy classes no longer compile | Delete the `@theme` palette block entirely after consumers migrate. Any surviving raw palette utility becomes a `vite build` error, enforcing the semantic-token contract via tooling. | "Hard delete" at maximum strength means the legacy palette utilities don't resolve. Anything softer leaves both forms valid and drift recurs because both `bg-primary-500` and `bg-primary` (semantic) still work. Build-error enforcement is the cheapest possible drift gate — CI catches it, no human review needed. | No | human |
| D014 | M003 | convention | Decision rights for information-architecture changes surfaced during the polish pass (collapsing redundant blocks, removing redundant headers, deduping stat strips, restructuring page layout) | Apply IA changes on agent judgment up to medium-impact (combining adjacent cards on one page, removing a redundant header, deduping a stats strip, the locked ViewPart price-block collapse exemplar). Surface a proposal and wait for user approval for high-impact changes (removing a feature, restructuring a page's primary layout, navigation changes). All applied changes are documented in the slice summary with rationale. | The polish pass surfaces UX problems that aren't visible without doing the systematic visit. Medium-impact decisions are reversible per-page and benefit from agent judgment so the pass doesn't stall on every micro-decision. High-impact decisions affect navigation or feature surfaces and need explicit user sign-off because the user's product mental model is at stake. | Yes — if a medium-impact change turns out to surprise the user, surface threshold lowers | collaborative |
| D015 | M003 | pattern | Migration ordering — global by-token sweeps vs. per-page sweep for the bulk consumer migration | Hybrid. Phase 1 (S01) is global by-token sweeps — replace one legacy class everywhere per atomic commit (e.g. all `text-emerald-400` → `text-success` across all consumer files in one commit). Phase 2 (S02 + S05) is per-page work for structural cleanup that doesn't fit a global swap (`glass-card` removal, layout fixes, animation replacements, IA collapses). | The bulk of the migration is mechanical class swaps and benefits from global atomic commits — easier to bisect, faster to ship, fewer commits, and visual regression catches issues better than bisect at this scale. The structural work (layout, IA, replacement animations) is intrinsically per-page and benefits from a focused per-page pass after the globals clear the noise. | Yes — if a global sweep produces a hard-to-review diff, fall back to per-page for that token class | collaborative |
| D016 | M003 | pattern | Visual-regression baseline refresh strategy for design-system migration slices | Refresh Playwright `toHaveScreenshot()` baselines per slice with maximum coverage: every page touched by a slice gets baselines refreshed at 360 / 768 / 1280 (mobile / tablet / desktop), and the diff is reviewed against expected token-swap diffs only before commit. No pragmatic carve-out for secondary pages — the long tail gets the same coverage as priority pages. | M002 burned on this (MEM066, MEM140) — batch baseline refresh at the end was the painful one because legitimate diffs (~100+) hide real regressions. Per-slice review distributes the cost and keeps each review honest: real regressions stand out against ~5 expected diffs, not ~150. Maximum coverage means no surface ships without verification. | Yes — if per-slice cost becomes prohibitive, narrow to priority pages and accept end-of-milestone tail review | collaborative |
| D017 | M003 | convention | How targeted gap-fill additions to the design system (new semantic tokens, new `ui/*` primitives, new tokenized keyframes) should be authored when the migration surfaces a real gap | Stop the migration, add the token / primitive / keyframe in `tokens.css` or `components/ui/`, commit as a standalone atomic commit with rationale, then resume. Each addition gets its own commit with the gap-fill justification documented in the message. The bias is consumption of the existing system — additions require concrete justification ("X pages need this and there's no clean way to express it with what we have"), not "this would be nice." | Friction enforces the consumption bias. If additions land inline in migration commits, it's easy to keep adding "just one more"; atomic commits with rationale force the question "is this gap real?" and keep the audit trail clean for future devs. Subject to R017 — additions land in `tokens.css` or `components/ui/`, never in `components/common/` or `components/buttons/`. | No | human |
| D018 | M003/S03 | pattern | Information architecture of the price-related blocks on `/parts/:id` (ViewPart) — currently shows two redundant blocks ("Price summary (90 days)" + "Price by retailer") that overlap on retailer name + last price + observation timing | Aggressive collapse. ViewPart shows ONE "Price by retailer" table with columns for retailer name, last observed price, sparkline, observation timing, and outbound `View at retailer` link. The standalone summary stats card is either dropped or compressed to a one-line header above the table (e.g. `$X–$Y across N retailers, last observed Z`). Both blocks are inline in `ViewPart.tsx` today — this is an in-place refactor, not a component swap. | The two blocks overlap on retailer name + last price + observation timing; the unique signal in the second block is the outbound `View at retailer` link, which is the business value (outlinking). Collapsing to one block + preserving outbound links resolves the redundancy without losing information. Conservative alternatives (keeping the summary card and adding outbound links to its rows) preserve clutter without UX benefit. | Yes — if the collapsed block loses useful summary information at scale, restore a one-line header above the table | human |
| D019 | M003/S03 | convention | Outbound retailer link safety attributes and visual affordance | Every outbound `View at retailer` link uses `target="_blank" rel="noopener noreferrer"` and renders a small Lucide external-link icon affordance to signal that clicking leaves carmodpicker.com. No confirmation interstitial; no `rel="sponsored"` (affiliate program isn't established). | Outlinking is the business value of the price-by-retailer block. Standard link-safety attributes prevent reverse-tabnabbing; the icon affordance signals navigation intent without adding friction. `rel="sponsored"` is premature for an unestablished affiliate program; an interstitial would add user friction without clear benefit at this product stage. | Yes — `rel="sponsored"` revisits when affiliate program is established | collaborative |
| D020 | M003/S05 planning, autonomous mode (gsd_plan_slice invoked without human in loop) | workflow | How to handle high-impact information-architecture changes surfaced during the M003/S05 polish pass when running in autonomous mode (no human available to approve) | Defer all high-impact IA changes to S06 UAT walkthrough rather than guessing during S05. The 6 deferred items are: (1) auth-shell unification (Login/Register/ExtensionAuth glass-card vs ForgotPassword/Confirm AuthCard vocabulary), (2) ContactUs 3-identical-card collapse, (3) BuildListsCatalog sidebar drawer/accordion at narrow widths, (4) ViewBuildLog markdown renderer Tailwind Typography prose-plugin adoption, (5) UserManagement 11-column table responsive redesign (card layout vs column-hide vs detail-dialog), (6) SystemAdmin DangerActionPanel extraction across 10 near-identical danger sections. S05 in autonomous mode performs only mechanical token migration, primitive consumption, and bug fixes; structural changes wait for S06's manual UAT. | Per MEM146, decision rights for high-impact IA changes (removing features, restructuring page layouts, navigation changes) require user approval. Auto-mode cannot ask. Guessing risks mislabeled as "approved cleanup" what should be a deliberate user choice — and the cleaner alternative is to surface the 6 items in S05-SUMMARY.md's Deferrals section so S06's UAT walkthrough has a concrete list to walk through with the user. S05 still ships meaningful value (token sweep, primitive extraction, invisible-text bug fix, polish-coverage spec for the 33 unbaselined routes) without requiring human-in-the-loop on judgment calls. | If a future autonomous run gains an interactive escalation path (e.g. surfacing IA proposals via a side channel for async approval), revisit and consider applying medium-to-high IA changes within S05 instead of deferring. | agent |
